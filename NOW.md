# 当前工作

## 状态

`RESEARCH_ONLY`

## 当前唯一目标

固定结束日期验证已经完成。当前需要决定是否开始一个新的简单策略研究任务；在明确批准新假设前，不继续自动派生工作。

## 完成条件

- 新任务必须先说明策略假设、固定参数、数据区间和停止条件。
- 一次只验证一个假设，不同时扩建运行系统。
- 结果必须同时报告策略收益、同期市场变化、样本数、回撤和强制平仓影响。

## 停止线

- 不调参，不运行 Hyperopt，不新增策略。
- 不启动 Dry-run 或实盘。
- 不读取账户、密钥或私有交易数据。
- 当前 EMA50/EMA200 基线停止继续投入，不用新增系统代码掩盖证据不足。
