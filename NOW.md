# 当前工作

## 状态

`RESEARCH_ONLY`

## 当前唯一目标

在不调整策略参数的前提下，使用多个预先固定的回测结束日期，检查 BTC 趋势基线的结果是否持续依赖最后一笔开放仓位。

## 完成条件

- 结束日期在运行前固定并记录。
- 所有运行使用相同策略、数据口径、仓位和成本假设。
- 分别报告正常退出收益与期末强制平仓贡献。
- 给出继续研究或停止投入的明确结论。

## 停止线

- 不调参，不运行 Hyperopt，不新增策略。
- 不启动 Dry-run 或实盘。
- 不读取账户、密钥或私有交易数据。
- 结果不足时如实停止，不用新增系统代码掩盖证据不足。
