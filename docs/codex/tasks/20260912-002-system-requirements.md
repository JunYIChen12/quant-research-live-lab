# 20260912-002 系统需求与模块边界梳理

## 身份与基线

- 创建日期：2026-09-12；最近整理日期：2026-09-14，Asia/Shanghai。
- 状态：ANALYZING。
- 当前责任线程：00｜项目控制塔。
- 唯一根目录：D:/CodexProjects/projects/quant-research-live-lab。
- Git 分支：codex/workbench-init。
- 原产品基线（2026-09-12 启动 HEAD）：a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28。
- 2026-09-14 计划同步前 HEAD：60abb777ff079a301b70685cbdfd4ecffc96df3c；与 origin/codex/workbench-init 一致，工作区干净。
- 2026-09-14 本次总路线修订前 HEAD：eb1c62afdf4e3df10565e9841a4e3f33b5786199；分支不变，工作区干净，暂存区为空。本次仅本地文档整理，不提交或推送。
- 写入前文档快照提交：78165c8112779701cd3ace8c9cca88c9576a315d，保留 2026-09-12 工作台和初次需求记录。
- origin fetch/push：https://github.com/JunYIChen12/quant-research-live-lab.git；2026-09-13 已核对远程引用，详见本次证据。动态 HEAD 以 Git 核对，不把基线当永久当前版本。
- 环境：Windows / PowerShell；未检查、安装或修改业务运行环境。
- 讨论进度：用户已于 2026-09-14 确认第一期系统框架书面稿，并决定评分公式、参数阈值、接口字段和异常细节留到第二期讨论。第一期设计结束；任务仍是需求分析，没有进入业务实施或独立验收。

## 下次从这里继续

先读取 [当前任务](../CURRENT_TASK.md)、本文件和现行安全规则，重新核对 Git 基线及已有修改。继续使用 Superpowers brainstorming，每次只问一个关键问题；未经确认，不启动子代理，不修改产品或运行规则。

当前有效入口为 [第一期系统框架设计](20260912-002-phase-1-framework.md)，需求细节见下文“当前契约（2026-09-14 更新）”。首期仍仅模拟交易；核心目标为用户填写的净盈利金额和有效运行时长，原 ROI/固定 2 小时约定不再沿用。

下一步继续同一任务的第二期细节分析，每次只处理一个关键问题；不修复或合并 PR #7，不开始业务开发。策略准入评分、资金分配、接口和异常处理仍须设计，但不要求用户凭空猜技术阈值。

用户已采纳独立验证、三类结果、评估前固定标准及保留全部候选记录；这不等于批准任何候选策略或进入实盘。只继续当前分析，不新建子代理，不自动开展业务开发。

## 2026-09-14 第一期框架确认

### 当前结论

用户确认第一期只固定系统框架，评分公式、算法、字段、阈值和验收场景放在第二期。完整书面稿见 [第一期系统框架设计](20260912-002-phase-1-framework.md)。该稿是当前框架入口；下方七阶段路线、Freqtrade 静态对照和 PR #7 记录只保留为历史分析输入，不再决定当前实施顺序。

框架固定了以下边界：项目控制层统一管理轮次、策略资格、资金、风险、账本和恢复；Freqtrade 是唯一回测、Dry-run 和执行底座；Codex 只研究候选；策略只提交交易意图，控制层可以批准、缩小或拒绝，但不能放大或改变方向；持仓版本冻结到平仓核对完成；同一合约只归属一个策略；临时问题可在核对通过后自动恢复，本轮结束不能自动重启；第一版只做 Dry-run。

事实：现行项目规则仍禁止自动增加资本、杠杆、持仓数、合约范围或亏损上限，安全模型仍规定杠杆为 1 且最多一个仓位，发布政策仍要求人工批准和冻结发布身份。本次没有修改这些规则。

冲突：框架目标允许用户预设边界内的多合约并行和模拟候选自动晋级，但当前安全与发布规则尚不允许直接实施。第二期必须提出兼容方案或保持更严格现状，不能用设计文档绕过规则。

控制塔结论：用户已复核并确认书面稿，第一期设计正式结束。整个任务仍为 ANALYZING，不达到 READY。

### 第二期边界

第二期讨论策略评分、资金与仓位计算、信号时效、事件优先级、账本字段、接口错误语义、核对与重试、通知和界面细节、规则冲突处理及验收场景。未经第二期确认，不拆实施任务，不创建实施或验收线程。

### 本次授权与记录

用户先授权控制塔整理第一期框架稿并更新任务入口，随后确认第一期并要求按 Git 治理方案提交到远程。本次只新增框架稿并修改本文件和 CURRENT_TASK.md；保留此前未提交修改，不更新 TASKS.md 或 PROJECT_STATE.md。允许提交并普通推送到 `origin/codex/workbench-init`；不直接更新 main，不创建或合并 PR，不打标签、不部署，不修改代码、安全规则、发布政策或运行环境。

提交前远程核对：2026-09-14 执行 `git fetch origin --prune` 成功；本地 HEAD 与 `origin/codex/workbench-init` 均为 `eb1c62afdf4e3df10565e9841a4e3f33b5786199`，领先/落后为 0/0。远程没有需要先合并的提交，禁止强推仍然适用。

写入前核对：唯一目录 D:/CodexProjects/projects/quant-research-live-lab，分支 codex/workbench-init，HEAD eb1c62afdf4e3df10565e9841a4e3f33b5786199；工作区已有 CURRENT_TASK.md 和本任务文件两处未提交修改。共享与项目工作流、项目状态、适用 AGENTS、现行架构、安全模型、发布政策及 migration 说明已回读；migration 仍无正式交接记录。

失败记录：PowerShell 管道调用 apply_patch 两次因补丁参数不是 UTF-8 而失败；通过 apply_patch.bat 传递多行参数两次因换行被破坏而失败；WSL Bash 不存在；一次过大的任务文件补丁被执行策略拒绝。这些尝试均未写入对应目标文件。随后核对包装脚本，改为直接调用同一 Codex 补丁入口并通过 .NET ArgumentList 传递单个多行参数，分段写入成功。

写后检查：三个目标文档均通过严格 UTF-8、末尾换行、行尾空白和冲突标记检查；本地 Markdown 链接全部可解析；框架稿包含目标、模块、数据、状态、权限、冲突、第二期和完成标准，且无 TBD、TODO 或“待补充”占位。git diff --check 退出 0；HEAD 和分支未变；最终改动对象仅为 CURRENT_TASK.md、本任务文件和新增框架稿。定向敏感信息模式搜索无匹配，rg 因零匹配返回 1，按工具语义记录为未发现匹配，不冒充全面安全审计。

## 2026-09-14 旧总路线与 Freqtrade 对照（历史输入）

以下内容记录第一期框架确认前的七阶段路线、静态源码对照和 PR #7 审查证据。事实证据继续有效，但其中“修复 PR #7 后进入下一阶段”的顺序已被上方框架确认取代，不得作为当前实施授权。

### 本次决定、范围与完成条件

用户要求重新设计总计划后，对七阶段路线及“整理进现有任务文件和 CURRENT_TASK.md，不含代码实施或 Git 推送”的询问回答“确认”（E11）。本次唯一目标是将该路线和近期确认可靠落库；只修改这两个文档。旧路线与历次 Git 回执保留为历史，不延续其中的提交、推送授权。TASKS.md、PROJECT_STATE.md、产品、安全与发布规则保持不变；没有稳定工作流变更，不改 CHANGELOG.md。

完成条件：当前入口能找到新路线、下一步和有效契约；近期确认与待确认项分开；旧证据保留；状态和责任一致；差异仅含两个授权文件且文档结构、链接、空白检查通过。这里只验收资料整理，不宣布需求任务 READY、产品可用或策略盈利。

### 已确认的七阶段路线

以 Freqtrade 为优先基础，先验证适配，再逐段交付完整模拟流程，最后接入自动研究和多策略。改的是顺序，不取消已有需求。优先复用现成回测、Dry-run、订单查询与控制接口及已有界面；复用并验证项目 gates.py 和 release.py。只有证据表明现有能力不足，才补本轮目标、计时、核算、持久化停机锁、候选准入及共享风险预算，不预建另一套平台或服务。

| 阶段 | 主要目标 | 可复核通过条件，当前均未通过完整阶段验收 |
| --- | --- | --- |
| 1. 复用与差异分析 | 对照已确认需求、Freqtrade 指定版本和 PR #7，分类直接使用、配置解决、需要补充、存在阻塞 | 每条关键需求都有依据、验证方法；影响架构的障碍及处理路径明确 |
| 2. 修复并验收现有适配 | 在另行授权的任务内处理适配缺陷，建立受控 Dry-run 基础 | 实际加载制品被篡改、实际开仓金额超额、风控状态不明均被拒绝；模拟开平仓记录可核对 |
| 3. 完成一轮模拟运行 | 四项输入、首次成交计时、净盈亏核算、自动停止、主动结束、重启恢复 | 达标后扣费不足、到期、触限、部分平仓重启均可复算；不重复记账、不误解锁 |
| 4. 建立独立策略准入 | 复用分析工具，补齐数据隔离、成本、风险、证据及版本管理 | 每个候选得到合格、不合格或证据不足；生成方不能修改标准和验证结果 |
| 5. 接入 Codex 研究循环 | 运行数据反馈、生成候选、独立验证、合格晋级、继续反馈 | 候选及失败过程可追溯；额度中断保存进度，恢复研究不解除交易停机锁 |
| 6. 扩展多策略与研究范围 | 多策略共用总资金和总风险，覆盖约定的全部合约研究候选池 | 不重复分配本金，不混淆持仓归属；失格退出不误伤其他策略，数据不足品种不能入场 |
| 7. Windows 日常使用与整体验收 | 完善后台运行、原生提醒、开机核对、退出和恢复体验 | 在实际 Windows 环境核对正常使用及故障场景，留下独立、可复核的整体验收记录 |

这些是里程碑，不是七个已登记任务或七个并行线程。单策略只是中间验证步骤；Codex、多策略及全部约定合约候选池仍属于这版模拟产品。最小操作和观察入口随模拟流程提供，第 7 阶段完善日常体验，不等到最后才允许操作。工程测试策略须与正式候选隔离；没有合格策略时正确等待是合法状态，不能为推进计划降低标准。软件正确性不等于盈利证明。

### 第 1 阶段必须提前核对

- 多策略、同一合约单一持仓归属及共享预算能否由现有能力承载；不能等到第 6 阶段才发现基础限制。
- 暂停和结束状态能否跨重启可靠保留；不能把单个暂停接口当作持久化停机锁。
- 成交、手续费、资金费、浮动盈亏和预计平仓成本的数据来源是否充分、可对账且不重复计入。
- 现有 Codex 的任务提交、结果收集、额度中断及恢复路径是否可集成；桌面版可用不证明系统能自动调用。

下一项分析产物至少包含：需求编号、Freqtrade/现有项目能力、适用版本及源码或文档依据、复用分类、缺口、核验方法、尚未解决的取舍。技术资料先核对指定版本；若需要运行探针、依赖、服务或环境变更，另行确认范围，不把路线批准当运行授权。发现硬性不兼容先列阻塞及最小替代方案，不悄悄削弱需求或扩建平台。

现行单持仓、禁止自动扩大风险及人工发布要求仍有效。自动模拟晋级、多策略等冲突必须在受影响实施前审阅解决；无法安全解决就停止该项。每个后续任务沿用既有流程：分析明确范围，达到 READY 后由单一实施者修改、自测，再独立验收，由控制塔关闭。不新增工作流规则，不自动派发实施或验收线程。

### 已核对事实与尚未验证的能力

2026-09-14 通过 GitHub get_pr_info 和 list_pull_request_review_threads 核对 PR #7（Add governed Freqtrade dry-run adapter）：仍 open、未合并，head 为 8d1037fe317ebfddbc3f70b5b306a73ac4681036，base main 为 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28。它在 integration/freqtrade-dry-run，不是本地当前分支已经具有的运行成果。入口：<https://github.com/JunYIChen12/quant-research-live-lab/pull/7>。

三条未解决、未过时的 P1 审查意见分别指向：实际加载制品与清单绑定（评论 3994788029）、实际入场金额校验（3994788034）、开仓前运行时风控（3994788038）。这是远程审查记录，不是本次已复现或已修复的结论。PR 描述报告 Freqtrade 2026.8 及若干检查通过，但本次未复跑，也未核验运行实例；分析时须锁定实际版本。

控制塔推断：现有适配可作为优先调查起点，但上述审查与未验证能力不足以批准合并或宣告可用。具体复用比例、架构、指标和实施规格仍待分析；阶段 2 的修复只在达到 READY 的独立实施任务中进行。

### 2026-09-14 阶段 1：Freqtrade 复用与差异分析

本节是静态源码、PR 与本地安全内核对照，不是容器、模拟订单、交易所、账户或实盘验证。外部引擎基准以 Freqtrade `2026.8` 标签为准；当前工作分支没有该适配代码，远程 PR #7 的 head 为 `8d1037fe317ebfddbc3f70b5b306a73ac4681036`，仍为 open、未合并。PR 作者报告的镜像、pytest、ruff 和启动检查未在本轮复跑，不能替代独立验收。

| 需求或能力 | 分类 | 已核对事实与依据 | 缺口或下一步验证 |
| --- | --- | --- | --- |
| 回测、Dry-run、订单与持仓记录 | 可直接复用 | Freqtrade 2026.8 提供 Dry-run；PR #7 声明独立 SQLite、命名卷、只读候选目录和仅本机端口 | 只在阶段 2 的受控 Dry-run 验收中验证实际存储隔离、开平仓记录和恢复行为 |
| Binance USDT 永续、逐仓、1 倍、单一 BTC 合约与每笔上限 | 可通过配置约束 | PR 策略显式检查 dry-run、futures、isolated、`max_open_trades == 1`、BTC/USDT:USDT、配置 stake 不大于 10，并固定返回 1 倍杠杆 | 配置值不是实际订单金额校验；实际请求名义金额必须在开仓临界路径重新计算并拒绝超限 |
| 同一合约由一个策略持仓 | 可直接复用 | Freqtrade 2026.8 官方 FAQ 明确同一交易对同时只开一个仓位；与 R49 一致 | 策略归属、等待队列和共享预算仍由项目控制，不能交给多个机器人各自判断 |
| 冻结制品、发布批准与开仓闸门 | 需要修复，当前阻塞 | 本地 `release.py` 可校验提交、标签、清单和制品摘要；PR #7 有三条未解决 P1：实际加载文件未与清单绑定、强制开仓实际金额未校验、`evaluate_runtime_gate` 未接到开仓路径 | 阶段 2 必须先以失败测试证明三种情形都拒绝，再修复；未通过不得合并 PR 或启动 Dry-run |
| 风控失败时拒绝开仓 | 需要修复，当前阻塞 | 本地 `gates.py` 只表达纯规则，尚无交易所快照接入；Freqtrade 2026.8 用 `default_retval=True` 包装初始开仓回调，而 PR 的回调未捕获 `ReleaseGate` 异常 | 入口闸门必须对数据、清单、账本和风控的任意异常明确返回拒绝；用故障注入验证，不能仅依赖抛异常 |
| 本轮四项输入、有效计时、净盈亏、平仓核对、重启续平仓与持久停机锁 | 需要项目补充 | 现有 `gates.py` 只计算门槛；R17-R28、R43-R48 定义的是整轮生命周期，而非单笔交易参数 | 阶段 3 建立正式轮次账本和状态机；数据不可靠时禁止新开仓，未完成平仓按 R46 核对后续办 |
| 独立策略准入、Codex 研究反馈与版本晋级 | 需要项目补充 | R31-R41 要求生成方不能修改准入标准或验证结果；本地 Codex CLI 已观察到支持非交互执行和 JSONL 输出，但第三方中转 API 的自动调用路径尚未核验 | 阶段 4-5 才建立隔离数据、候选证据和额度恢复处理；研究恢复不得解除交易锁 |
| 多策略跨合约并行 | 当前阻塞 | R29 允许最终共享预算多策略，R49 已解决同一合约冲突；但现行 AGENTS 第 4 条与 `RiskLimits.max_open_positions = 1` 仍禁止自动增加仓位数 | 阶段 2-3 保持全局单仓位；阶段 6 前须经过独立的安全规则审阅、变更理由、测试和回滚设计，不能借 Freqtrade 配置绕过 |

官方与版本依据：<https://github.com/freqtrade/freqtrade/blob/2026.8/docs/faq.md>（Dry-run 和同一交易对单仓位）；<https://github.com/freqtrade/freqtrade/blob/2026.8/freqtrade/freqtradebot.py> 及 <https://github.com/freqtrade/freqtrade/blob/2026.8/freqtrade/strategy/strategy_wrapper.py>（初始开仓回调与异常默认返回值）。项目和 PR 依据：`src/quant_lab/gates.py`、`src/quant_lab/release.py`、PR #7 及其三条未解决审查意见。这里的“需要修复”和“阻塞”是控制塔基于静态证据的判断；尚未运行的行为明确保留为待验证。

### 阶段 1 结论与阶段 2 边界

结论：保留 Freqtrade 作为唯一的执行、回测和 Dry-run 引擎；不再设计第二套交易引擎，也不为每个策略启动一个机器人。项目控制层保留正式轮次账本、统一开仓否决、共享风险、策略归属和持久停机锁。该结论仅授权准备阶段 2 任务，不授权实现、合并 PR、启动容器或交易。

阶段 2 的最小范围应仅包括：(1) 实际加载的策略和配置与冻结清单一一绑定；(2) 用实际订单名义金额和固定 1 倍检查每次开仓；(3) 从可信快照调用 `evaluate_runtime_gate`，任何未知或异常都拒绝；(4) 对应失败分析、回滚说明和独立验收场景。不得处理正式轮次计时、策略生成、跨合约多仓、Windows 常驻或实盘接口。阶段 2 任务尚未创建，当前任务继续 ANALYZING，不能跳过 READY。

### 近期确认来源

来源均为控制塔任务 01a09416-4080-7b41-a5bc-a56dae7cf247（00｜项目控制塔_量化交易）。下列记录对应本次直接回读的用户消息和已完成答复；金额例子不是固定配置，路线批准不将未回答的问题自动变成确认。R24 已替换旧口径，新增 R43-R49 见当前契约。

| 来源 | 已完成提问或方案轮次 | 用户确认轮次、原话及含义 |
| --- | --- | --- |
| E11 | 01a09daf-df6f-7651-9b52-d34f4acbe8df | 01a09db9-7ec6-7352-b995-ba44b096561f：“确认”；认可七阶段路线，并授权两个文档本地整理 |
| E12 | 01a09d7d-5f88-7273-85a4-a132b5791846 | 01a09d82-e711-77f0-a7c1-bbd872fa2931：“同意”；本轮亏损硬上限启动前确定，运行中固定，仍可按批准规则提前保护 |
| E13 | 01a09d57-217f-7cc0-a568-35a6339100e9 | 01a09d5b-c701-7d40-882c-6fba290ffc10：“同意”；同时只运行一轮正式模拟，隔离试跑不算另一轮 |
| E14 | 01a09d5b-c701-7d40-882c-6fba290ffc10 | 01a09d60-0253-7993-93f6-61c537545380：“同意”；一轮内不追加或取出模拟资金 |
| E15 | 01a09d60-0253-7993-93f6-61c537545380 | 01a09d65-9ec0-7c12-a5f2-39c64471c8be：“同意”；行情可靠且无其他暂停原因时，平仓核对继续计时 |
| E16 | 01a09d82-e711-77f0-a7c1-bbd872fa2931 | 01a09d84-da69-7263-9e7f-ad6a95ce0c1d：“是”；重启核对后自动续完已启动的平仓，保留账目和锁 |
| E17 | 01a09d84-da69-7263-9e7f-ad6a95ce0c1d | 01a09d86-f36e-76f0-b7de-f0619f3c7311：“按最新结果核算”；否定仅因离线期间曾越线而追认停机 |
| E18 | 01a09d9b-37a3-71b3-b2c9-b82282a13ec5 | 01a09d9d-acab-7d33-9d04-3ca2aa1b7860：批注“就按照你这个去定义吧”；一次结束指令后平仓核对并暂停，盈利不足也不再追补 |
| E19 | 当前控制塔对话的 Freqtrade 复用与差异分析 | 用户答复“那就等待吧”；确认同一合约已有某策略持仓时，其他策略等待该仓完全结束后再竞争下一笔 |

E18 批注定位的原答复 messageId 为 msg_070965b6a1089944016aa752fbfeac87d2a1b9366d713fee01。仍待确认的建议：在整轮平仓核对期间暂时禁止修改盈利目标和时长、也不排队生效。该问题在 01a09d9f-404e-79b1-bf1d-038ab4cb8407 提出，用户随后转向 Freqtrade 和总路线，没有回答；R22 的正常运行动态修改权限不因此取消。

### 后续场景与本次核验边界

保留已有正常流程、钱和时间核算、停止与继续、异常恢复、一致性检查五项内容，纳入新路线相关阶段，不新建五个任务。后续用虚构数据核验：预计达标但扣费不足、部分平仓后重启、到期与开仓信号并发、普通空仓与无合格策略等待、Codex 恢复但交易锁仍在。将旧“运行中调低上限后超限”场景替换为“用户或系统尝试修改本轮亏损硬上限被拒绝，原值保留”；另覆盖主动结束后盈利不足不追补、离线最新核算不清零且不解已有锁。上述业务场景本次均未执行，精确并发优先级尚未定稿。

写前已执行 Get-Location、git rev-parse HEAD、git status --short --branch --untracked-files=all、git remote -v 及 git diff --check，目录/分支/HEAD 如本文件基线，工作区干净。适用父级与项目 AGENTS、共享及项目 WORKFLOW、状态、索引、指针及当前任务已核对；migration 仅 README，无未吸收交接。Node 内置模块只在内存保存两个目标和十一项保留文件的写前快照；没有新建辅助文件。

失败与补救：父级规则读取命令首次出现 PowerShell “An empty pipe element is not allowed”，改用变量承接后读取成功；read_thread 的 turnLimit=12 被拒绝，改为 10 后只返回元数据和空 items，不能据此声称读到消息。随后只读当前任务的本地 rollout，按 JSON 类型提取用户消息及 task_complete 的完成答复，核对 E11-E18。首次筛选 response_item 的 assistant/final 未找到目标，未作为缺失事实；改读明确的完成答复字段后找到来源。未读取其他项目对话、凭据或账户导出。

写后自查：git diff --check 退出 0，已回读两文件完整差异；Node 内置模块核验两个文件的严格 UTF-8、末尾换行、空白、冲突标记、围栏、12 个本地链接及 18 个表格均 PASS。R01-R48 共 48 个唯一编号，E01-E18 共 18 个唯一来源；E11-E18 的八组提问、紧接的用户确认轮次和原话与本地任务记录相符。首次编号完整性检查误用 R1 匹配原有 R01 而失败，改为解析编号数值后重跑通过，没有改动原编号。

历史与范围核验：旧六阶段计划正文及其回执、2026-09-13 推送回执、2026-09-12 从“可观察目标”开始的正文保持原样；十一项保留文件与写前快照逐字节相同。git diff --name-only HEAD 恰为本文件和 CURRENT_TASK.md；暂存区及未跟踪列表为空，分支和 HEAD 未变。状态仍为 ANALYZING，责任仍为 00｜项目控制塔，与任务索引一致。新增差异的定向私钥和令牌模式检查通过，不是全面安全审计；文档源文和表格核对不冒充浏览器视觉验收。

本次资料整理已完成，但未运行 pytest/ruff、模拟交易、Windows 运行测试、远程 CI 或独立验收；未提交、推送、合并 PR、安装依赖、启动服务或子代理。下一步仍为当前任务内的 Freqtrade 复用与差异分析，不进入业务实施。回退须另获授权后使用审阅过的反向补丁，不覆盖用户后续修改。

## 2026-09-14 旧推进计划与同步记录（历史）

本节六阶段路线、五步顺序和当时的“下一步”已由上方七阶段路线替代；保留原文与同步证据。这里的“本次”及提交、推送权限均仅指此前计划同步，不是当前授权；有效需求以下方更新后的当前契约为准。

### 确认来源与本次边界

来源为当前控制塔线程 01a09416-4080-7b41-a5bc-a56dae7cf247。用户对“复用现有基础、先打通可靠模拟流程、再接入自动研究与多策略”的路线回答“同意”，随后要求通俗解释，并在“一轮模拟交易，从开始到结束到底怎么运行”的批注中问“这个有计划吗”。控制塔给出五步细化计划后，用户要求同步到远程 Git，前次工具读取后中断，2026-09-14 再次明确“这个计划同步到远程git吧”。此授权用于保存计划，不代表逐项设计审批、业务实施或验收通过。

本次唯一可观察目标：将六阶段路线和五步细化计划保存在当前任务文件，使仓库入口能找到下一步，并核对 GitHub 工作分支包含该计划。不新建业务任务，不改变当前状态和责任人；详细设计、技术选型和执行级任务清单尚未定稿。计划不覆盖下文 R17-R42 或现行安全与发布规则。

仅修改本文件及 CURRENT_TASK.md。TASKS.md 保留本任务 ANALYZING；PROJECT_STATE.md 通过现有当前任务链接定位计划，不复制计划或改写历史快照。其他文档、代码、测试、配置、依赖和共享规则保持不变；没有稳定规则变更，因此不修改 CHANGELOG.md。

### 已认可的总体方向

保留完整的首阶段模拟自动化目标，按依赖分段交付：先检查基础是否可靠，再建立策略证据，最后扩大自动化范围。优先复用现有风险及发布校验，评估成熟行情、回测和模拟引擎，只补项目特有的轮次控制、核算与权限衔接；不推倒重写，也不为了适配工具而改变核心需求。Freqtrade 及既有适配成果仅是待评估对象，不是本次确定的技术选型或已验收能力。

| 阶段 | 主要交付 | 阶段检查点，不代表已通过 |
| --- | --- | --- |
| 1. 收口总体设计 | 一轮运行流程、权限边界、异常优先级、准入指标及校准方案；检查现有适配、行情来源和 Codex 接入可行性 | 影响下一项实施的关键冲突已解决；范围、保持不变项、允许修改对象与验证方法明确 |
| 2. 最小模拟流程 | 四项输入、模拟开平仓、完整成本与盈亏核算、有效计时、停止和重启恢复 | 用可复算场景证明账目、计时、平仓核对和停机锁正确；工程测试策略不得冒充合格策略或进入正式合格池 |
| 3. 独立验证流程 | 数据版本及隔离、候选冻结、成本评估、隔离试跑、三类结论、持续复评和退场 | 不合格或证据不足不能入场；每个结论可追溯到版本、数据、标准和全部试验记录 |
| 4. Codex 研究循环 | 运行反馈、生成新候选、独立验证、合格后自动参与模拟；额度中断和恢复 | 完成一次可追溯迭代；Codex 不可用不撤销风控，不能改准入标准或解除交易停机锁 |
| 5. 多策略与研究范围 | 多策略共用资金和总风险，明确持仓归属、动态分配及全部目标合约的候选管理 | 不重复分配本金或风险预算；策略退出不误伤其他策略；新增合约数据不足时不得入场 |
| 6. Windows 使用与整体验收 | 最小界面、原生通知、关窗后台运行、开机核对、完整退出和故障恢复 | 在实际 Windows 环境核对正常流程及断网、休眠、重启、额度耗尽、未完成平仓等场景 |

六阶段是路线，不是六个已登记或已获实施授权的大任务。Codex、多策略及目标合约全范围仍属于首阶段产品目标，只调整开发先后；第 2 阶段可提供最小操作入口，第 6 阶段完善日常使用与整体验收。候选隔离执行、文件访问权限和资源限制须设计、验证，不能只靠提示词防越权。

### 当前分析的五步细化计划

| 顺序 | 工作内容 | 可复核产物与完成条件 |
| --- | --- | --- |
| 1. 讲清正常流程 | 从填写目标、研究与验证，到模拟买卖、计时、平仓和暂停，顺序走完一轮 | 一轮交易步骤表；每步写明触发条件、动作、记录及下一状态，与既有契约一致 |
| 2. 补齐意外情况 | 处理断网、重启、平仓未完成、无合格策略、Codex 额度耗尽，以及多个条件同时触发 | 异常处理表；写明禁止动作、恢复条件和提醒，不以发出平仓命令代替已平仓 |
| 3. 确定分工与标准 | 定义核算、订单、风控、生成与验证权限，提出准入及复评指标和校准方法，检查可复用工具 | 职责与权限表、指标草案和复用结论；不要求用户猜技术阈值，未决冲突不得写成已批准 |
| 4. 写出验收场景 | 用正常、边界和失败例子逐项检查设计；区分工程正确与策略证据 | 每例有输入、操作、预期结果和复核方法，可对应需求；需用户确认的取舍仍明确标出 |
| 5. 拆成小开发任务 | 按依赖拆成一个目标、一个责任人、一个写入范围的小任务 | 用户确认相应设计后，控制塔登记当前一项任务；范围和验收方法完备且达到 READY 才交实施，实施后独立验收 |

第 1-4 步仍属于本分析任务，不另起多个业务任务。总体路线已获认可，具体设计和执行级任务清单尚待形成、核对及确认；第 5 步不得将文档生成或路线获认可当作 READY。已有需求不重复盘问，由控制塔先给草案，每次只问一个真正影响行为的关键问题。

验收清单至少保留下列已讨论场景，具体输入数值和复核命令待设计时补齐，当前均未执行：

- 触及亏损上限时禁止新开仓，完成平仓核对并保留暂停锁；平仓未完成时不能声称已空仓。
- 重启后保留本轮盈亏与停机锁，核对未通过不得恢复；不能通过重启重新获得风险额度。
- 仅 Codex 额度不可用时保存研究进度，执行与独立风控仍按规则运行；额度恢复不解锁交易。
- 预计达标但平仓扣费后不足，只在本轮剩余有效时长和原风险限制内继续；不承诺一定补足。

### 未决事项与停止条件

现行单持仓、禁止自动扩大风险及人工发布规则与新需求的冲突仍未解决，继续按现行规则执行。不得靠计划文字开放权限或放宽限制；后续须通过规定的审阅与变更流程解决。数值阈值、离线估值、平仓重试、并发优先级、同币种持仓归属及具体技术选型仍需设计，详见下文“当前模块边界与未决事项”。

本次不运行测试策略、不访问交易所或读取真实凭据与账户导出，不设置服务、自启、通知或自动继续；不启动子代理。仅推送 origin/codex/workbench-init，不更新 main、不创建或合并 PR、不打标签、不部署、不强推。若远程历史不一致、出现无法保留的并发改动或推送拒绝，停止修改范围扩展并如实报告，不覆盖远程。

### 本次基线与核验记录

2026-09-14 使用 Get-Location、git status --short --branch --untracked-files=all、git rev-parse --verify HEAD、git remote -v 及 git ls-remote --symref origin HEAD refs/heads/main refs/heads/codex/workbench-init 核对：唯一目录与本文件一致，分支 codex/workbench-init，工作区干净，本地及远程工作分支均为 60abb777ff079a301b70685cbdfd4ecffc96df3c；远程默认分支 main 为 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28。前次中断仅已读取技能，没有留下文件改动。

父级及项目规则、共享及项目 WORKFLOW、状态、索引、指针、当前任务和 migration 已回读；migration 仅有 README，无未吸收的交接文件。记忆关键词查询无匹配退出 1，仅表示没有相关记忆，不是业务检查结论。本次不依赖聊天作为唯一计划存储。

写入工具失败记录：首次通过管道调用 apply_patch 被拒绝为“requires a UTF-8 PATCH argument”；随后经 PowerShell 向批处理传递多行参数被拒绝为“last line ... must be '*** End Patch'”。git status 再核对仍干净。停止重复尝试后读取 apply_patch.bat，确认它调用 codex.exe --codex-run-as-apply-patch；改由 Node execFileSync 直接向相同补丁入口传递完整参数，避免批处理的多行参数传递问题。不是绕过审批或使用其他文件写入方式。

写后自查：已回读 git diff 中两文件的实际改动并与本线程已讨论计划对照。Node 内置 fs/path/assert/child_process 检查两文件 UTF-8、末尾换行、行尾空白、冲突标记及围栏均 PASS；12 个本地链接均存在，15 个表格的列数一致。当前状态、责任与任务索引一致；从“2026-09-13 当前契约”起的原始字节完整保留。git diff --check 退出 0；git diff --name-only 60abb777ff079a301b70685cbdfd4ecffc96df3c 恰为本文件与 CURRENT_TASK.md，其余跟踪文件未改。

新增差异的定向凭据模式检查 PASS，仅覆盖所检查的私钥和令牌模式，不是全账户安全审计。上述为文档源文、结构和差异自查，不冒充浏览器视觉验收、独立验收或业务测试。提交前复查暂存内容与白名单，提交后仍须核对真实远程引用；实际提交及推送结果在后续回执记录，不以本段自查替代。

未执行业务 pytest/ruff、模拟运行、Windows 运行测试、远程 CI 或独立验收；这些不在计划同步范围内。回退需另获授权后使用审阅过的反向补丁或新 revert 提交，不重写已推送历史。任务继续 ANALYZING，下一步仍由控制塔开展第 1 步讨论。

### 计划同步回执（2026-09-14）

- git diff --cached --check 退出 0；暂存白名单恰为 CURRENT_TASK.md 与本任务文件，未暂存改动为空。提交前复查文档结构、链接、状态、原契约字节保留及新增差异凭据模式，结果同上。
- git commit -m "docs: persist agreed roadmap and simulation planning steps" 成功；2 文件，72 行新增、6 行删除。git rev-parse --verify HEAD 返回 a94905146dbe9fcbc83489ecf761e72052494e6a。
- git push origin HEAD:refs/heads/codex/workbench-init 退出 0，从 60abb777 推进到上述计划提交，无强推。
- 随后 git ls-remote origin refs/heads/codex/workbench-init refs/heads/main 退出 0：工作分支为 a94905146dbe9fcbc83489ecf761e72052494e6a，与本地一致；main 保持 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28。git status --short --branch --untracked-files=all 显示跟踪一致且无未提交或未跟踪文件。

本回执在上述结果确认后补写，随后单独提交、同步，不改写已推送历史。可用 git log -1 --format=%H -- docs/codex/tasks/20260912-002-system-requirements.md 定位含本回执的最后提交，再对照 git rev-parse HEAD 与 git ls-remote 核对最终分支；不预填本回执自身未来的 SHA。计划文件已远程持久化，不等于需求定稿、主分支合并、业务实施或独立验收，当前任务与责任保持不变。

## 当前契约（2026-09-14 更新）

R17-R42 于 2026-09-13 形成；2026-09-14 按 E12 更新 R24，新增 R43-R48 并同步关联边界。未被明确替代的已确认需求继续有效。下列旧授权说明和本节末尾的历史自查、Git 回执只记录 2026-09-13 当次操作，不扩大上方本次两文件本地整理权限。

### 2026-09-13 目标、授权与证据等级（历史说明）

主要目标仍是形成可实施、可复核的个人自动化系统需求与模块边界。本次可观察交付仅为：完整整理续聊确认事项，标明历史替代与未决项，并在指定 GitHub 工作分支保存文档；不宣称需求任务完成。

用户在当前线程先说“你的建议我采纳了”，随后对整理入库的询问回复“允许，顺便也更新一下远程的git”。据此，本次控制塔可修改本任务文件、CURRENT_TASK.md、PROJECT_STATE.md，并提交、推送此前已审阅的工作台文档及本次更新。TASKS.md 状态无需改变，原样保留。此次不改稳定工作流规则，CHANGELOG.md 只随原有初始化记录入库，不新增规则修订。

需求确认、用户描述的使用条件、仓库实测、公开作者自述和控制塔建议分开记录。以下 R17-R42 为经连续问答确认的产品需求，不是运行事实。涉及现行安全或发布规则的冲突仍是实施阻塞，不以本文件覆盖原规则。

R01-R13 未被本节替代或收紧的部分继续作为需求保留：个人使用、第一阶段仅模拟、验证后自动模拟切换、亏损停机须明确重授权、Windows 通知，以及全部可交易 USDT 本位永续合约进入研究候选池。新上架同类合约须先满足数据和验证要求才可参与模拟，不要求所有合约同时持仓；固定持仓数未定，不等于无限风险。R02/R03 的适用边界见替代对照表。ROI 相关 R14-R16 仅保留历史效力。

### 盈利目标与风险

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R17 | 启动时由用户填写模拟本金、预期净盈利金额、期望达成时长、最大允许净亏损金额 | 四项均为用户输入，不硬编码金额或固定 2 小时；预期盈利不是收益保证。E01 |
| R18 | 盈利目标按本轮整体扣除手续费、资金费等交易成本后的净盈利计算 | 不按单笔或某个策略单独达成目标；本轮资金流按 R44，金额单位及精度待定。E02、E05、E14 |
| R19 | 以“现在全部平仓后本轮预计净盈利”触发目标平仓，包含此前已实现盈亏、当前持仓盈亏及预计平仓成本 | 先禁止新开仓、平掉全部模拟持仓并核对；最终达标以实际结算为准。E02 |
| R20 | 实际净盈利超过目标则保留超额；实际不足可在本轮剩余时长和原风险授权内按原计划继续开仓尝试补足 | 不是无限追补、不保证补足，不绕过到期、亏损停机或 R48 主动结束；达标全平后暂停。E02、E18 |
| R21 | 有效时长用尽仍未达标时，禁止新开仓、平掉全部模拟持仓、核对结果、暂停并发 Windows 提醒 | 未经用户明确授权不得恢复；“二次平仓”经澄清只是平仓并核对，不是另设特殊交易流程。E02 |
| R22 | 运行中用户可动态修改盈利目标和期望时长；系统不能自行降低目标或延长期限，只能在授权内调整策略 | 修改不清零本轮盈亏；修改时点、输入校验和与平仓并发时的优先级待定。E03 |
| R23 | 亏损上限按“本轮启动本金减去现在全部平仓后预计剩余资金”判断，计入浮动盈亏与预计平仓成本 | 触及上限立即禁止开仓、全平并锁定暂停，用户明确重新授权才能恢复；不重置记录绕过。E03 |
| R24 | 第一版中，本轮亏损金额硬上限在启动前确定，运行中用户和系统均不能修改 | 替代“只允许调低”；仍可按已批准规则提前减仓、平仓或停机，不要求等亏够才保护。R03 不适用于修改本轮硬上限。E12 |

### 计时、多策略与停机

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R25 | 本轮第一笔模拟开仓成交开始计时；之前的研究、验证和等待机会不计时；开始后普通空仓等待计入 | 不是点击开始就计时，也不是每次平仓都暂停。E04 |
| R26 | 电脑关机期间不运行且不计时；断网导致行情不可靠时暂停计时、禁止新开仓；恢复后先核对状态再续算剩余时长 | 盈亏与停机锁保留，最新结果核算按 R47；补数、估值实现、休眠和故障边界待定，时间暂停不是风险消失。E04、E08、E17 |
| R27 | 所有策略退场、账户已空仓且没有合格策略时，进入等待合格策略：暂停计时、继续研究 | 新合格策略出现后，状态核对通过且未触发硬停机，可自动恢复同一轮交易和计时。普通空仓不自动适用此例外。E07 |
| R28 | 达标、到期、亏损停机或按 R48 主动结束后，研究和验证可继续，但新候选、重启或 Codex 恢复均不能解锁交易或自动启动下一轮 | 人工明确授权边界保留；结束本轮不是退出整个程序，不创建实际后台研究任务。E06、E08、E09、E18 |
| R29 | 同一轮允许多套合格策略并行，共用本轮本金、目标、总盈亏和总亏损上限；系统在总预算和风险内动态分配资金 | 不给每个策略复制一份完整本金或亏损额度；同一合约持仓归属按 R49，仓位数与相关性约束仍待定。E05、E19 |
| R30 | 策略产生信号；执行统一处理订单和持仓核对；独立风控对新开仓有最终否决权，可要求全平并锁定 | 策略不能改硬限制或绕过风控直接下单。“独立”是权限分离，不要求多套部署或人工逐笔批准。E05 |
| R49 | 同一合约已有某策略持仓时，其他策略必须等待该仓完全结束后，才能竞争下一笔开仓 | 同一合约同一时间只归属一个策略；其他合约是否可并行仍受 R29、现行单持仓和统一风控约束。E19 |

### 研究、验证与退场

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R31 | 生成策略的模块不能修改准入标准或验证结果，只有独立验证通过的具体版本才获得模拟交易资格 | 验证自动执行，不由生成方自行宣布合格；模拟资格不是实盘资格。E06 |
| R32 | 不直接改写正在运行的策略版本；代码或参数变化形成新候选并重新验证 | 沿用不可变版本边界；正常替换按 R02 交接，失格和紧急风险按 R35/R23 处理。E07 |
| R33 | 持续循环：合格策略运行取数，积累当期行情、信号、模拟成交、成本与盈亏；复盘反馈给 Codex，生成新候选，独立验证后合格入场 | 反馈数据若用于开发或调整候选 B，就不能再作为 B 的独立验证；使用未参与开发的预留数据或后续新行情。E07 |
| R34 | 新候选先跟随新行情做隔离模拟试跑，单独验证和记账；不占本轮模拟本金，不计入本轮盈亏与目标 | 通过后才可正式参与本轮模拟交易；隔离试跑不等于真实交易试错。E09 |
| R35 | 运行策略按预定标准被判不合格时，自动停止它的新开仓并平掉其模拟持仓；其他合格策略在账户总风控允许下继续 | 失格标准与持仓归属须可执行、可验证；不把任意一次亏损自动等同策略失效。E07 |
| R36 | 每次验证保存策略版本、数据范围、所用标准、结果及失败原因，保留全部候选与失败记录 | 不能只保存赢家；版本、反馈和重复试验可追溯，不能以换策略重置本轮记录。E06、E10 |
| R37 | 候选准入检查正确性、独立收益证据、完整成本、风险和运行一致性 | 覆盖未来信息使用、指标初始化偏差、开发与验证数据隔离、模拟成交假设、信号/订单/持仓/账本核对；短期盈利本身不足以准入。E10 |
| R38 | 验证输出三种结果：合格、不合格、证据不足 | 合格仅入池，启用还受总风控和运行状态约束；不合格拒绝并说明原因；证据不足继续采集，不冒充合格或直接判无效。E10 |
| R39 | 评估前确定标准；不能因没有策略通过就自动放宽；由控制塔提出有依据的指标并用项目数据校准，再确认数值 | 尚未约定统一胜率、Sharpe、交易笔数或观察天数，也未选择统计实现或工具。用户盈利目标不能替代验证证据。E10 |

### Codex 与 Windows 运行条件

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R40 | 研究工具选现有 Codex，用于生成和改进候选；已验证策略、执行和风控应脱离 Codex 独立运行 | 仅 Codex 不可用时，只暂停依赖它的研究，不据此中断正常模拟交易或撤销风控。接入方式尚未实施。E09 |
| R41 | 按用户描述：当前是 Codex 桌面版配第三方中转 API、订阅额度制；额度用完停止，恢复时间不固定。额度不足时保留进度、暂停依赖 Codex 的研究并通知；确认恢复可用后自动续研 | 这是用户描述，未读取配置、账单或凭据核验。不假定官方按量计费或固定重置时间；没有购买额度或提高套餐的授权。自动续研不解锁交易。E09 |
| R42 | Windows 原生通知、关闭窗口后后台运行、开机恢复核对及保留停机锁继续沿用 R07-R10；第一阶段不要求电脑关机后继续运行 | 不引入常驻云服务；主窗口关闭不等于完全退出。自启、通知、后台进程均尚未设置或验证。E08 |

### 2026-09-14 补充的运行边界

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R43 | 第一版同一时间只运行一轮正式模拟交易，多套合格策略共用这一轮资金、目标和风险预算 | 候选隔离试跑不算另一轮正式交易；不能另开一轮绕过停机锁。E13 |
| R44 | 第一版一轮开始后不允许追加或取出模拟资金，本轮起始本金固定 | 余额可随盈亏和费用变化；策略间可在授权内重新分配，不能补钱改写盈利或亏损。E14 |
| R45 | 已开始计时后，行情可靠且没有其他已确认暂停原因时，整轮平仓与核对继续消耗有效时长 | 时间用尽后不能因盈利不足重开仓；即使剩余时长为零，仍须完成平仓与核对。E15 |
| R46 | 已启动但未完成的平仓，重启后先禁止新开仓，核对已成交和剩余持仓，再自动继续，无需再次批准 | 原盈亏和停机锁保留；核对不清保持异常并提醒，不盲目重复下单或声称已平仓。E16 |
| R47 | 离线恢复后先核对状态，按最新可靠结果计算本轮净盈亏，不因离线期间曾越线而追认停机 | 历史盈亏与费用继续累计；中断前已触发并记录的锁不能因结果好转解除；继续还须满足时间、资格和其他风控，未知状态禁止开仓，不虚构离线成交。E17 |
| R48 | 用户发出一次“结束本轮”指令后，禁止新开仓，处理剩余平仓、核对实际盈亏与费用，然后保持暂停 | 盈利不足也不追补；未平完或核对失败显示结束处理中或异常并提醒，不能宣布成功；重启按 R46，恢复交易须明确重新授权，关窗不等于结束本轮。E18 |

### 被替代或收紧的旧约定

| 旧约定或问题 | 当前处理 |
| --- | --- |
| R14-R16：ROI 核心目标、固定 2 小时、用户填 ROI | 已被 R17-R22、R25-R27 替代。ROI 是否作为辅助展示尚未定，不继续按旧问题追问 |
| “可以调高调低亏损上限”及其二次确认方案 | 用户已撤回；R24 为当前约定 |
| 原 R24：“本轮运行中亏损金额硬上限只允许调低，用户和系统都不能调高” | 已被 E12 的运行中固定硬上限替代；不再设计中途调低及其立即超限流程，提前降低实际风险仍保留 |
| 预计达标平仓后差一点也保持暂停 | 此建议未获采纳；按 R20，在剩余时长和风险内可继续尝试补足 |
| 截止时间一律按墙钟流逝，关机/断网也算 | 用户否定；当前是带明确暂停情形的有效计时，离线恢复细则仍待设计 |
| 每次空仓暂停计时 | 用户否定；普通空仓计时，只有 R26-R27 等已确认例外暂停 |
| R03：降低后的风险额度可恢复 | 不能解释为修改本轮固定亏损硬上限；其他风险额度的类型和恢复条件仍待确认，并受现行 AGENTS 第 4 条约束 |
| R02：正常替换先等待旧仓退出 | 正常版本替换继续适用，不再暗示全局只运行一个策略；失格策略平仓退场按 R35，整体紧急风控按 R23 |
| 旧方案将 AI 策略生成延后 | 当前需求选择第一阶段接入现有 Codex；技术可行性和现行规则兼容性仍待验证 |

### 2026-09-13 来源索引（E01-E10）

当前来源线程：`01a09416-4080-7b41-a5bc-a56dae7cf247`，应用标题 `00｜项目控制塔_量化交易`，逻辑责任角色为 00｜项目控制塔。本次通过 read_thread 回读用户消息与最终答复，不把内部推理、历史自测或未完成输出当作用户批准。以下 ID 是对话定位标识，不是交易账户或订单标识。

| 来源 | 可定位的问答与确认 |
| --- | --- |
| E01 | 目标回退及四项输入：01a097c7-1c04-7e61-9bdb-9207dbc66aa3 至 01a097ca-4caa-7e01-8303-84c668bf8505；用户提出预期盈利金额，随后连续确认时长、本金和亏损上限 |
| E02 | 目标、到期、平仓、净收益及差额：01a097ca-fa5f-7a41-ba70-0ea4a22b0eea 至 01a097d7-e338-7723-a0bb-97debbfdb2f2；明确“多了就算了”“少了按照原来的计划继续开仓”，并确认到期仍停 |
| E03 | 亏损基准和动态权限：01a097d8-c617-7d63-9388-3bcfdb1097de 至 01a097f6-d4d1-70a3-a674-6866922bdb58；含撤回调高亏损上限的批注及后续“对” |
| E04 | 首次开仓计时：01a09801-0b24-7f21-af6b-b784485c68f0 至 01a09802-cfe4-7332-86c0-c1ab92378cde；用户两次“不算”，随后“不用，算在一起”确认普通空仓计时 |
| E05 | 多策略、资金与独立风控：01a09803-a9a4-7292-910c-9121addfc3ae 至 01a09815-8e1c-7403-aa03-12ec4be19e2e；用户允许并行和动态分配，接受职责分离并要求继续 |
| E06 | 独立验证批注及研究留档：01a09837-74c4-7911-a397-9518a5e2762d、01a09839-1162-70b1-a2ad-7eb62fa38e3b；达标/到期继续研究确认 01a09839-e6e5-7bc1-b1b8-319ee43e3d01 |
| E07 | 持续迭代至等待恢复：01a0984e-4c6c-7e10-bf04-4a55e00342c3 至 01a0987c-49cb-72d1-a9cc-9088562c108e；包含用户对完整五步闭环、失格退场、空仓无策略暂停与自动恢复的确认 |
| E08 | 关机与断网：01a0983c-fa46-7801-9389-e17234bd6ec4 至 01a09842-79f0-7eb1-9c10-55b571f1c73f；关机不用继续运行、不计时，断网行情不可靠暂停并恢复核对 |
| E09 | Codex 分工确认 01a09845-01bd-71e1-be91-7a895db99e7e、01a0984a-fd6a-7782-bedb-2544823965e5；计费、恢复与隔离试跑 01a09880-04a6-7282-92b5-6e7ddcf820db 至 01a09898-695b-7ae2-8c2f-3a68d02c7f62 |
| E10 | 公开实践调研 01a0989e-3c93-7d92-ba55-79a77022442c；用户在 01a098dc-3d5c-7dc0-a9ad-a3abe69f24c1 说“你的建议我采纳了”；本次 01a098dd-b33a-7032-9346-733f3072e45a 授权整理并更新远程 Git |

公开资料仅作为本线程建议的来源，不是本项目实测或盈利证明。此前汇报的六个账号集中于两个社区讨论及一个 GitHub 问题，部分含项目推广；未独立审计收益，不能称为行业共识。本次整理回读了研究答复，未重新抓取网页或运行第三方工具。保留原始入口，后续确定具体技术指标前须重查原文与适用版本：

- 案例：<https://www.reddit.com/r/algotrading/comments/1r1x7mp/my_journey_into_automated_crypto_trading/>、<https://www.reddit.com/r/algotrading/comments/1tv8m1z/its_finally_working/>、<https://github.com/freqtrade/freqtrade/issues/2472>。仅作者自述，不用自报盈利推导阈值。
- 方法参考：<https://www.freqtrade.io/en/stable/lookahead-analysis/>、<https://www.freqtrade.io/en/stable/recursive-analysis/>、<https://www.freqtrade.io/en/stable/backtesting/>、<https://www.quantconnect.com/docs/v2/writing-algorithms/live-trading/reconciliation>、<https://www.davidhbailey.com/dhbpapers/deflated-sharpe.pdf>。保留验证正确性、成本、执行差异和重复筛选风险的依据，不等于选定这些框架或统计指标。

### 当前模块边界与未决事项

已确认的是逻辑职责：研究生成候选、独立验证准入、执行管理订单、独立风控否决、统一核算审计、界面接收用户授权及 Windows 通知。任务目标与计时需要统一控制，但具体模块命名、接口、进程和存储拆分仍是待定设计，不引入额外服务或平台。

实施前必须处理的冲突：

1. AGENTS 第 4 条与安全模型仍禁止自动扩大风险、杠杆及范围，并要求最多一个仓位；多策略资金分配、全部合约候选池及 R03 的非亏损额度恢复必须明确研究/执行权限和边界。需求记录不解除这些限制。
2. 当前发布政策要求人工批准、冻结版本与发布身份；自动模拟准入必须形成受审阅的模拟专属边界，不能跳过现行发布门槛。实盘仍需单独里程碑和人工批准。
3. 本轮亏损硬上限固定、运行中不追加或取出模拟资金已确认；仍需明确金额单位与精度、新一轮授权如何继承历史亏损、目标与时长修改的原子生效规则。整轮平仓核对期间禁止修改目标和时长、且不排队生效的建议尚未确认，不按已通过规格实施。
4. 具体准入/持续复评/失格阈值、样本充分性、成本情景、数据版本与访问隔离、重复试验的统计校准尚未确定；不得把用户接受原则写成接受具体数字。
5. R46-R47 已确认自动续平仓和恢复后最新核算原则，R49 已确认同一合约单一策略持仓；补数、估值、订单去重、重试节奏、同时触发优先级、交易对下架/暂停仍需设计并验证。不能把发出命令当作已平仓，既有锁不可自动解除。
6. UI 形式、主动完全退出程序、历史数据供应与覆盖、数据保存期限、本机资源限制、Codex 可用性探测与重试节奏仍未选定或核验。R48 只定义结束本轮交易，不代表完全退出程序的交互已定。

控制塔推断：按当前确认的暂停规则，“期望达成期限”应在设计中清楚展示为有效运行时长及暂停原因，不能让用户误以为固定墙钟截止时刻；具体命名和交互待确认。当前证据足够保存需求阶段记录，不足以进入 READY、业务实施或独立验收。

控制塔建议：首期复用中转服务已有的额度限制，不另建费用管理模块；独立的本机计算资源限制和 API 可用性探测仍待设计。此项是最小实现建议，不是对供应商计费行为的独立核验。

### 2026-09-13 范围、验收方式与 Git 留痕（历史）

- 仅修改本任务、CURRENT_TASK.md、PROJECT_STATE.md。保留其他八个工作台文件原始字节；先前 11 项文档变化已审阅并原样提交为 78165c8112779701cd3ace8c9cca88c9576a315d。没有新增稳定规则，因此不新增 CHANGELOG 条目。
- 允许将这两个阶段的分析文档提交并推送至 origin/codex/workbench-init；不覆盖远程已有工作，不强推，不直接更新 main，不创建或合并 PR，不打标签、不发布。若远程拒绝或出现不同历史，停止推送并记录阻塞。
- 完成条件：源文可读，历史/当前约定明确分开；R17-R42 可对应确认来源；当前状态、责任及入口一致；三个文件外无本次新增改动；原安全条款和旧验收证据不变；本地提交与远程工作分支 SHA 一致。
- 核验方式：逐条回读问答和文档，严格 UTF-8/空白/冲突标记/围栏/本地链接检查，八个保留对象逐字节对比，检查暂存路径、敏感模式和 git diff --check；提交后用 git rev-parse、git ls-remote 及 git status 验证。结果在实际执行后补入。
- 任务仍为 ANALYZING。文档自查不冒充独立验收；业务 pytest/ruff、端到端模拟、通知/自启、交易所接口、部署和实盘均不在本次执行范围。分支推送也不代表远程 CI 通过。
- 回退仅在另获授权后，以审阅过的反向补丁或新 revert 提交恢复文档；不重写已推送历史，不自动删除用户资料。

本次已执行的基线检查：Get-Location 为唯一根目录；git status --short --branch --untracked-files=all 为原有 11 项文档改动，暂存区初始为空；git remote -v 指向约定仓库；git ls-remote --symref origin HEAD refs/heads/main refs/heads/codex/workbench-init 退出 0，HEAD/main 为 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28，目标工作分支不存在。未执行 fetch/pull，未混入其他分支代码。

写前自查：11 文件 UTF-8、换行、空白、围栏、冲突标记检查 PASS，本地链接 41 处均可解析；暂存 11 文件白名单、原 AGENTS 完整前缀和定向敏感模式检查 PASS，git diff --cached --check 退出 0。随后 git commit 成功，git rev-parse --verify HEAD 返回上述文档快照完整 SHA。

本次失败或限制：read_thread 首次请求 turnLimit=30 被工具拒绝（最大为 10），未写入任何状态；改为每页至多 10 后成功回读。记忆关键词检索无匹配退出 1，不作为项目检查失败或通过证据。没有读取真实凭据、账户导出或旧项目目录。

### 2026-09-13 文档自查结果（历史）

2026-09-13 写后回读确认：新增 R17-R42 共 26 条，与 E01-E10 问答对照；R14-R16 及被撤回的建议有明确替代说明；从旧文“可观察目标”开始的历史正文逐字保留。当前契约与 CURRENT_TASK 的行首状态均唯一为 ANALYZING、责任均唯一为 00｜项目控制塔，TASKS 的当前任务 ANALYZING 与旧任务 CLOSED 未变。

Node 内置 fs/path/assert 与严格 UTF-8 解码检查 11 文件，UTF-8、末尾换行、空白、冲突标记、围栏及 42 个本地链接全部通过；比较写前内存字节快照，恰有三个授权文件变化，另八个文件原样保留。git diff --check 退出 0。源文和表格已回读，不冒充浏览器视觉验收或独立验收。

保留对象可按文档快照复核：AGENTS.md、CHANGELOG.md、docs/codex/WORKFLOW.md、docs/codex/TASKS.md、docs/codex/tasks/20260912-001-workbench-init.md，以及 decisions、migration、archive 的 README.md。八文件相对 78165c8112779701cd3ace8c9cca88c9576a315d 不应有差异。本次没有新增规则、代码、依赖、CI、配置或运行数据。

提交与推送前后须执行下列 Git 检查；远程操作尚未完成的部分不得提前视为通过，实际结果见后续推送回执：

```powershell
git diff --cached --check
git diff --cached --name-only
git diff --name-only 78165c8112779701cd3ace8c9cca88c9576a315d
git rev-parse --verify HEAD
git ls-remote origin refs/heads/codex/workbench-init refs/heads/main
git status --short --branch --untracked-files=all
```

### Git 推送回执（2026-09-13）

需求主体已提交并推送至 origin/codex/workbench-init。本节是在推送成功并独立读取远程引用后补记的实际证据，不是预期结果；本节自身随后作为文档回执提交同步，不重写前述提交。

| 命令或检查 | 实际结果 |
| --- | --- |
| git diff --cached --check；git diff --cached --name-only（需求提交前） | 空白检查退出 0；暂存范围恰为 CURRENT_TASK.md、PROJECT_STATE.md、本任务文件 |
| 定向暂存差异敏感模式检查；git diff --quiet 对比产品基线和八个保留对象 | PASS；未发现所检查的凭据模式；产品文件未变、八个保留文件未变；不是全账户或全磁盘安全审计 |
| git commit -m "docs: reconcile confirmed simulation requirements and open decisions" | 成功；3 文件，165 行新增、16 行删除，包含历史标识、当前契约、来源及检查记录 |
| git rev-parse --verify HEAD（需求主体提交后） | 98233df9c18f9f5625b3ecaea82566accad4a6cd |
| git diff --check a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28 HEAD | 退出 0 |
| git push --set-upstream origin HEAD:refs/heads/codex/workbench-init | 退出 0，新建远程工作分支并建立上游跟踪；未强推 |
| git ls-remote origin refs/heads/codex/workbench-init refs/heads/main（推送后） | 工作分支为 98233df9c18f9f5625b3ecaea82566accad4a6cd，与本地需求提交一致；main 仍为 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28 |
| git status --short --branch --untracked-files=all（补写本回执前） | 仅 codex/workbench-init...origin/codex/workbench-init，无未提交或未跟踪文件 |

回执核对方法：`git log -1 --format=%H -- docs/codex/tasks/20260912-002-system-requirements.md` 可定位包含本节的最后提交，再用 `git rev-parse HEAD` 和上述 `git ls-remote` 命令核对最终本地与远程工作分支；本节不填写自身未来的提交号。提交树中的文档及上述需求主体 SHA 是交付证据，不要求依赖对话恢复内容。

未创建或合并 PR；main 未由本次操作更新。没有标签、部署、交易、后台服务、子代理或独立验收。未运行本地业务 pytest/ruff、浏览器渲染或远程 CI/保护核验，均不冒充通过。需求数值、规则冲突和异常细节尚未定稿，当前任务继续 ANALYZING，下一步仍是控制塔与用户细化指标。

## 2026-09-12 历史记录起点

以下保留上次整理的原始内容、确认编号 R01-R16、未决项、授权边界及检查结果，仅代表 2026-09-12 的快照。其中“本轮”“当前”“未提交”“禁止推送”和 ROI 续聊问题均属当日语境，不覆盖上方当前契约、七阶段路线及本次仅本地文档整理的授权。旧条目不删除；以替代对照表理解其效力。

## 可观察目标

形成一份可复核、经用户确认的个人自动化量化系统需求与模块边界基线，明确自主权限、风险约束、指标口径、异常行为及第一阶段边界，让后续设计不依赖聊天记忆。

本次只完成讨论资料落库和续聊入口。整个需求梳理尚未完成，不是最终设计、可实施规格或业务验收结论。

## 来源与证据等级

1. 当前讨论：用户要求先讨论、不写代码、每次一个关键问题；最新授权为“今天的讨论先到这里，把资料整理一下明天再继续”。这只授权本轮文档整理。
2. 用户指定的旧线程：`codex://threads/01a084fd-5247-7210-a498-43d9ae564af0`，实际标题 `Clarify task request`。本线程此前已回读其需求、评审及阶段总结；历史结论只按历史记录保存，不等于本轮重新验收。
3. 当前仓库：已读 [AGENTS.md](../../../AGENTS.md)、[工作流](../WORKFLOW.md)、[项目状态](../PROJECT_STATE.md)、[架构](../../architecture.md)、[安全模型](../../safety-model.md)、[发布政策](../../governance/RELEASE_POLICY.md)。这些是现行文件声明，不证明运行可用。

下文区分用户确认的需求、旧线程记录、当前仓库事实、待确认建议。需求确认不代表实现完成或现行规则已经改变。

## 项目的来龙去脉：历史记录

| 日期 | 旧线程记录的推进 | 不能据此推出的结论 |
| --- | --- | --- |
| 2026-09-09 | 从部署 Freqtrade 和合约自动交易起步，尝试下单与界面记录 | 会下单不等于交易、核算、界面和安全约束已形成完整闭环 |
| 2026-09-10 | 研究过去一年的交易行为、盈利与亏损画像，结合行情及交易成本提出候选；记录过测试失败的候选 | 历史表现或少量结果不能代替当前流程的完整策略证据 |
| 2026-09-11 | 用户提出先打通受控实验闭环、持续研究策略；评审推动研究与执行隔离，需求文档演进至 V0.4，并讨论规则表修改基线 | 旧 V0.4 曾延后 AI 策略生成，不是本轮已确认的第一阶段范围 |
| 2026-09-12 | Git 治理、测试、证据、批准和冻结版本进入主线；旧线程最后记录 PR #5 已合并、PR #7 等待批准，尚未启动长期 Dry-run | PR、CI 和旧线程声明不等于本轮远程状态、部署状态或实盘授权 |
| 当前讨论 | 明确个人使用、自主研究与模拟运行，以及权限、停机、通知、品种范围和 ROI 目标配置 | 讨论不是业务开发授权，未决项不能自动补成已确认规则 |

旧线程提到的资料包括 `D:/量化交易/客户需求说明.md`（V0.4）、`D:/量化交易/量化实验规则表_V0.1.xlsx`、V0.2 修改基线及早期研究记录。本轮没有打开旧目录、复制这些文件、读取账户导出或核验原始研究数据；V0.2 不能仅因讨论过就记成已生成文件。

修正此前不完整的项目认识：项目历史早于当前 Git 仓库，已有早期需求和研究；“当前仓库未收录”不等于“此前从未做过”。失败候选归档、完整费用核算和实验可追溯是旧讨论的重要方向，具体归档与核算规格仍待本任务确认。

`migration/` 当前只有说明文件，没有旧线程生成的正式交接记录。本文件是控制塔对指定历史的整理，不冒充旧线程交接，也不宣称历史工件已全部迁移。

## 需求背景与此前对齐方向

- 短期用户是本人，核心痛点是情绪化扛单和亏损后扩大风险；目标是减少人工参与，而非仅增加提醒。
- 产品希望覆盖研究、提出假设、生成候选策略、验证、筛选、执行和复盘，而不只是自动下单。
- 此前上下文已对齐第一阶段先模拟、暂不动用真实资金；新策略或新版本进入实盘仍需单独批准。
- 系统可以在授权范围内自主调整，但不能自行突破用户授权的硬风险上限。
- 没有候选达标时告知用户，由用户决定继续研究还是改变筛选标准；系统不得自行降低标准。改变筛选标准不等于放宽风险上限，变更后须重新验证。
- “没有合格策略”和“证据不足”可以是研究结论；用户希望用 ROI 衡量收益目标，不构成收益承诺，也不自动替代技术正确性与风险约束。

以上承接本线程此前的需求回顾。以下表格单列本轮连续问答中已经明确的具体偏好，避免把旧方案的限制自动继承为新需求。

## 本轮用户确认的需求契约

| 编号 | 已确认需求 | 用户确认依据及边界 |
| --- | --- | --- |
| R01 | 模拟环境中，候选策略通过预先确认的独立验证后，可以自动切换，不逐次请求批准 | 对自动切换问题回答“希望”；不包含实盘授权，独立验证的具体标准和权限设计尚未定稿 |
| R02 | 正常换策略时，旧策略停止开新仓，按原规则管理已有模拟仓位，全部退出后再启用新版本 | 回答“是”；不等于紧急风险事件也必须等待正常退出 |
| R03 | 仅在模拟阶段，主动降低的风险额度可以在满足预先约定的恢复条件后自动恢复到原授权水平 | 对解释后的问题回答“允许”；不允许超过原上限或自行放宽恢复条件。解释中的 100/30/200 只是示例，不是需求数值 |
| R04 | 模拟账户触发最大累计亏损上限后停止开新仓，并立即平掉全部模拟持仓，随后保持停机 | 先回答“停止”，再确认立即平仓；具体数值、核算口径、执行失败行为待定 |
| R05 | 这类停机必须由用户明确重新授权才能恢复 | 回答“是”；不能清零亏损记录、重启或换策略来绕过停机，不得套用 R03 的普通风险恢复 |
| R06 | 交易停机期间，研究可以继续生成、测试和筛选候选 | 回答“是”；找到合格新策略也不能自动解除交易锁 |
| R07 | 使用 Windows 原生系统通知，提醒停机、无合格策略和待授权事项 | 用户指定“电脑 Windows 自带的提醒功能”；当前不扩展邮件、微信等渠道 |
| R08 | 通知只负责提醒；用户进入系统查看原因和证据，再明确确认恢复或标准变更 | 回答“是”；收到、关闭或点击通知本身都不是批准 |
| R09 | 关闭主窗口后，后台研究、模拟交易、风控和通知继续运行 | 回答“是”；关闭界面不等于退出后台，主动完全退出的交互尚未讨论 |
| R10 | Windows 开机后自动启动，先恢复记录并核对状态；停机锁跨重启保留，核对失败不恢复模拟交易 | 回答“是”；这不授权本次设置系统自启，休眠和掉电后的细节待定 |
| R11 | 第一阶段候选池覆盖币安全部可交易的 USDT 本位永续合约，由系统筛选 | 先说“上架的全部”，再明确同意 USDT 本位永续范围；不扩大为所有合约类型，也不要求所有品种同时持仓 |
| R12 | 新上架的同类合约自动进入研究候选池，满足数据和验证标准后才参与模拟交易 | 回答“是的”；数据不足或验证未通过不得交易，下架及暂停交易的处理尚未确认 |
| R13 | 用户不预先指定固定的同时持仓数量，希望系统根据目标自主决定 | 用户原话“这个没规定啊，能够达成目标就行”；不等于无限风险，算法与账户总风险约束尚未形成可实施规格 |
| R14 | ROI 是核心收益指标 | 用户指定“roi指标”；尚未确认账户/单笔口径、分母、浮盈浮亏、费用和资金流处理 |
| R15 | ROI 考核周期目前取 2 小时 | 用户回答“2小时吧”；固定窗口还是滚动窗口、能否配置未确认，周期结束的交易动作未确认 |
| R16 | 目标 ROI 是用户在系统中自行填写的变量，不写死收益数字 | 用户两次明确“这个是一个变量，用户自己填写”；数值范围、校验、修改何时生效及历史目标保留尚未确认 |

约束说明：2 小时 ROI 考核不能自动被解释为必须交易、必须清仓、必须盈利或可以提高风险。当前也没有确认每两小时重置累计亏损、模拟资金或停机锁。

## 模块边界：待用户确认的草案

沿用仓库已有概念做逻辑分工，不承诺拆成独立服务、多个进程或新的平台。

| 模块 | 建议职责 | 建议边界 |
| --- | --- | --- |
| 研究 | 数据整理、行为画像、提出假设、生成候选与新版本 | 不直接修改执行中的版本，不拥有真实下单权限 |
| 验证 | 回测、独立数据检验、成本与异常检查，输出结论和证据 | 生成策略的一方不能自行改准入标准或绕过结果；隔离机制待确认 |
| 发布 | 绑定版本、参数、证据及获准用途，管理模拟切换与实盘批准 | 模拟资格不得自动升级为实盘资格；须解决与现有发布政策的冲突 |
| 执行 | 运行指定版本、处理订单及持仓状态、反馈结果 | 不自行改写策略；正常切换与累计亏损紧急停机走不同流程 |
| 风控 | 执行账户约束、降低及按条件恢复风险额度、停机锁定 | 普通恢复不能解锁累计亏损停机，也不能扩大授权上限 |
| 核算与审计 | 记录成交、费用、收益、版本、调整和失败原因 | 不只保存成功结果，不允许通过删记录改变停机或考核事实 |
| 用户界面与通知 | 展示原因和证据、填写 ROI 目标、接收明确授权、Windows 提醒 | 通知不批准；后台与界面生命周期区分；前端形式尚未决定 |

这是后续讨论草案，不是已完成架构审批。没有选择新的技术栈、数据库、桌面框架或 AI 服务供应商。

## 与现行规则的未决冲突

| 冲突 | 当前文件或旧方案 | 本轮需求方向 | 当前处理 |
| --- | --- | --- | --- |
| 自主恢复风险 | AGENTS 第 4 条禁止自动增加资金、杠杆、持仓数量、币种范围或亏损限额 | R03 希望模拟阶段可恢复已降低的风险额度 | 只记录需求；不能把“恢复”改名后直接绕过禁止条款 |
| 持仓与品种范围 | 安全模型规定最多一个仓位；现行规则禁止自动扩大币种范围 | R11-R13 为全部 USDT 永续、新上架自动纳入、持仓数自主决策 | 尚未修改现行固定限制；研究范围与实际下单权限须分别定义 |
| 自动模拟发布 | 当前发布政策要求匹配冻结版本、证据、批准 PR 和人工 release-approved 行为 | R01 希望验证通过后自动模拟切换 | 自动研究结果不能冒用现有发布身份，需设计模拟专属授权边界 |
| 第一阶段能力 | 旧 V0.4 曾将 AI 生成策略延后，先做人工固定策略 | 本轮希望自主研究与生成进入模拟闭环 | 保留历史依据，以用户最新方向继续收敛，不能直接照搬旧阶段范围 |
| 运行版本变化 | 架构规定执行结果不能原地修改正在运行的策略，变更须新版本全程验证 | 希望系统自主调整策略和风控 | 区分版本升级、获准参数调整与账户风险控制，避免把自主性等同于任意热修改 |
| 数据研究来源 | 项目 AGENTS 第 1 条禁止读取真实凭据或账户导出 | 旧项目曾用个人交易数据做画像 | 本轮不读取旧导出；未来须明确合法且符合项目边界的数据输入方式，不能沿用旧授权 |

本文件不是 AGENTS、安全模型或发布政策的修订。没有获得现行规则变更和业务实施授权之前，继续执行原规则；不得降低安全要求。稳定规则如需变更，应另行说明原因、影响和验证，并按工作流记录 CHANGELOG，而不是在本任务中悄悄放宽。

## 未确认问题与下一步顺序

1. 首先回答本文件开头的续聊问题：2 小时周期 ROI 未达标时如何处理。
2. ROI 的核算口径：账户或单笔、资本分母、浮动盈亏、交易成本、资金流、窗口起点及未满周期如何展示。
3. 目标 ROI 修改的生效时点、输入校验，以及无法满足目标时的告知方式；收益目标与策略准入标准不可混为一项。
4. 累计亏损上限的数值、计量区间、重授权后的预算处理；考核周期不等于亏损预算周期。
5. 总风险授权如何表达，持仓数自主决策、相关持仓、杠杆及额度恢复的边界和恢复条件。
6. 独立验证标准、评估数据隔离、策略生成方是否能修改评估规则，以及反复试验的证据保存。
7. 多策略并行需求、切换条件和切换频率；当前正常交接规则不代表已经允许并行执行。
8. 下架、行情缺失、断网、异常退出、休眠及重启后的状态核对；紧急平仓失败/部分成功的恢复与告警。
9. UI 形式、完整退出方式、模拟资金初值、研究数据来源、计算资源预算及技术复用范围。

这些是后续问题清单，不是本次同时询问用户。不要为补齐文档而猜定数值、配置、交易规则或自动回复。

## 本次修改范围与保持不变项

本轮只允许控制塔整理分析材料：本文件、[TASKS.md](../TASKS.md)、[CURRENT_TASK.md](../CURRENT_TASK.md)，以及 [PROJECT_STATE.md](../PROJECT_STATE.md) 对“初始化关闭时无活动任务”这一历史快照的澄清与当前指针引用。项目状态不新增未经验证的产品结果。

保留全部既有修改。产品代码、测试、依赖、CI、策略、风险数值、安全规则、共享工作流、发布政策、旧任务与已有验收证据、历史目录和数据保持不变。不创建真实发布清单，不读凭据或账户导出，不连接交易所，不运行或设置后台服务、自启和通知，不创建子代理，不安排自动继续或提醒，不提交、推送、合并、部署或清理资源。

## 验收标准与停止条件

本次阶段记录的检查标准：

1. 明天仅凭当前任务入口即可找到已确认需求、历史来源、冲突、未决问题和续聊位置。
2. R01-R16 能对应本轮用户确认，不把例子当数值，不把讨论当实现或收益保证。
3. 索引、当前任务及本文件仅有一个当前状态 ANALYZING 和一个责任线程；旧任务仍 CLOSED。
4. 修改只在上述四文件；保留对象未变化；文档可读、链接可解析、无冲突标记和行尾空白。

需求任务进入 READY 的条件尚未满足：需要用户确认指标口径、异常行为、风险和自主权限的可执行边界，并确认模块草案及下一阶段范围。不得因本文件生成就进入实施或标记 ACCEPTED/CLOSED。

用户已要求本日讨论停止，文档核对后停止推进；下次由用户主动继续。不存在应在后台自动消化的业务开发任务。

## 分析记录、命令与检查结果

2026-09-12 登记：此前没有活动任务；按用户已提出的需求梳理和本次整理授权，登记 DRAFT 后进入 ANALYZING。只写分析交接资料，不进入 IN_PROGRESS，不跳过 READY 进行产品实施。责任仍为当前控制塔，未创建分析、实施或验收子任务。

| 命令或检查 | 本轮实际结果 |
| --- | --- |
| Get-Location | 本文件唯一根目录 |
| git rev-parse HEAD | a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28 |
| git status --short --branch --untracked-files=all（写入前） | codex/workbench-init；AGENTS.md 已修改，CHANGELOG.md 及 8 个 docs/codex 文档未跟踪，共 10 个既有改动对象 |
| git remote -v | origin fetch/push 与本文件基线一致；未 fetch |
| 父级、项目及文档层 AGENTS 检查 | 适用 D:/CodexProjects/AGENTS.md 与项目 AGENTS；D:/AGENTS.md、projects/AGENTS.md、docs/AGENTS.md、docs/codex/AGENTS.md、tasks/AGENTS.md 均不存在 |
| 共享和项目 WORKFLOW、状态、索引、指针、旧任务、migration 回读 | 旧任务 20260912-001 CLOSED；migration 无正式交接；新任务编号 20260912-002 未占用 |
| git diff -- AGENTS.md（写入前） | 只有先前追加的工作流入口，不是本轮修改 |
| 写前保留对象快照 | 在工具内存中读取既有 10 个工作台文件并计算 SHA256，未额外创建备份文件或读取业务数据 |

### 文档自查结果（2026-09-12）

- 完整回读本文件，将已确认需求与本轮问答对照：R01-R16 共 16 项，ROI 目标没有被填入固定数值；未达标处理保留未确认；模块草案和现行规则冲突分开列示。未把历史 PR/测试报告当成本轮验收。
- 以 Node 内置 fs、assert、path、crypto 和严格 UTF-8 解码检查本轮四文件：输出 PASS，UTF-8、末尾换行、行尾空白、冲突标记和围栏检查 4/4；本地链接解析 24 处，全部存在。未依赖业务环境，未新增依赖或检查脚本文件。
- 对写前内存快照进行逐字节比较：既有文件仅 CURRENT_TASK.md、TASKS.md、PROJECT_STATE.md 改变，新建本任务文件；其余 7 个既有文件逐字节保持一致。项目状态的两处修改仅澄清初始化快照和指针，不改变验收结论。
- 索引、当前任务和任务顶部的当前状态均为 ANALYZING；两处当前责任均为 00｜项目控制塔；旧任务索引仍 CLOSED，旧任务文件未修改。
- git diff --check HEAD 退出 0；因工作台文件尚未跟踪，另做上述逐文件检查，不把普通 diff 为空当作新增文档通过。
- 最终 git status --short --branch --untracked-files=all 为原有 10 个改动对象加本任务文件，共 11 个；git diff --name-only HEAD 仍只有先前的 AGENTS.md；git rev-parse HEAD 与登记基线一致。七项字节对比证明该 AGENTS 改动不是本轮新增。

保留对象的写前 SHA256 如下，最终字节对比均一致；可在唯一根目录对表中路径执行 `Get-FileHash -Algorithm SHA256 -LiteralPath <路径>` 复核：

| 保留文件 | SHA256 |
| --- | --- |
| AGENTS.md | 298c902bdcd158323f96b370440d4c90b701c2e6d60de204895556c0dc7c03f5 |
| CHANGELOG.md | a6ec2f8b86ea4e851682c8613b231611cf30385eca8a13c1eec3b6166707f7be |
| docs/codex/WORKFLOW.md | 6c9abb70a7d2e1ff8f59dc8221db4b0c1497f3d050e15a34854b129150d2a19c |
| docs/codex/archive/README.md | 9a31d7f77739092929c9006bbb2862dfe9c1d4a64916fe997cb677d9c8a2a9a0 |
| docs/codex/decisions/README.md | 2868571e0596a6f46ea69291d0df8730fca29ee10835a3623e0c9dc345d6dda5 |
| docs/codex/migration/README.md | e3479b98e877db7c01ca34aabdb9886208d811db2e797221d16cc266d33e47b6 |
| docs/codex/tasks/20260912-001-workbench-init.md | 00f8f5b7c3aeaae568321f1fcf14beb2220aa09614367e9f0221415069fc5f4d |

本轮没有失败的文件写入或检查。文件搜索、记忆相关性搜索的无匹配退出 1 仅表示没有匹配项，不作为业务失败或业务通过证据。没有修改稳定工作流规则，因此不改 CHANGELOG。

## 实施、独立验收与限制

业务实施：未开始。本轮授权范围内的分析资料写入不构成业务实现。

独立验收：未进行；用户未授权创建子代理，本任务未进入 READY_FOR_VERIFY。用户尚未复核整份整理稿，部分需求待继续讨论，不关闭任务。

未执行：业务 pytest/ruff、运行场景、Windows 通知/自启/后台测试、交易或实时 ROI 验证、远程 PR/CI/保护检查、旧文档及研究数据原件核验。Markdown 只做源文、结构、链接检查，不宣称浏览器视觉验收或真实交易通过。

当前风险：新需求与现行安全规则未消解；ROI 和风险口径未完备；旧工件未迁移；本地文档未提交，GitHub 尚无本次交接。任何后续实施前必须重新确认边界和 READY 条件。

回退：仅在另获授权后，对本轮四文件使用经审阅的反向补丁，保留之前修改与旧任务证据；不自动删除文件或使用破坏性 Git 命令。
