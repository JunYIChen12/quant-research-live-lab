# 20260912-002 系统需求与模块边界梳理

## 身份与基线

- 创建日期：2026-09-12；最近整理日期：2026-09-16，Asia/Shanghai。
- 状态：CLOSED。
- 当前责任线程：00｜项目控制塔（已关闭）。
- 唯一根目录：D:/CodexProjects/projects/quant-research-live-lab。
- Git 分支：codex/workbench-init。
- 原产品基线（2026-09-12 启动 HEAD）：a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28。
- 2026-09-14 计划同步前 HEAD：60abb777ff079a301b70685cbdfd4ecffc96df3c；与 origin/codex/workbench-init 一致，工作区干净。
- 2026-09-14 本次总路线修订前 HEAD：eb1c62afdf4e3df10565e9841a4e3f33b5786199；分支不变，工作区干净，暂存区为空。本次仅本地文档整理，不提交或推送。
- 写入前文档快照提交：78165c8112779701cd3ace8c9cca88c9576a315d，保留 2026-09-12 工作台和初次需求记录。
- origin fetch/push：https://github.com/JunYIChen12/quant-research-live-lab.git；2026-09-13 已核对远程引用，详见本次证据。动态 HEAD 以 Git 核对，不把基线当永久当前版本。
- 环境：Windows / PowerShell；未检查、安装或修改业务运行环境。
- 讨论进度：用户已于 2026-09-14 确认用 Freqtrade 核心版框架替代此前的重控制层方案，并授权更新项目文档和同步远程 Git。第一期设计完成；具体阈值、核算、持久状态、异常和验收场景留到第二期，整个任务仍是需求分析，没有进入业务实施或独立验收。

## 下次从这里继续

先读取 [当前任务](../CURRENT_TASK.md)、本文件和现行安全规则，重新核对 Git 基线及已有修改。继续使用 Superpowers brainstorming，每次只问一个关键问题；未经确认，不启动子代理，不修改产品或运行规则。

当前有效入口为修订后的 [第一期系统框架设计](20260912-002-phase-1-framework.md)，需求细节见下文“当前契约（2026-09-15 更新）”。Freqtrade 是完整交易基础，项目只补策略生产、独立验证、冻结批准和极薄轮次安全监督；首期仍仅 Dry-run，原 ROI/固定 2 小时约定不再沿用。

2026-09-16 用户确认暂停新增微观 R 条款，先做首版收缩审查。精简后的实施入口为 [首版 MVP 收缩范围](20260912-002-mvp-scope.md)：完整 R01-R84 保留为产品事实和历史证据，但首版只实现 Freqtrade 配置、顺序验证脚本、冻结清单和单个小型监督程序；验收规则不建设独立平台，动态杠杆、多正式策略和实盘延期。此项不新增 R85，也不表示任务已 READY。

2026-09-16 用户修订首版界面边界：首版仍不支持多正式策略并发，但所有项目操作统一进入最小 Web 管理台，包括轮次参数、验证报告、正式版本批准、运行状态、停止和重新授权；订单、持仓和交易明细继续使用 FreqUI。Web 由同一个监督程序提供，不另建通用审批平台或交易界面。此前 R57、E26 和第一期框架中的“首版不开发审批界面”被本项替代；不新增 R85。

2026-09-16 用户确认首版 Web 只允许本机访问。监督程序只监听回环地址（例如 `127.0.0.1`），不开放局域网或公网，不建设用户、角色或登录系统；未来其他设备访问须建立独立安全与认证任务。本项是首版访问边界，不新增 R 条款。

2026-09-16 用户确认首版 Web 采用单页工作台：轮次参数、当前状态、策略验证报告、批准/拒绝、停止和重新授权集中在同一页，不建设菜单、多页面后台或通用管理框架；订单、持仓和交易明细仍进入 FreqUI。本项只限定首版交互形态，不新增 R 条款。

2026-09-16 用户确认首版使用一个独立本地 SQLite 保存项目状态。该库只保存本轮四项输入、准确发布身份、批准/拒绝记录、当前阶段、有效计时、停机锁、待完成动作和核对时间；不复制 Freqtrade 的订单、成交、费用、余额或完整盈亏流水。控制塔已据此在 MVP 文件收口切片 1-2 的输入输出和最小字段，READY 阻塞由四项减为三项；本项不新增 R 条款，也不表示实现或验收完成。

2026-09-16 用户确认候选隔离试跑与正式 Dry-run 采用“行为一致、隔离字段可不同”：策略、参数、交易范围、周期、杠杆、仓位、止损/退出、Protections、订单和定价等行为字段必须一致；目录、数据库、日志、端口和进程标识等纯隔离字段可以不同。行为字段变化或差异性质无法证明时，旧证据失效并须重新形成完整结论。控制塔已据此关闭第二个 READY 阻塞；本项不新增 R 条款。

2026-09-16 用户确认将交付拆为 1.0 与 1.1：1.0 只完成单个正式策略、最小 Web 管理台、轮次监督和 Windows 宿主；候选并发试跑及自动研究循环进入独立任务 `20260916-004`，待安全/发布治理完成后再做。R33-R34、R68-R71、R76、R84 的需求继续保留，但不阻塞 1.0 READY，也不据此修改现行安全规则。本项不新增 R85。

控制塔随后按既有契约为 1.0 写出九个虚构数字验收场景，覆盖证据不足、扣费后未达标继续、正常达标、到期与新信号竞争、硬亏损停机、部分平仓重启、用户主动结束、冻结版本改动和 Codex 不可用。场景是待用户复核的设计草案，未运行 Freqtrade、业务测试或浏览器，不等于验收通过或任务 READY。

2026-09-16 用户确认 1.0 收缩范围及九个验收场景。控制塔 READY 一致性检查确认产品目标、范围、保持不变项和验收方法已收口；Web 是唯一用户批准入口，但后台仍须完成并核验现行 GitHub `release-approved` 动作，本地 SQLite 记录不能单独放行，不修改发布政策。

环境检查事实：`freqtrade` 命令缺失；Python 模块不可用；`python` 指向 Microsoft Store 占位入口；`py -3.12` 报告没有合适运行时；项目无 `.venv`。GitHub CLI 2.94.0 已登录当前仓库账号并具备所需仓库权限，输出中的令牌被工具掩码且未写入仓库。因建立 Python/Freqtrade 环境属于环境与依赖变更，尚未获得本轮明确授权，任务继续 `ANALYZING`；唯一 READY 阻塞改为环境建立授权，不安装、不运行、不创建实施线程。

随后用户明确授权建立环境。控制塔使用 `winget` 安装用户级 64 位 Python `3.12.10`，在仓库创建已被 `.gitignore` 排除的 `.venv`，并仅在该环境安装项目 `.[dev]` 与 `freqtrade==2026.8`。安装后核对：Freqtrade `2026.8`、CCXT `4.5.78`；`pip check` 输出 `No broken requirements found.`；`pytest` 为 `14 passed`；`ruff check .` 为 `All checks passed!`；`freqtrade --help` 正常列出验证命令。未启动 Freqtrade、创建用户目录、下载数据、连接交易所、读取凭据或修改生产依赖声明。

READY 决定：用户已确认 1.0 范围与九个验收场景，MVP 文件已明确单一产品目标、实施切片、允许修改范围、保持不变项和验证方法，开发环境门槛已满足。任务于 2026-09-16 从 `ANALYZING` 流转为 `READY`；责任仍为控制塔，下一步仅创建第一个候选验证切片的实施线程，不同时开展 Web、Dry-run 轮次或 Windows 宿主开发。

实施接管：用户确认继续后，控制塔创建同目录线程 `01a0a983-8717-71e0-aaec-098a71a8741b`，命名为 `20｜实施｜20260912-002 1.0 候选验证入口`，任务从 `READY` 流转为 `IN_PROGRESS`。同目录仅该实施线程可以继续写入；控制塔停止写入直到交回。

验收接管：实施线程完成自测并交回 `READY_FOR_VERIFY` 后，控制塔创建同目录线程 `01a0a99d-1d9e-75e0-bb73-0ca9c6e35068`，命名为 `30｜验收｜20260912-002 1.0 候选验证入口`。验收线程默认只读产品实现，不自行修复；仅可按工作流回写任务状态和验收证据。控制塔停止写入，等待独立验收结论。

返工接管：独立验收回写 `REWORK` 后，控制塔将任务重新流转为 `IN_PROGRESS`，由原实施线程 `01a0a983-8717-71e0-aaec-098a71a8741b` 继续作为唯一写入者。返工只处理验收记录中的两项失败，不扩大到 Web、Dry-run、Windows 宿主、1.1、提交或推送。

返工复验接管：实施线程完成最小返工并交回 `READY_FOR_VERIFY` 后，控制塔创建新的同目录验收线程 `01a0a9e9-8fe3-7823-873c-e15b95fc6441`。该线程独立复核两项原失败及相关回归边界，默认只读产品实现，不自行修复；仅可回写任务状态和验收证据。

第二次返工接管：返工复验发现陈旧制品仍可被本次运行接受后，控制塔将任务重新流转为 `IN_PROGRESS`，继续由原实施线程 `01a0a983-8717-71e0-aaec-098a71a8741b` 唯一写入。鉴于同一证据校验路径已连续两次暴露缺口，本次必须从统一的“本次运行证据归属”根因修复，不再逐个添加孤立非空判断；范围仍只限候选验证入口及其测试和状态证据。

证据隔离复验接管：实施线程以每次独占运行目录完成第二次返工并交回 `READY_FOR_VERIFY` 后，控制塔创建新的同目录验收线程 `01a0aa12-d897-7be3-a62d-8a061d1fc249`。该线程独立复核旧制品跨运行不可复用、运行身份一致及此前报告/清单边界，默认只读产品实现，不自行修复；仅可回写任务状态和验收证据。

本切片单一目标：给定标准 Freqtrade `IStrategy`、最终无敏感配置快照、数据集身份和验证规则版本，按固定顺序编排锁定版 Freqtrade 原生命令，输出机器可读的三类结论报告、原始证据引用和与现有 `release.py` 兼容的冻结清单；不启动 Dry-run。

允许写入：`src/quant_lab/validation.py`、`tests/test_validation.py`、必要时最小修改 `src/quant_lab/__init__.py`、`src/quant_lab/release.py`、`tests/test_release.py`、`pyproject.toml` 和一个 `config/*.example.toml`；以及本任务、`CURRENT_TASK.md`、`TASKS.md` 的实施状态与证据。仅测试所需的合成策略/数据可进入 `tests/fixtures/`。不得修改其他治理、安全、发布政策或 1.1 文件。

实施约束与停止条件：TDD 先红后绿；优先复用 Freqtrade 2026.8 CLI、现有 `release.py` 和标准库，不新增生产依赖或常驻服务。任何必需命令失败、证据缺失/冲突或最终身份无法绑定时结论必须为“证据不足”或“不合格”，不得假装通过。不得读取真实凭据、下载真实账户数据、连接交易所、运行 Dry-run、开发 Web/轮次监督/Windows 宿主或修改 1.1。完成后运行真实 CLI 加载场景、定向测试、全量 pytest、Ruff、`pip check` 和差异检查，记录失败/跳过项并流转 `READY_FOR_VERIFY`；若范围或环境不足则记录 `BLOCKED` 后停止。

下一步继续同一任务的第二期细节分析，每次只处理一个真正由项目补充的关键问题；Freqtrade 已覆盖的行情、回测、优化、交易、数据库、REST API 和界面默认直接复用，不再重新设计。不修复或合并 PR #7，不开始业务开发，也不要求用户凭空猜技术阈值。

用户已采纳独立验证、三类结果、评估前固定标准及保留全部候选记录；这不等于批准任何候选策略或进入实盘。只继续当前分析，不新建子代理，不自动开展业务开发。

## 2026-09-16 候选试跑安排（运行与授权方向已确认，实施治理未完成）

R69 写后核对：git status/HEAD 确认仍在 codex/workbench-init、ba8738fe18289398a8c6713772416facddb5532d，写前写后均仅本任务、CURRENT_TASK、第一期框架三份文档有修改。Node assert 逐文件核对仅预定改动且 R01-R68 原表格行保持一致，严格 UTF-8、末尾换行、行尾空白、冲突标记及 git diff --check 通过。未执行业务测试、Freqtrade、远程 CI 或独立验收，未提交推送。

最新确认：用户对“首版就做到旧策略继续正式模拟、新候选同时独立试跑”回答“是”。登记为 R68，替代 R56 的首版总共单进程限制及对这一个候选试跑实例的延期；不扩大正式轮次的策略数量、资金或风险权限。下文此前的“建议/待确认”是本次确认前的讨论记录，不再表示运行安排仍待选择。

最新授权方向确认：用户对“范围内自动试跑，正式入场逐版本批准”回答“是”，登记 R69。产品设计采用预先批准固定隔离试跑范围、范围内自动测试候选、完整验证后正式运行逐版本批准。此次未提供具体试跑范围，也未修改 RELEASE_POLICY；不构成任何候选的实际启动授权。隔离、范围校验、证据身份、失败关闭和安全/发布治理仍未完成，不再重复询问该授权方向。

下一步验证顺序草案（控制塔依据 R31-R32、R34、R55、R57-R59、R68-R69 推导，非新增已确认条款）：保留 R55 前期加载/正确性检查和 Hyperopt；调参完成后固定最终候选代码、参数、配置及身份，针对最终候选完成正确性检查覆盖、开发回测及独立留出验证；通过试跑启动门槛并具备有效范围授权后，才进入隔离试跑；按预先固定标准汇总三类结论，全部通过后形成冻结证据并申请一次正式批准；获批后按 R58 等待旧持仓结束并核对再切换。调参前结果不能直接代替最终候选检查，版本变化按 R32 重新验证，不能把跨版本试跑成绩拼为同一版本的准入证据。

尚未解决：试跑范围和启动门槛的可执行字段、候选隔离与正式运行配置差异的证据绑定、最终版本各检查的重跑条件、准入阈值，以及候选并发对现行风险/发布规则的独立治理。以上缺口不因 R69 的方向确认而消失；当前仍是第二期 ANALYZING，不运行 Freqtrade。

本次记录范围：更新本任务、CURRENT_TASK 和第一期框架中被 R68 局部替代的说明；R01-R67 原条款保留，既有未提交修改保留；HEAD 仍为 ba8738fe18289398a8c6713772416facddb5532d，状态 ANALYZING，责任为控制塔。未改代码、稳定工作流、安全/发布政策，未启动服务、子代理或提交推送。

本次写后检查：逐文件与写前快照应用预定改动后的结果完全一致；R01-R67 原表格行未改；严格 UTF-8、末尾换行、行尾空白、冲突标记和 git diff --check 通过，差异仅上述三份文档。首次 apply_patch 因日期行上下文不完整退出 1、未写入；改为完整行后成功。未运行 Freqtrade、业务测试、远程 CI 或独立验收。

用户“可以，继续吧”授权继续第二期分析，不视为确认新的并发范围、候选试跑授权或启动服务。任务继续 ANALYZING，责任为控制塔，保留现有 R 条款和第一期框架。

事实：R34 要求新候选跟随新行情隔离试跑，盈亏不进入正式轮次；第一期框架限定一个 Freqtrade 进程并延期多实例；R57 规定完整证据后的正式批准，现行 RELEASE_POLICY 又要求 Dry-run 启动前发布验证。因此运行安排和试跑授权是两个不同的未决问题，不能用解决前者冒充解决后者。

外部依据：2026-09-16 读取 Freqtrade 2026.8 官方 advanced-setup 的 Running multiple instances 章节，支持同机多实例，要求不同数据库，启用 REST API 时使用不同端口。来源：https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/advanced-setup.md 。仅确认文档能力，未安装或实测；原生多实例不会自动提供本项目的资金归属、准入或批准边界。

待确认取舍：串行方案保留单进程，候选实时试跑在正式轮次结束并核对后排队进行，不为试跑强平有效旧策略，也不擅自暂停轮次计时；代价是新候选不能一边实时试跑一边等待接班。并行方案采用一个正式 Dry-run 实例和最多一个候选试跑实例，两者都只模拟，配置、数据目录、数据库与模拟资金独立；候选盈亏不计入本轮，不复制交易引擎，不建设多机器人平台。

控制塔建议：为保留旧策略不中断、候选跟随新行情积累证据的连续研究目标，优先讨论原生的“一正式、一试跑”安排，而非为保住进程数量限制自建模拟器。它会改变第一期单进程/多实例延期的边界，须用户明确确认，并在实施前解决独立安全治理与试跑发布授权；隔离的候选持仓不能自行认定豁免现行仓位限制。当前不修改框架、R56 或安全政策。

本轮只问：首版是否需要在旧策略继续正式模拟时，让一个新候选独立跟随新行情试跑？确认此产品取舍后，再解决试跑如何获准且不循环依赖正式准入证据；每版本正式批准仍保留，不能提前承诺自动试跑或增加第二次正式批准。

核对基线：cwd D:/CodexProjects/projects/quant-research-live-lab；分支 codex/workbench-init；HEAD ba8738fe18289398a8c6713772416facddb5532d；origin 为 JunYIChen12/quant-research-live-lab。写前仅本任务文件和 CURRENT_TASK.md 已修改，保留全部既有内容。仅追加本分析节及更新当前指针，不提交、推送、运行业务测试、启动 Freqtrade 或子代理。

### 试跑范围与启动门槛（2026-09-16，授权恢复已确认，其余细节仍为草案）

R70 写后核对：仍为 codex/workbench-init / ba8738fe18289398a8c6713772416facddb5532d / ANALYZING；Node assert 检查本轮仅预定的任务与指针变更、R01-R69 原表格行及框架字节保持不变；严格 UTF-8、换行、空白、冲突标记及 git diff --check 通过。仅文档自查，未运行业务测试、Freqtrade、远程 CI 或独立验收，未提交推送。

本轮“可以”按继续细化理解，不认定用户已经批准下列具体字段或实际运行。依据 R31-R39、R57-R59、R68-R69 整理最小规格，复用原有分析任务，不另建审批平台或评分引擎。

| 检查对象 | 草案范围与启动条件 | 依据或未决 |
| --- | --- | --- |
| 用途与并发 | 只允许候选隔离 Dry-run，最多一个试跑实例；不能借试跑开启另一轮正式模拟或真实交易 | R34、R43、R68；实际并发仍须安全治理 |
| 授权范围 | 一个不可由生成器修改的授权配置版本，记录模拟资金/风险上限、品种范围及选择规则、验证规则版本；范围内可更换候选，但每个候选代码和参数分别固定、留档 | R31-R32、R36、R39、R69；具体金额/规则尚未确定，不要求用户逐候选填参数 |
| 运行隔离 | 候选只能使用自己的配置、数据、原生数据库与模拟资金，不能写正式数据库、批准记录或停机锁，不能接触真实凭据；隔离不是只改一个目录名，须有后续可复核证据 | R34、R68 和安全约束推导；执行隔离方法尚未实现 |
| 试跑启动证据 | 最终候选身份可核对；调参后的正确性检查、历史回测和独立留出检查达到预先固定的试跑门槛；行情、存储及授权状态可核对；任何必需证据缺失都不启动 | R31-R39 推导；精确门槛仍待设计。尚未取得的前向试跑结果不能被设为开始该次试跑的前提，否则循环依赖 |
| 正式资格 | 试跑许可不等于候选合格；前向证据与历史证据完整汇总为三类结论，全部通过后才申请准确版本的一次正式批准，再按 R58 交接 | R38、R57-R59、R69；不得用试跑授权解开正式停机锁 |
| 授权恢复 | 已确认 R70：固定范围批准后持续有效，不因范围内换候选或重启重复审批；重启须先核对授权和状态；撤销、范围改变或无法核对时禁止新启动试跑 | 只确认授权延续原则，不等于候选免检查或正式版本免批准；具体恢复核验及存量仓位处理仍需规格化 |

2026-09-16 用户对“范围不变就沿用授权，重启先核对、不重复审批”回答“是”，登记 R70。不把这次确认扩大为本表全部技术字段、具体试跑范围或实际启动许可。

2026-09-16 用户对撤销试跑授权的收尾方案回答“是”，登记 R71，补齐 R70 中存量仓位收尾的未决项：停止候选新开仓，取消尚未成交的入场委托，平掉剩余模拟持仓、核对并保留记录后停止本次试跑；收尾失败或状态无法确认时保持暂停并提醒，不能删库抹账或宣称已结束。正常撤销仅作用于候选试跑，正式策略仅在自身安全且隔离可核对时继续；共同安全故障仍按原规则处理。此处确认产品行为，不授权操作现有进程或账户。

R71 写后证据：仍在唯一根目录 D:/CodexProjects/projects/quant-research-live-lab，分支 codex/workbench-init，HEAD ba8738fe18289398a8c6713772416facddb5532d，任务 ANALYZING/控制塔负责。Node assert 验证本轮仅任务和指针的预定修改，R01-R70 原表格行与框架原字节保留；严格 UTF-8、换行、空白、冲突标记、git diff --check 通过。以上五项是待运行的验收场景，不是测试通过证据；未运行业务测试、Freqtrade、远程 CI 或独立验收，未提交推送、启动子代理或修改安全/发布政策。

最小验收场景（设计预期，未实际执行）：

| 场景 | 必须观察到的结果 |
| --- | --- |
| 空仓且无入场委托时撤销 | 核对后结束试跑，拒绝新试跑，保留已有记录 |
| 有未成交入场委托或模拟持仓时撤销 | 先禁止新增风险，核对撤单和实际剩余仓位，再完成平仓核对；发出命令不算完成 |
| 部分平仓后重启 | 保留撤销和未完成状态，核对最新记录，只处理实际剩余仓位；不恢复新开仓，不重复计账 |
| 收尾失败或无法核对 | 保持暂停并通知，显示未完成；不伪造空仓或通过重置数据宣布成功 |
| 正式实例仍在运行 | 隔离和正式实例安全仍可核对时不因正常撤销被误停；若共同安全无法确认，按既有失败关闭规则处理 |

R68-R71 的并发安排、范围授权原则、授权延续及撤销行为已确认，不继续逐条重复提问。下一步回到候选准入主线：把已有 R37-R39 对应到检查项、输入/证据、合格/失败/证据不足判定及仍缺阈值的有限清单；技术依据先查现有资料和锁定版原生能力，不让用户凭空猜数字。具体范围和启动门槛仍未确定，安全/发布治理仍未完成，不宣称 READY。

金额、品种选择、历史门槛、授权检查频率和实现隔离仍需规格化；不能把本表当作完整 READY 或现行发布政策豁免。

核对：唯一根目录 D:/CodexProjects/projects/quant-research-live-lab，codex/workbench-init，HEAD ba8738fe18289398a8c6713772416facddb5532d；状态 ANALYZING，控制塔负责。写前任务、指针和框架三文件已有修改，本轮仅改任务与指针，框架既有改动保留。R01-R69 与安全/发布政策不改；不安装、运行、提交、推送或启动子代理。

本草案写后检查：Node assert 验证移除新增草案/指针替换后与写前内容一致，既有 R 条款未改；严格 UTF-8、末尾换行、空白、冲突标记及 git diff --check 通过。仅做文档静态检查，未执行业务测试、Freqtrade、远程 CI 或独立验收。

### 候选准入检查表草案（2026-09-16，判定结构待整体确认）

准入表写后证据：仍为 codex/workbench-init / ba8738fe18289398a8c6713772416facddb5532d / ANALYZING。Node assert 验证移除新增检查表及指针替换后与写前内容一致，R01-R71 和第一期框架未改；严格 UTF-8、末尾换行、空白、冲突标记、不可见控制字符及 git diff --check 通过。初次插入的三个命令名含异常 U+001F 控制字符，已按完整表格行修正并复查无残留。未执行 Freqtrade 命令、业务测试、远程 CI 或独立验收，未提交推送。

本表承接 R31-R39、R55、R57、R68-R71，只定义最小证据结构，不建立自定义评分系统，也不预填无项目依据的天数、笔数或收益阈值。三类结果沿用既有契约：确认触发硬失败为不合格；没有硬失败但必需证据缺失或未覆盖为证据不足；全部必检项通过才获得申请正式批准的资格，不自动运行。

| 顺序与检查 | 输入与应保存证据 | 硬失败（不合格） | 证据不足 | 通过含义 |
| --- | --- | --- | --- | --- |
| 1. 冻结身份与隔离 | 策略代码、参数、配置、交易对/选择规则、数据范围、原生版本、随机种子、授权配置摘要 | 身份冲突、产物被修改、生成模块可改验证规则、读写越过隔离边界 | 任一必需身份、摘要或来源无法核对 | 只证明待检对象唯一且可复核 |
| 2. 原生加载与结构 | `list-strategies`、配置解析及依赖结果 | 最终候选无法由锁定版加载，或配置违反已冻结硬限制 | 命令未执行、环境/依赖不是目标环境或输出不完整 | 只证明可加载，不证明逻辑或盈利 |
| 3. 未来数据检查 | 最终候选的 `lookahead-analysis` 原生结果、基准与切片回测输出 | 确认关键指标或信号使用未来信息 | 命令失败、信号未实际触发、覆盖不足或结果无法归属最终版本 | 未发现已覆盖路径中的未来数据偏差 |
| 4. 递归/启动期检查 | 最终候选的 `recursive-analysis` 结果、startup candle、指标差异 | 差异超过预先固定且有依据的容许规则，足以改变信号或风险行为 | 指标/周期未覆盖、数据不足或容许规则未确定 | 已检查路径满足固定递归一致性规则 |
| 5. 优化与版本固定 | Hyperopt 输入空间、损失函数、开发数据、随机种子、结果；调参完成后的最终候选摘要 | 优化越过冻结搜索空间/风险边界，或最终产物与结果不匹配 | 过程或最终摘要缺失、不能复现 | 只产生一个待验证最终候选；Hyperopt 好分数不是准入 |
| 6. 最终版本历史验证 | 最终候选重新执行必要正确性检查、开发回测、独立留出结果及完整费用/执行假设 | 任何硬规则失败；留出结果触发后续确定的风险/收益硬门槛 | 开发与留出泄漏、关键路径未触发、成本缺失、样本或门槛尚不足 | 历史证据完整，不等于前向运行已证明 |
| 7. 隔离前向试跑 | R68-R71 授权身份、候选数据库、订单/成交/持仓/费用和异常收尾记录 | 越权、隔离失效、账目矛盾、硬风险失败或确认的运行一致性失败 | 未覆盖关键入场/退出/恢复路径，观察证据不足或状态无法核对 | 前向模拟证据满足固定规则，可进入总评 |
| 8. 总评与正式批准 | 全部原生输出、证据包摘要、失败/不足原因和准确候选身份 | 任一必检项硬失败；不同版本证据拼接；因无人通过临时降标 | 任一必需证据缺失、过期、矛盾或阈值未定 | 仅取得向用户申请 R57 正式版本批准的资格 |

官方能力依据（只作为工具事实，不代表仓库已安装或运行）：Freqtrade 2026.8 lookahead-analysis 会比较基准回测与按信号切片的回测来识别可能的未来数据偏差，并明确指出信号不足会使结果不确定；recursive-analysis 用不同 startup candle 数比较指标差异；Hyperopt/Backtesting 提供优化和历史结果，但项目仍须负责数据隔离、最终版本绑定和证据充分性。来源：https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/lookahead-analysis.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/recursive-analysis.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/hyperopt.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/backtesting.md 。本轮未执行这些命令。

有限缺口：递归差异容许规则、历史/留出/试跑证据充分性、收益和风险门槛、成本压力参数、关键路径覆盖，以及“合格候选是否必须优于仍有效的正式策略”。前五项须由控制塔结合官方/研究依据和以后项目数据提出可校准方案，不让用户凭空给数字。

2026-09-16 用户对该建议回答“是”，登记 R72：合格只代表进入候选池；当前正式策略仍有效时，新候选必须用同一口径的风险、收益和运行证据证明整体更好，才可申请替换。未证明更好就继续试跑或等待，不为使用新版本而平掉仍有效策略。正式批准、R58 交接和硬风险失格流程保持不变。

R72 写后证据：基线仍为 codex/workbench-init / ba8738fe18289398a8c6713772416facddb5532d，任务 ANALYZING/控制塔负责。Node assert 验证仅任务与指针的预定修改，R01-R71 原表格行和第一期框架字节保持不变；严格 UTF-8、换行、空白、冲突标记、控制字符及 git diff --check 通过。未运行 Freqtrade、业务测试、远程 CI 或独立验收，未提交推送。

2026-09-16 用户补充事实：“我个人的投资偏好是风险型的。”这会修正控制塔此前“风险指标不得变差”的建议，但不自动形成 R73，也不表示允许越过硬亏损、爆仓安全距离、隔离、执行可靠性、授权或现行安全政策。风险偏好与情绪化追损、临时加杠杆不是同一件事；系统仍须按预先固定规则和证据决策。

风险偏好记录写后证据：仍为 codex/workbench-init / ba8738fe18289398a8c6713772416facddb5532d / ANALYZING。Node assert 验证仅任务与指针的预定修改，R01-R72 及第一期框架未改；严格 UTF-8、换行、空白、冲突标记、控制字符及 git diff --check 通过。当前只记录用户偏好并修订建议，不把它冒充 R73 或风险扩权；未运行 Freqtrade、业务测试、远程 CI 或独立验收，未提交推送。

2026-09-16 用户对受控风险收益权衡原则回答“是”，登记 R73：在硬安全、隔离和执行可靠性全部合格，且不超过用户预先批准风险上限时，允许候选用更高回撤或波动换取有充分证据支持的更高扣费后收益。不因风险指标高于旧策略自动否决，也不因收益更高自动通过；不得在亏损、追求回本或目标尚未达成时临时放宽风险。

R73 写后证据：分支 codex/workbench-init、HEAD ba8738fe18289398a8c6713772416facddb5532d、任务 ANALYZING/控制塔负责未变。Node assert 验证本轮仅任务与指针预定修改，R01-R72 和第一期框架保持不变；严格 UTF-8、末尾换行、空白、冲突标记、控制字符及 git diff --check 通过。未运行 Freqtrade、业务测试、远程 CI 或独立验收，未提交推送。

2026-09-16 用户对风险必须获得相匹配收益补偿的原则回答“是”，登记 R74：略高收益不能补偿明显更大的风险；候选相对仍有效策略增加的风险，必须由足够大、同口径且稳健的扣费后收益改善补偿，才可认定整体更好。具体计算方法和阈值不由用户凭空指定，须依据公开方法和项目数据校准。

R74 与指标方案写后证据：基线仍为 codex/workbench-init / ba8738fe18289398a8c6713772416facddb5532d，任务 ANALYZING/控制塔负责。Node assert 验证本轮仅任务与指针预定修改，R01-R73 和第一期框架保持不变；严格 UTF-8、末尾换行、空白、冲突标记、控制字符及 git diff --check 通过。来源为 Freqtrade 官方文档和原论文；尚未核定容差或运行命令。未执行 Freqtrade、业务测试、远程 CI、独立验收或提交推送。

来源驱动的最小比较方案（控制塔建议，尚未确认）：不新建综合评分平台。硬安全、隔离、执行可靠性和证据完整性先作为不可抵消门槛；通过后，用同一数据区间、资金、交易对、成本和版本口径比较扣费后净收益与 wallet 最大回撤，以 Calmar 作为“收益是否补偿回撤”的主比较指标。风险型候选申请替换时，建议同时要求净收益高于旧策略且 Calmar 不低于旧策略；Sortino、Profit Factor、Expectancy、回撤持续时间、关键路径覆盖和留出/前向一致性作为交叉检查，不允许单一漂亮指标抵消其他硬失败。多次生成和筛选候选时，记录试验次数，并评估使用 Deflated Sharpe Ratio 修正选择偏差；它不替代前向试跑。

依据事实：Freqtrade 当前 Backtesting 报告原生给出绝对/百分比利润、wallet 最大回撤、回撤持续时间、Sharpe、Sortino、Calmar、Profit Factor、Expectancy 和 mean profit p-value，费用包含在利润计算中；官方同时提示回测应与 Dry-run 比较。Bailey 与 López de Prado 的 Deflated Sharpe Ratio 用于修正多重选择、回测过拟合和非正态收益对 Sharpe 的夸大。来源：https://docs.freqtrade.io/en/stable/backtesting/ 、https://www.davidhbailey.com/dhbpapers/deflated-sharpe.pdf 。当前锁定目标仍为 Freqtrade 2026.8，实施前须再次核对相应标签并运行验证；本轮没有执行工具。

2026-09-16 用户表示未理解 Calmar 问题后，控制塔用“新策略可以更冒险，但多冒的风险必须值得；不能只多赚一点点就换策略”解释，用户回答“是”。本次视为对 R74 的再次确认，不新增重复的 R75。用户没有明确确认“净收益必须更高且 Calmar 必须不低于旧策略”这一具体数学规则；Calmar 继续作为待评估工具，不能写成已确认硬门槛。

R74 通俗确认写后证据：分支 codex/workbench-init、HEAD ba8738fe18289398a8c6713772416facddb5532d、状态 ANALYZING 未变。Node assert 验证本轮仅任务与指针预定修改，R01-R74、第一期框架和既有指标建议保持不变；严格 UTF-8、换行、空白、冲突标记、控制字符及 git diff --check 通过。本轮没有新增 R75，没有把 Calmar 提案冒充用户确认；未运行 Freqtrade、测试、远程 CI 或独立验收，未提交推送。

下一步由控制塔比较最小可解释方案：单一 Calmar 门槛、收益/回撤双门槛、或多指标证据表。选择标准是符合 R72-R74、支持风险型偏好、避免优化单一指标、可用 Freqtrade 原生输出实现且能用项目数据校准。先形成有依据的推荐，不继续要求用户理解或选择统计术语。

核对范围：仍为 20260912-002 / ANALYZING / 00｜项目控制塔，cwd、分支和 HEAD 不变。本轮只追加本表并更新 CURRENT_TASK；R01-R71、框架、安全与发布政策不变，不安装、运行、提交、推送或启动子代理。

### 风险收益比较方案对照（2026-09-16，推荐方案已确认）

比较方案写后证据：Node assert 验证移除新增方法对照及指针替换后与写前内容一致，R01-R74 与第一期框架未改；严格 UTF-8、末尾换行、空白、冲突标记、控制字符及 git diff --check 通过。来源核对只形成设计建议，未安装或运行 Freqtrade，未验证实际字段、业务测试、远程 CI 或独立验收，未提交推送。

目标：落实 R72-R74，又不重建自定义评分系统。仓库未安装 Freqtrade，设计目标版本仍为 2026.8；本节只比较方法，未运行计算。

| 方案 | 做法 | 优点 | 主要问题 | 结论 |
| --- | --- | --- | --- | --- |
| A. 单一 Calmar | 用收益相对最大回撤的一个比率排序 | 最简单，能表达“风险是否值得” | 年化结果可能受观察区间影响；一个数字会掩盖交易数量、回撤持续时间、成本、选择偏差和前向不一致 | 不建议作为唯一门槛 |
| B. 收益＋回撤双门槛 | 新候选扣费后净收益更高，并控制最大回撤或要求收益增幅补偿回撤增幅 | 容易解释，不需要总分 | 仍可能漏掉收益分布、亏损侧波动、单笔期望、执行一致性和反复筛选造成的偶然赢家 | 可作为主比较，不足以单独决定 |
| C. 无总分证据表 | 硬门槛先行；收益和 wallet 最大回撤为主比较；Calmar/Sortino、Profit Factor、Expectancy、回撤持续时间、留出/前向一致性分别列证据，不相加成一个总分 | 可解释，直接复用 Freqtrade 输出，不让某一漂亮指标抵消硬失败；适合以后校准 | 判定规则比单一公式多，需要明确哪些是硬门槛、主比较和交叉反证 | 推荐 |

控制塔推荐方案 C 的最小结构：

1. 硬门槛：未来数据、递归一致性、隔离、执行/账目、硬亏损和证据完整性任一失败都不合格，收益不能抵消。
2. 主比较：同一数据、资金、交易对、成本和版本口径下，候选扣费后净收益须表现出有意义的改善；增加的 wallet 最大回撤须得到足够补偿。
3. 交叉反证：Calmar/Sortino、Profit Factor、Expectancy、回撤持续时间、留出和隔离前向结果若明显恶化或互相矛盾，结论为不合格或证据不足，不能靠主指标强行通过。
4. 选择偏差：保存全部候选和试验次数；批量筛选时评估 Deflated Sharpe Ratio 等修正，不把“试很多次后最漂亮的一个”直接视为真实优势。
5. 不建立加权总分、排行榜平台或新的优化器。Freqtrade 的 MultiMetricHyperOptLoss 可用于生成候选，但优化得分不等于项目准入结论。

来源事实：Freqtrade 官方 Backtesting 输出包含 Absolute/Total profit、wallet 最大回撤、回撤持续时间、Sharpe、Sortino、Calmar、Profit Factor、Expectancy 和 mean profit p-value；官方 Hyperopt 提供 Calmar、ProfitDrawDown 和 MultiMetric 等原生损失函数，其中 MultiMetric 同时考虑利润、回撤、Profit Factor、Expectancy 与胜率。Sharpe 的原始说明把它定义为回报相对波动的度量，也讨论其限制；Bailey 与 López de Prado 的 Deflated Sharpe Ratio 用于修正多重选择、回测过拟合和非正态收益。来源：https://github.com/freqtrade/freqtrade/blob/2026.8/docs/backtesting.md 、https://github.com/freqtrade/freqtrade/blob/2026.8/docs/hyperopt.md 、https://web.stanford.edu/~wfsharpe/art/sr/sr.htm 、https://www.davidhbailey.com/dhbpapers/deflated-sharpe.pdf 。实施前仍须在锁定环境核对实际输出字段。

2026-09-16 用户对此产品选择回答“是”，登记 R75：候选替换不计算可互相抵消的总分；先检查不可抵消的硬门槛，通过后再以同口径扣费后收益与 wallet 最大回撤作主比较，其他 Freqtrade 原生指标及留出/隔离前向结果负责找反例。具体容差和证据充分性继续依据研究与项目数据校准，不要求用户选择 Calmar、Sortino 等统计术语。

2026-09-16 用户同意冲突处理：如果主比较认为候选更好，但辅助证据出现相反信号，安全或有效性硬门槛失败则不合格；只是证据互相矛盾则标记“证据不足”并继续试跑或收集数据，不直接通过或淘汰。这是 R38 三类结论在 R75 证据表中的具体应用，不新增重复的 R76。

2026-09-16 用户同意自动化边界：候选处于“证据不足”时，只要原有隔离试跑授权仍有效、范围未变且没有安全失败，系统可自动继续试跑和收集数据，不反复要求用户批准；证据变得明确、授权撤销、范围改变或安全失败时停止并通知。这是 R38、R69、R70 的组合应用，不新增重复的 R76。长期仍无合格候选时继续沿用既有约定：通知用户选择继续研究或修改非安全筛选标准，系统不得自行降低标准。

2026-09-16 用户确认固定试跑授权的结构，登记 R76：授权范围至少冻结交易所/市场类型、允许品种、最大杠杆、最大持仓数、模拟资金上限和有效期；系统可在这些边界内自动更换候选，任一边界扩大或无法核对都必须停止并重新批准。具体数值后续依据现行安全规则和项目数据填写；本条不自动放宽现行 1 倍杠杆、单持仓或发布门禁。

2026-09-16 用户确认授权到期的收尾：到期视同撤销授权，立即停止候选新开仓、取消未成交入场委托、平掉候选模拟持仓、核对并保留记录后停止；任何一步失败都保持暂停并通知，不影响仍安全隔离的正式策略。这是 R71 撤销流程对 R76 有效期的直接应用，不新增重复的 R77。

### 候选证据充分性方案对照（2026-09-16，推荐方案已确认）

目标：回答历史、未见数据和隔离前向证据何时足以支持判断，同时落实 R38-R39、R72-R75；不建立自定义总分，不预填无项目依据的统一天数或交易笔数。

来源事实：

- Freqtrade 2026.8 的 lookahead-analysis 只有达到命令配置的 minimum-trade-amount 才继续检查；达不到时官方建议扩大时间范围或选择信号更多的区间。该数量用于让偏差检查可执行，不是策略盈利或准入证明。
- recursive-analysis 官方建议用至少 5000 根蜡烛形成基准，但是否“足够小”仍要看指标差异是否会影响入场或退出；绝对零差异并非统一要求。
- Backtesting 输出交易数量、扣费后收益、wallet 回撤、Sharpe/Sortino/Calmar、Profit Factor、Expectancy 和 mean profit p-value。官方同时明确：p-value 假设交易独立同分布，现实交易往往不满足；大量回测或 Hyperopt 还会产生偶然漂亮结果，因此单个低 p-value 不是优势证明。
- Freqtrade 官方说明不同策略的交易频率可相差很大，历史结果也可能随 Hyperopt 随机状态变化；所以把所有策略统一规定为同一天数或同一笔数，会对高频和低频策略产生不同含义。
- Sharpe Ratio Efficient Frontier 用 Probabilistic Sharpe Ratio 和 Minimum Track Record Length 把样本长度、目标门槛、置信度、偏度和峰度联系起来；Deflated Sharpe Ratio 进一步修正多次筛选、回测过拟合和非正态收益；Probability of Backtest Overfitting 说明普通留出法在大量策略筛选中仍可能不可靠，并提出用多切分结果估计过拟合概率。

来源：https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/lookahead-analysis.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/recursive-analysis.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/backtesting.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/hyperopt.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/faq.md 、https://www.davidhbailey.com/dhbpapers/sharpe-frontier.pdf 、https://www.davidhbailey.com/dhbpapers/deflated-sharpe.pdf 、https://www.davidhbailey.com/dhbpapers/backtest-prob.pdf 。本轮只阅读资料，没有运行命令。

| 方案 | 做法 | 优点 | 主要问题 | 结论 |
| --- | --- | --- | --- | --- |
| A. 所有策略统一固定天数和笔数 | 例如不区分频率，统一要求同一观察天数和交易笔数 | 最容易解释 | 同一数字对高频和低频策略含义不同；可能样本很多但只覆盖单一行情，也可能时间很长却没有足够事件 | 不建议 |
| B. 只用统计公式 | 主要用 PSR/MinTRL、DSR 或 PBO 判断样本是否足够 | 能处理样本不确定性和多次筛选 | 不能证明订单、费用、重启、退出等运行路径可靠，也不能替代隔离前向试跑 | 不建议单独使用 |
| C. 自适应混合证据门槛 | 正确性硬门槛先行；历史开发、未见数据和隔离前向分别留证；样本长度按策略频率和统计不确定性决定；关键运行路径必须覆盖 | 不要求用户猜统一数字；兼顾过拟合、行情变化和实际执行；直接复用 Freqtrade 输出 | 后续仍须用项目数据校准置信要求、成本压力和关键路径清单 | 推荐 |

推荐方案 C 的最小结构：

1. 正确性先过关：最终冻结版本必须通过 lookahead、recursive 影响评估、数据/成本/配置绑定；任何硬失败直接不合格。
2. 历史证据分层：开发数据只用于生成和调参；未见数据不得反复偷看和回改；同时保存全部候选、试验次数及分段结果，防止只展示最漂亮区间。
3. 样本自适应：不用统一 90 天、100 笔之类默认值。根据策略实际交易频率、收益分布和筛选次数计算不确定性；MinTRL/PSR、DSR/PBO 能计算时作为反证工具，不能计算或不稳定时判为证据不足。
4. 不新建行情分类平台：先复用 Freqtrade 的日/月/年、品种、入场/退出标签和回撤分解，检查优势是否只来自单一时间段、品种或少数交易；发现集中依赖时不直接通过。
5. 隔离前向补执行证据：必须覆盖约定的入场、退出、费用、暂停、恢复和异常收尾路径，且结果方向不能明显反驳未见数据。未覆盖就继续试跑，不能用历史样本替代。
6. 三层证据都达到预先冻结的要求后，候选才从“证据不足”转为“合格并可申请批准”；这仍不是正式运行批准。

2026-09-16 用户对此产品选择回答“是”，登记 R77：不同策略不强行使用同一个天数和笔数；候选必须同时通过正确性、未见数据和隔离试跑三层证据，样本是否充分按策略频率、收益分布、筛选次数和统计不确定性判断。无法稳定计算或仍有明显矛盾时保持“证据不足”，不能用历史样本替代隔离前向证据，也不能据此自动获得正式运行批准。

2026-09-16 用户确认首个正式候选的比较基准，登记 R78：没有仍有效的正式策略时，以“不交易/保持空仓”为主要基准，候选必须证明同口径扣费后优势为正、风险在授权内且 R77 三层证据充分；市场变化或买入持有结果只作辅助参考。已有仍有效正式策略时继续按 R72-R75 证明整体更好。用户盈利目标不替代这两个准入基准。

### 成本压力方案对照（2026-09-16，推荐方案已确认）

目标：补齐 R18、R37、R39、R73-R78 的成本证据，不重建成交账本，不用无依据的统一倍数，也不让回测未覆盖的成本被当作零。

来源事实：Freqtrade 2026.8 的 Backtesting 默认使用交易所费用，利润计算包含手续费；也可用 --fee 输入自定义费率，且对入场和退出各应用一次。期货模式会把可用的资金费率计入交易结果，但官方警告缺失或伪造资金费率会使历史结果不准确。回测假设订单在请求价格成交且没有滑点，无法替代 Dry-run；Freqtrade 能计算清算价格，但不跟踪清算费用，因此接近清算的结果可能不准确。来源：https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/backtesting.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/leverage.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/bot-basics.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/strategy-101.md 。本轮未执行命令。

| 方案 | 做法 | 优点 | 主要问题 | 结论 |
| --- | --- | --- | --- | --- |
| A. 只用普通回测成本 | 采用交易所默认手续费和已有资金费率，不额外检查滑点或成本变化 | 完全复用原生结果 | 默认无滑点，缺失资金费率或清算费用会造成乐观或不准确结果 | 不足以准入 |
| B. 所有策略统一提高固定倍数 | 对手续费、滑点或资金费统一乘同一个固定倍数 | 容易执行 | 不同品种、时段、订单类型和杠杆的成本差异很大；此前无依据的统一倍数已撤回 | 不建议 |
| C. 正常成本＋不利成本双情景 | 正常情景使用可核对的费率、历史资金费和实际试跑执行偏差；不利情景依据交易所规则、历史尾部和项目观测值校准，检查优势和硬风险是否仍成立 | 不需要猜固定倍数；能暴露薄利润和高换手策略；复用原生输出，只补未覆盖成本 | 在积累试跑数据前，一些压力范围只能保持“证据不足” | 推荐 |

推荐方案 C 的三类结果：

1. 正常成本数据缺失、费率身份无法核对或资金费被伪造：证据不足，不能启动或通过。
2. 正常成本下扣费后优势不为正，或已有策略时未达到 R72-R75：不合格。
3. 不利成本下触及硬亏损、清算安全距离或其他硬风险：不合格。
4. 正常成本通过，但合理不利成本会消除优势、且没有触及硬风险：证据不足，说明策略太依赖低成本，继续积累执行数据或改进策略。
5. 正常与不利成本都通过，且隔离试跑中的实际费用、滑点和资金费没有明显反驳：成本证据通过。

压力范围不在本轮写死：优先使用交易所费率档位、实际订单与理论价格偏差、历史资金费尾部及隔离试跑记录校准；数据不足时保持证据不足，不能恢复此前撤回的 1.5 倍、2 倍之类默认值。

2026-09-16 用户对此产品选择回答“是”，登记 R79：成本证据采用正常与不利双情景。正常成本数据缺失为证据不足，正常成本下扣费后优势不成立为不合格；合理不利成本触发硬亏损、清算安全距离或其他硬风险为不合格，只让盈利优势消失但未触发硬风险为证据不足；两种情景均通过且隔离试跑实际成本未明显反驳，成本证据才通过。压力范围依据交易所规则和项目数据校准，不使用统一固定倍数。

### 关键运行路径与证据复用方案（2026-09-16，推荐方案已确认）

Freqtrade 2026.8 把策略入口、退出、止损、仓位调整、杠杆、超时和成交回调暴露为策略能力；执行循环、订单状态更新和持久化恢复由引擎与数据库承担。多实例还要求独立数据库等资源。来源：https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/strategy-callbacks.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/bot-basics.md 、https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/advanced-setup.md 。本轮未执行命令。

| 方案 | 做法 | 结论 |
| --- | --- | --- |
| A. 每个候选重跑全部平台场景 | 每个策略版本都重新测试引擎重启、数据库恢复、通知等共享能力 | 重复且慢，把平台验证误当作策略验证，不建议 |
| B. 平台证据全部长期复用 | 候选只测试收益，不重新检查策略路径或证据是否过期 | 可能复用到不兼容版本，风险过高，不建议 |
| C. 按依赖复用 | 候选相关路径每版本重跑；共享平台证据只在依赖指纹不变、证据未过期且无失败时复用 | 推荐 |

推荐方案 C 的最小边界：

1. 每个候选必重跑：策略加载、最终代码/参数/配置身份、实际使用的入场与退出标签、止损、杠杆、仓位调整、订单价格/超时回调，以及对应的历史、未见和隔离前向结果。未使用的可选回调不要求制造测试。
2. 平台证据可复用：启动/暂停、数据库恢复、订单/持仓核对、候选授权撤销/到期收尾和 Windows 通知；前提是 Freqtrade 版本、交易所与市场模式、订单类型、数据库/存储、外围编排和相关配置未变。
3. 任一依赖改变、证据过期、曾失败或身份无法核对，只使受影响证据失效并重跑；不无条件清空全部证据，也不把失败证据继续继承。
4. 候选应覆盖却未触发的策略路径判为证据不足；确认路径行为错误或越过硬风险才判为不合格。
5. 只保存一份简单的证据清单和依赖身份，不建设测试矩阵平台或新的运行时框架。

2026-09-16 用户对此产品选择回答“是”，登记 R80：策略代码、参数和配置决定的路径每个冻结候选必须重跑；Freqtrade 平台公共路径只有在版本、配置、环境、依赖身份和证据有效性均未变化时才可复用，发生变化只使受影响证据失效并重跑。候选应覆盖但未触发的路径为证据不足，确认错误或越过硬风险为不合格；复用不免除 R57 对新冻结版本形成完整验证结论和重新批准的要求。

### 递归差异容许方案（2026-09-16，推荐方案已确认）

Freqtrade 2026.8 的 recursive-analysis 用不同 startup_candle_count 计算指标，并把最后一行相对长区间基准的差异列出来。官方建议基准区间至少约 5000 根蜡烛，并明确要求按指标判断差异是否足够小到不影响入场或退出；绝对零差异不一定合理。命令只比较最后一行指标，不直接判断真实信号影响，也不会覆盖写在 populate_entry_trend 或 populate_exit_trend 中的指标计算。来源：https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/recursive-analysis.md 。本轮未运行命令。

| 方案 | 做法 | 主要问题 | 结论 |
| --- | --- | --- | --- |
| A. 所有指标必须绝对零差异 | 任何非零值都淘汰 | 官方明确不要求机械追求零；可能需要不现实的启动蜡烛数量 | 不建议 |
| B. 所有指标统一固定百分比 | 给每个指标同一个差异上限 | 同一数值对 RSI、EMA、价格和布尔信号含义不同，不能证明是否改变交易 | 不建议 |
| C. 按交易决策影响判断 | 原生差异表先定位问题，再比较所选启动蜡烛是否改变实际入场、退出、止损、杠杆或仓位决策 | 需要冻结覆盖样本和决策比较方法 | 推荐 |

推荐方案 C 的三类结果：

1. 指标无法计算、命令未覆盖相关计算、样本或启动蜡烛不足、靠近信号阈值的场景没有覆盖：证据不足。
2. 在交易所可提供的数据限制和候选配置下，递归差异确认会改变入场、退出、止损、杠杆、仓位或其他风险决策：不合格。
3. 指标差异非零，但在预先冻结的覆盖样本中没有改变任何相关决策，且所需启动蜡烛可由目标交易所可靠提供：该项通过。
4. 不为每个指标创建独立人工百分比，也不建设新的指标分析平台；只保存原生命令结果、启动蜡烛配置和最小决策差异证据。

2026-09-16 用户对此产品选择回答“是”，登记 R81：递归指标不要求绝对零差异；在目标交易所数据限制和冻结配置下，差异确认会改变入场、退出、止损、杠杆、仓位或其他风险决策时不合格；命令未覆盖、样本或临界场景不足时为证据不足；存在非零差异但已证明不改变任何相关决策时，该项可通过。

### 统计置信校准方案（2026-09-16，推荐方案已确认）

来源事实：Freqtrade 2026.8 提供 mean profit p-value，并把小于 0.05 说明为常见显著性经验线，但官方明确警告该检验假设交易独立同分布，真实策略通常不满足；大量 Backtesting/Hyperopt 还会产生偶然低 p-value，因此它不能单独证明真实优势。Bailey 与 López de Prado 的 PSR/MinTRL 用样本长度、门槛、置信度、偏度和峰度估计优势可信度；论文以 95% 展示大量案例。DSR 进一步按候选试验数量和非正态收益修正赢家偏差；PBO/CSCV 用多切分结果估计回测过拟合风险。来源：https://raw.githubusercontent.com/freqtrade/freqtrade/2026.8/docs/backtesting.md 、https://www.davidhbailey.com/dhbpapers/sharpe-frontier.pdf 、https://www.davidhbailey.com/dhbpapers/deflated-sharpe.pdf 、https://www.davidhbailey.com/dhbpapers/backtest-prob.pdf 。本轮未运行计算。

| 默认置信要求 | 优点 | 主要问题 | 结论 |
| --- | --- | --- | --- |
| 90% | 较快得到结论 | 偶然优势进入候选池的概率较高 | 不建议作为首版默认 |
| 95% | 常见的 5% 显著性经验线；在速度与误判之间较平衡 | 不是盈利保证，仍需修正多次筛选和非正态 | 推荐默认 |
| 99% | 更保守 | 低频策略可能需要过长时间，容易长期停在证据不足 | 不建议作为统一默认 |

推荐的最小规则：

1. 95% 是证据可信度默认值，不是用户风险偏好或盈利保证；风险型偏好不能降低证据真实性要求。
2. 首个候选针对 R78 的空仓基准，已有策略的替换针对同口径现有策略差异；统计比较必须与扣费后收益、回撤和三层证据使用同一冻结版本和数据口径。
3. 只生成或比较单个候选时，用 PSR/MinTRL 或等价单侧不确定性检查衡量是否达到 95%；Freqtrade 原生 p-value 只作辅助反证。
4. 多次生成、Hyperopt 或筛选候选时，必须记录全部试验数量，并使用 DSR 或等价选择偏差修正；可计算 PBO 时作为额外交叉反证，不用未修正的最好结果直接通过。
5. 扣费后优势已确认不成立为不合格；优势为正但修正后置信不足 95%、试验数量无法核对或方法不稳定为证据不足；修正后达到 95% 且没有其他硬失败，统计证据才通过。
6. 不建立统计服务或自定义评分平台；实施时只在冻结报告中保存输入、方法版本、结果和是否达到门槛。

2026-09-16 用户对此产品选择回答“是”，登记 R82：默认要求修正后的统计置信达到至少 95%；生成、Hyperopt 或比较的候选越多，越必须按全部试验修正选择偏差；未达到门槛、试验数量无法核对或方法不稳定时先判“证据不足”，不因单次最好结果直接通过，也不把统计置信当作盈利保证。

### 未来数据偏差判定方案（2026-09-16，推荐方案已确认）

来源事实：Freqtrade 2026.8 的 `lookahead-analysis` 先做完整基准回测，再对每个实际触发的入场和退出分别切片回测，比较信号与指标是否变化。`has_bias=Yes` 表示发现偏差，`No` 只表示已检查信号中没有发现；交易数量不足会明确测试失败。官方同时警告未触发信号可能造成假阴性，偏差入场可能连带造成退出误报，允许限价单可能误报，FreqAI 目标指标也可能被错误标记。官方文档和源码来源沿用本文件既有 `lookahead-analysis` 依据；本轮未运行命令。

推荐的最小三类判定：

1. 命令失败、交易数量不足、某类冻结信号未触发或结果无法绑定最终候选：证据不足；扩大时间范围或选择能覆盖该信号的未见数据后重跑，不能把 `No` 当作未触发信号也已通过。
2. 先使用官方默认的防误报设置；发现偏差时优先处理入场，再重跑退出。限价单、入场连带退出或 FreqAI 目标列等官方已知误报必须有可复核排除证据，否则仍是证据不足。
3. 排除已知误报后，确认任何实际入场、退出、止损、杠杆、仓位或风险决策依赖未来数据：不合格；没有确认偏差且所有冻结信号类型均有覆盖时，该项通过。不设置“少量未来数据偏差可以接受”的容差。

2026-09-16 用户对此产品选择回答“是”，登记 R83：先排除 Freqtrade 官方已知误报；排除误报后，确认影响实际交易或风险决策的未来数据偏差一律不合格，不设置可接受容差；命令失败、交易数量不足、冻结信号未触发或结果无法绑定最终候选时为证据不足，不能用未覆盖路径的 `No` 冒充通过。

R75-R83、证据、成本、路径、递归、统计及未来数据偏差方案写后证据：本轮补丁只更新本任务文件和 CURRENT_TASK 指针；Node assert 确认 R75-R83 条款各出现一次，没有新增 R84；方案复用 R18、R31-R39、R55、R57-R59、R72-R83 和 Freqtrade 原生边界，不建立总分、成交账本、固定成本倍数、测试矩阵、指标或统计平台；严格 UTF-8 文本、末尾换行、尾随空白、冲突标记和控制字符检查通过，git diff --check 通过。一次包含长外部 URL 的组合补丁在执行前被策略拦截，未写入文件；随后引用本文件已有官方来源并分段应用成功。资料阅读不等于命令已对项目候选运行；未运行 Freqtrade、业务测试、远程 CI 或独立验收，未提交或推送。

### 候选隔离试跑启动门槛方案（2026-09-16，推荐方案已确认）

来源事实：Freqtrade 2026.8 的配置最终值可能同时来自策略、多个配置文件、环境变量和命令行，优先级不同；官方建议使用 `show-config` 查看合并后的最终配置。同机多实例至少需要不同数据库，启用 REST API 时需要不同端口；`dry_run`、`dry_run_wallet`、`strategy`、`user_data_dir`、`db_url`、交易模式、品种范围和持仓限制都有原生配置入口。官方依据沿用本文件 Freqtrade 复用审计中的 Configuration、Advanced setup 和命令文档；本轮未运行命令。

对照三个方案：

| 方案 | 做法 | 问题 | 结论 |
| --- | --- | --- | --- |
| 只检查配置文件 | 启动前读取候选 `config.json` | 可能漏掉环境变量、附加配置和命令行覆盖，也不能证明最终运行对象 | 不采用 |
| 冻结试跑清单加原生预检 | 保存候选身份和授权摘要，用 `show-config`、策略加载结果及简单路径核对最终值 | 需要一份很薄的清单和启动前检查 | 推荐 |
| 新建启动与编排平台 | 独立服务管理清单、审批、启动和实例 | 重复 Freqtrade 和现有任务流，首版没有必要 | 不采用 |

推荐方案的四组最小启动条件：

1. 候选身份一致：冻结 Git 提交、策略文件及参数摘要、策略类名、Freqtrade 版本和验证证据身份完全对应；任何文件变化都按 R32、R80 重新形成候选。
2. 授权与最终配置一致：使用不显示敏感值的 `show-config` 结果核对 `dry_run=true`，交易所/市场类型、允许品种、最大杠杆、最大持仓数、模拟资金和有效期没有超出 R76；环境变量、附加配置或命令行覆盖也必须计入。
3. 隔离可证明：候选使用独立的用户数据目录、数据库、日志和模拟资金；启用 API 时端口及机器人名称独立。每个冻结候选使用新的数据库身份，旧数据库只读留档，避免跨版本试跑成绩混合；不得包含真实交易凭据或写入正式实例状态。
4. 前置证据完整：加载、R80 要求的候选相关路径、R81 递归影响、R83 未来数据偏差、历史/未见数据和 R79 成本检查达到预先固定的试跑门槛；R77 的隔离前向成绩不是启动该次试跑的前提。授权失效、已有未知候选实例、状态无法核对或任何身份冲突都失败关闭。

启动检查在 R69-R70 的固定授权范围内自动执行，不增加逐候选人工批准；它只允许开始隔离试跑，不等于 R57 的正式 Dry-run 版本批准。实施时复用原生命令和一份简单清单，不开发审批界面、数据库复制工具或通用编排平台。

2026-09-16 用户对此产品选择回答“是”，登记 R84：每个冻结候选使用一份最小试跑清单，绑定候选身份、最终生效配置、前置证据和隔离路径；系统在 R69-R70 的固定授权范围内自动核对，全部一致才允许启动，任何缺失、过期或冲突都失败关闭；不增加逐候选人工审批，也不把试跑启动等同于 R57 正式版本批准。

启动门槛方案写后证据：仍为 `codex/workbench-init` / `ba8738fe18289398a8c6713772416facddb5532d` / `20260912-002 ANALYZING`；R83-R84 表格行各唯一且没有新增 R85。严格 UTF-8、末尾换行、行尾空白、冲突标记、控制字符和 `git diff --check` 通过；工作区仍只有 CURRENT_TASK、本任务和第一期框架三份既有修改。本轮仅阅读官方资料和写分析文档，未运行 `show-config`、Freqtrade、业务测试、远程 CI 或独立验收，未提交或推送。

首版收缩审查决定：用户确认不再按当前方式继续增加 R 条款，先将 R01-R84 归入首版交付、首版验收、延期/历史三类，并用独立精简入口指导后续 READY 判断。原任务文件继续保存完整讨论和证据，不删除、降级或重编号；后续新增需求须先证明不能由现有契约、Freqtrade 原生能力或延期范围覆盖。

基线仍为 codex/workbench-init / ba8738fe18289398a8c6713772416facddb5532d / 20260912-002 ANALYZING。本轮只追加此对照并更新指针，不改 R01-R74、第一期框架、安全/发布政策，不运行、提交、推送或启动子代理。

## 2026-09-15 需求确认线路倒查与纠偏

本次唯一目标：按用户“从现在的进展往前推里查询当前需求确认线路是否偏离主线，如果有及时纠正”的要求，核对近期讨论与既有需求、第一期框架和第二期边界。控制塔只更新本任务文件和 CURRENT_TASK.md 的纠偏记录；保留已有未提交修改，不改正式 R 条款、第一期框架、产品代码、安全政策或稳定工作流，不提交或推送。

### 已确认事实与倒查顺序

| 从近到远 | 可核对事实 | 控制塔判断 |
| --- | --- | --- |
| 最近参数草案 | 控制塔提出 90 天/100 笔、30 天/30 笔、1.5 倍/2 倍成本、参数附近 ±10%；用户随后要求审查主线，尚未接受这组数字 | 缺少项目数据校准依据，不应作为推荐默认值继续请求批准 |
| 逐项过关原则 | 本对话中用户已确认：硬性失败不能靠其他收益抵消；无明确失败但检查不全为证据不足；全部必检项通过后才申请批准 | 与 R31、R37-R39、R57 一致，保留，不重复提问 |
| 最小验收表与外部调研 | 用户授权调查和提出方案；没有授权实际运行或安装；既有来源说明不等于选定统计指标 | 调研是依据收集，不应转化为无止境的通用量化评分设计或默认增加候选等待周期 |
| R01-R67 复用审计 | 原生能力直接复用，项目只设计不足部分；R39 保留具体阈值未定，R53-R59 固定核心版框架 | 审计方向正确，但归类为项目补充不等于每条都需要新模块或一次用户确认 |
| 第一期框架 | Freqtrade 完整交易基础，Codex 生产策略，项目只补验证编排、冻结批准、轮次监督和 Windows 通知 | 仍是当前基线，不能恢复第二套交易或评分平台 |
| 原始目的 | 减少情绪化扛单、追损和临时加杠杆，形成可追溯的策略研究、验证、模拟、反馈循环 | 主目标未变；讨论应服务于该闭环，而不是寻找通用的必赚策略及格线 |

证据入口：本对话最近的调研、验收表、用户确认、参数建议和纠偏请求；本文件 R31-R39、R53-R59、当前未决事项；[第一期框架](20260912-002-phase-1-framework.md)的目标、生产验证链、非目标和第二期边界。此次未重新遍历其他线程，也未重新验证外部技术资料；这是控制塔自查，不是独立验收。

### 立即纠正的推进口径

1. 撤回上述数字作为控制塔推荐默认值：仅保留这次曾提出但未确认的记录，不写成正式契约、强制等待期或后续开发参数。撤回的是建议，不是用户已确认的需求；样本充分性、完整成本、稳健性和重复筛选风险仍须检查。
2. 区分三件事：第二期完成设计和验收场景；后续获授权的工程任务实现并验证流程；具体候选再执行策略准入。不能要求第二期先跑满模拟观察期，也不能用工程测试通过代替候选合格。第二期仍不安装、不启动、不运行 Freqtrade。
3. 必须在第二期明确检查输入、规则来源、结果分类、证据和失败处理；无法有依据确定的阈值、统计方法或安全冲突继续作为明确未决项，阻塞受影响的准入规格。不能只写“以后校准”便宣称整个任务 READY，也不以此为理由先越权运行。
4. 不重复确认已经接受的原则；技术可查部分由控制塔查证，只把确实改变用户行为、权限、等待成本或风险边界的未决取舍交给用户，每次一个。技术方法不等于新增自定义评分系统。
5. 原生工具、R 条款和未来产品方向继续保留。安全治理只阻塞受冲突影响的实施，不用未来多策略、动态杠杆的全部细节阻塞当前不受影响的单策略流程设计；也不据此开放任何风险权限。

### 收口顺序与当前下一步

2026-09-15 按用户再次指定 Superpowers/Ponytail 进行增量复核：上轮撤回无依据默认数字仍然有效，但撤回数字不是取消阈值设计；用户要求控制塔提出有依据的方案本身没有偏离。偏离在于建议证据不足、反复确认同一原则，以及把尚未接通的流程留在后面。此次不新增总计划或第二套契约，仅明确以下待收口边界。

- 先核对 R34 的候选隔离试跑与 R56/第一期框架的单个 Freqtrade 进程限制。框架流程图没有明确展示隔离试跑的位置；还需说明旧策略运行期间是否试跑、如何隔离数据和资金，以及试跑授权怎样符合发布政策。当前不能直接把“正式运行前必需隔离试跑”删除，也不能默认启动第二个实例或认为研究区域天然免批准。属于首版流程未决，不是新需求。
- R55/框架先做正确性检查、再 Hyperopt 调参；R32/R57 又要求批准准确代码、参数和配置。控制塔据此指出证据绑定缺口：不能拿调参前版本的检查结果直接证明调参后版本全部通过。流程表须写清最终候选的检查覆盖和需重跑项目，不新增检查引擎，不擅自改写 R55 的原文。
- 先解决以上阶段、版本、授权边界，再收口准入阈值与证据充分性。软件流程达到可实施设计、具体候选拿到准入资格、用户发出运行批准是三个不同判断；不得循环要求“先正式运行才可获得正式运行资格”，也不得用工程测试代替独立策略证据。

这些结论来自本地 R34、R32、R55-R57、第一期框架和 RELEASE_POLICY 的交叉阅读，不是 Freqtrade 新功能结论或独立验收。本次没有追加市场调研、产品实现或用户审批步骤；原有正式 R 条款、未来方向和更严格安全规则保持不变。

接下来先将独立验证收口为一张端到端流程表：标准候选与冻结配置、隔离数据及规则输入 -> 原生检查和验证 -> 三类结论及证据 -> 冻结批准 -> 正式 Dry-run -> 反馈与复评。复用 R34、R55、R57 核对候选隔离试跑与正式运行的先后、批准和进程边界；有冲突就明确列出，不用新建议默默覆盖。该表要列出有限未决项，不再从样本数字逐个发散。

完成这一块后，按已有范围收口轮次监督、冻结批准与切换、Windows 运行恢复，再做跨模块场景和安全冲突核对。这些都是同一 ANALYZING 任务内的设计产物，不是新建任务或实施授权；无关键缺口、范围和验收完整且按工作流确认后，才可流转 READY。

基线：D:/CodexProjects/projects/quant-research-live-lab；codex/workbench-init；HEAD ba8738fe18289398a8c6713772416facddb5532d。写前仅两个目标文件已有修改；当前任务和 TASKS 均为 20260912-002 / ANALYZING，责任为 00｜项目控制塔。适用 AGENTS、共享/项目 WORKFLOW、PROJECT_STATE、CURRENT_TASK、TASKS、任务与框架已回读；migration 只有 README，没有交接记录。未 fetch 或查询实时远程。

本次同时消除 CURRENT_TASK.md 中“本次修订获准继续普通推送”的历史授权歧义，明确不延续到本轮。没有稳定工作流变更，不更新 CHANGELOG.md；未创建子代理、运行交易、读取账户凭据或改变环境。

写后证据：通过 Node 内置 assert 将新纠偏节移除后，与内存中的写前文档比较，原任务正文保持一致（仅规范换行后比较）；两文件所有既有 R 表格行逐条相同。严格 UTF-8、末尾换行、行尾空白、冲突标记、本地 Markdown 链接和 git diff --check 均通过；git diff --name-only 仍仅包含两个目标文件。未运行业务测试、Freqtrade、远程 CI、独立或跨模型验收；本轮检查的是讨论一致性和文档范围，不证明系统可用。

## 当前契约导航（2026-09-15）

本节只提供中文主题导航，不建立第二套契约编号。`Rxx` 条款及其用户确认依据是正式产品需求；导航不得修改、覆盖或降低这些条款的优先级。同一事项发生变化时，只有后续用户明确确认才能替代较早条款，替代关系必须保留。用户已确认但与现行安全规则冲突的条款仍是正式产品方向，只标记实施受阻，不视为降级或取消。

产品需求按“最新用户明确确认、较早用户确认、控制塔建议、Freqtrade 默认行为、外部经验”的顺序解释。实际实施仍须遵守现行 `AGENTS.md`、安全模型和发布政策；需求优先级不等于已经取得实施权限。

| 中文目录 | 正式契约入口 | 当前说明 |
| --- | --- | --- |
| 1. 系统边界 | R53-R59 | Freqtrade 是唯一交易基础；项目只补策略生产、独立验证编排、冻结批准和极薄轮次监督，不复制原生交易能力 |
| 2. 策略生命周期 | R31-R39、R55、R57-R58 | 标准 `IStrategy` 候选经过独立验证、冻结和每版本一次批准；运行版本不原地修改 |
| 3. 轮次生命周期 | R17-R28、R43-R52 | 四项输入、首次成交计时、目标/到期/主动结束、平仓核对和暂停规则 |
| 4. 安全权限 | R23-R24、R28、R30、R46-R48 | 当前继续执行固定亏损硬上限、失败关闭、硬停机和人工重新授权 |
| 5. 事实与恢复 | R36、R40-R42、R46-R47、R59 | Freqtrade 数据库是交易事实源；项目只保存最小轮次状态，恢复前必须核对 |
| 6. 用户操作 | R17、R22、R28、R42、R48、R50-R52、R57-R58 | 用户创建轮次、批准冻结版本及处理硬停机；普通交易动作自动执行，通知不构成批准 |
| 7. 未来演进 | R29、R49、R60-R67 | 多策略、多持仓和动态杠杆是已确认产品方向，但在安全治理完成前不得实施 |

Freqtrade 已有的行情、交易对过滤、订单类型、未成交超时、部分成交、减仓、紧急退出和界面行为默认引用锁定版本的原生能力，不重复写成项目自研契约。只有实际证据证明原生能力不能满足已确认需求时，才登记最小项目缺口。

### R01-R67 Freqtrade 复用审计（2026-09-15）

本表落实用户确认的取舍原则：Freqtrade 已有且满足需求的能力直接复用；已有但需要选择参数或外围编排的，只保留配置或项目差异；原生没有的才做最小补充；与现行安全规则冲突的单列治理。此处不改变任何 R 条款的产品优先级，也不建立第二套契约编号。

归类含义：`已替代` 表示后续用户确认的 R 条款已经取代旧口径，旧条款仍保留为决策历史；`直接复用` 表示不设计同类项目能力；`Freqtrade 配置` 表示用原生配置、策略回调或标准命令实现；`项目补充` 表示确有原生未覆盖的薄层能力；`安全治理` 表示产品方向已确认，但现行规则禁止实施。混合条款按实际边界并列标注。

本次只依据 Freqtrade 官方仓库 `2026.8` 标签核对，不用社区文章替代产品事实。官方依据包括配置、策略回调、交易对列表、REST API、多实例、回测、Hyperopt、前视偏差、递归偏差、保护和 Producer/Consumer 文档；具体链接列于本节末尾。文档事实不等于本项目已经安装或运行验证。

| 契约 | 归类 | Freqtrade 2026.8 对照 | 保留的项目差异或处理 |
| --- | --- | --- | --- |
| R01 | 已替代 | 原生不提供本项目候选版本自动晋级审批 | 以 R57-R58 的每冻结版本一次人工批准和批准后自动切换为准 |
| R02 | 已替代 | 原生数据库和 REST 状态可判断旧仓是否结束 | 正常切换以 R58 为准；失格和平仓分别按 R35、R23 |
| R03 | 安全治理 | 原生可配置 stake、仓位调整和保护，但没有本项目风险额度恢复语义 | 不得解释为修改 R24 固定亏损硬上限；其他额度恢复须以后单独定义并符合禁止自动扩大风险规则 |
| R04 | 直接复用＋项目补充 | `/pause`、`/forceexit all`、订单状态和锁可执行停开与平仓 | 本轮累计净亏损触限和跨重启硬停机锁由最小轮次监督补充 |
| R05 | 项目补充 | 原生启动或解锁接口不等于本项目重新授权 | 硬停机后的明确用户授权、授权记录和失败关闭由项目保存 |
| R06 | 项目补充 | 交易实例暂停不提供 Codex 研究任务生命周期 | 研究与交易停机解耦，由研究编排处理 |
| R07 | 项目补充 | 原生消息能力不等于 Windows 系统提醒 | 只补 Windows 原生通知适配 |
| R08 | 项目补充 | 原生通知没有本项目授权语义 | 明确通知只告知状态，不能批准启动、解锁或扩大风险 |
| R09 | 项目补充 | Freqtrade 可独立进程运行，但不定义本项目桌面窗口生命周期 | Windows 宿主负责关窗后后台继续和显式完全退出 |
| R10 | 项目补充 | SQLite 可恢复交易事实，原生状态接口可核对运行状态 | Windows 自启、恢复编排和不得误解除项目停机锁由薄层补充 |
| R11 | Freqtrade 配置＋项目补充 | 动态 PairList、交易所市场信息和黑名单可生成候选交易对 | 币安全部可交易 USDT 本位永续只进入研究池，不等于全部可下单 |
| R12 | Freqtrade 配置＋项目补充 | Age、Delist、Precision、Price、Spread、Volume、Volatility 等过滤器可处理基础准入 | 新品种的数据充分性和独立策略验证仍由项目补充 |
| R13 | 安全治理 | `max_open_trades`、动态 stake 和余额约束已有；参数不会替项目决定可承受风险 | 用户不逐笔决定仓位可沿用原生，但系统自动扩大持仓数与现行单仓规则冲突，不能实施 |
| R14 | 已替代 | 原生报告可继续展示 ROI/收益率 | 核心目标已改为 R17-R22 的本轮净盈利金额；是否展示 ROI 以后只属界面选择 |
| R15 | 已替代 | 原生没有本项目固定盈利轮次时长 | 固定 2 小时已被 R17、R22、R25-R27、R52 的用户可填有效时长替代 |
| R16 | 已替代 | 原生 ROI 配置不是本项目轮次目标 | 用户输入目标已改为净盈利金额，按 R17-R22 执行 |
| R17 | Freqtrade 配置＋项目补充 | `dry_run_wallet` 可设置模拟本金 | 盈利金额、有效时长、最大净亏损和一轮生命周期由项目薄层补充 |
| R18 | 直接复用＋项目补充 | 交易利润计算和回测包含手续费，期货配置支持资金费 | 以整轮为单位聚合净盈利及明确费用口径由项目定义，不复制成交账本 |
| R19 | 直接复用＋项目补充 | Freqtrade 提供未实现盈亏、利润、余额、持仓和 `/forceexit` | “预计全部平仓后达标”触发、可靠快照和结算后二次判定由轮次监督补充 |
| R20 | 项目补充 | 原生没有“目标平仓后不足则在剩余授权内继续”的轮次语义 | 只定义继续条件，不改 Freqtrade 进出场机制 |
| R21 | 直接复用＋项目补充 | `/pause`、`/forceexit all`、状态与利润接口可执行动作 | 到期判定、平仓核对、持续暂停和 Windows 通知由项目编排 |
| R22 | 项目补充 | 原生配置重载不提供本项目目标和有效时长的原子修改语义 | 只保存两项轮次参数及 R50-R52 的生效规则 |
| R23 | 直接复用＋项目补充 | 原生利润、余额、持仓和退出接口提供事实与动作；Protections 主要基于已结束交易并临时锁单 | 包含浮亏和预计退出成本的整轮硬亏损、全平、跨重启永久锁必须由项目补充 |
| R24 | 项目补充 | 原生允许改配置，但不会强制本项目轮次亏损授权不可变 | 轮次开始前冻结硬上限，运行中拒绝用户和系统修改 |
| R25 | 项目补充 | `order_filled()` 和数据库可提供首次成交事实 | 有效计时从首次成交开始及普通空仓继续计时由项目保存 |
| R26 | 项目补充 | 原生 `/health`、交易事实恢复和市场数据可辅助核对 | 关机、行情不可靠时暂停计时、禁止新开仓及恢复顺序由项目定义 |
| R27 | 项目补充 | 原生可暂停入场并继续由外部运行研究命令 | “无合格策略且空仓”暂停计时和合格候选出现后的条件恢复由项目编排 |
| R28 | 项目补充 | 原生实例可启动，但不知道本项目轮次已终止或硬锁原因 | 项目停机锁优先，候选、重启或 Codex 恢复不得自动解锁 |
| R29 | 安全治理 | 单实例可配置多仓；多实例必须分离数据库、端口和配置，原生没有跨实例共享轮次预算 | 多策略共用本金、目标和总风险需独立治理；现行单仓规则下不得实施 |
| R30 | 直接复用＋项目补充 | `IStrategy` 信号、`custom_stake_amount()`、`confirm_trade_entry()`、Protections 和退出机制可复用 | 独立于生成器的准入和整轮硬风险最终否决由项目闸门补充 |
| R31 | 项目补充 | Freqtrade 不提供生成器与验证器权限隔离 | 标准、结果和资格由独立验证流程控制，生成模块不可修改 |
| R32 | Freqtrade 配置＋项目补充 | 策略类和参数可按文件、配置加载，原生不要求运行版本不可变 | 代码或参数变化必须形成新冻结候选并重新验证、批准 |
| R33 | 项目补充 | Producer/Consumer 可复用已分析数据，但不提供 Codex 候选生成闭环 | 只补数据反馈、候选生成、独立验证和批准编排，不复制行情分析 |
| R34 | Freqtrade 配置＋项目补充 | 多 Dry-run 实例可用独立数据库、端口和配置隔离 | 候选试跑不占正式轮次资金、不计目标及其结果晋级规则由项目补充 |
| R35 | 直接复用＋项目补充 | 原生信号退出、force exit、pair lock 和持仓状态可执行退场 | “策略失格”的判定、只停止该策略及不误伤其他策略属于项目验证和未来多策略治理 |
| R36 | 项目补充 | 原生可导出回测、Hyperopt 和交易结果，但不保存本项目完整候选决策链 | 版本、数据范围、固定标准、结果和失败原因形成项目证据包 |
| R37 | 直接复用＋项目补充 | lookahead、recursive、backtesting、Hyperopt 和原生费用模型直接复用 | 开发/留出隔离、证据充分性、压力场景及 Dry-run 一致性由独立验证编排补充 |
| R38 | 项目补充 | 原生命令返回各自结果，但不提供统一的合格、不合格、证据不足资格状态 | 项目只做结果归并和资格状态，不重算原生指标 |
| R39 | 直接复用＋项目补充 | 原生报告、指标和 `MultiMetricHyperOptLoss` 可复用 | 评估前冻结阈值、不得因无人通过而降标及用项目数据校准属于治理 |
| R40 | 项目补充 | Freqtrade 运行策略和执行不依赖 Codex | Codex 只用于研究候选；额度中断不得影响已批准交易与风控 |
| R41 | 项目补充 | Freqtrade 不负责第三方 Codex API 额度和恢复 | 保存研究进度、暂停依赖任务、探测恢复和通知；不得读取或保存真实凭据到仓库 |
| R42 | 项目补充 | 原生 REST、消息和独立进程可作为基础 | Windows 通知、后台、自启、完全退出和恢复核对属于宿主适配 |
| R43 | 项目补充 | 单实例本身可限制并发，但没有“同一时间只能有一轮正式模拟”的业务对象 | 项目只保存唯一活动轮次标识和状态 |
| R44 | Freqtrade 配置＋项目补充 | `dry_run_wallet`、`available_capital` 和 stake 配置可限制可用资金 | 轮次启动后本金不可追加或取出是项目冻结规则 |
| R45 | 项目补充 | 原生记录订单和成交时间，不计算本项目有效时长 | 平仓核对期间是否继续消耗时长按 R45 由轮次时钟处理 |
| R46 | 直接复用＋项目补充 | SQLite 恢复持仓和订单事实，REST 状态及 force exit 支持继续处理 | 重启后先锁新仓、对账、只处理剩余仓位及未知状态失败关闭由项目恢复编排 |
| R47 | 直接复用＋项目补充 | 原生数据库提供最新可靠交易事实 | 按最新可靠快照重算、保留已触发锁及不追认离线越线是项目恢复规则 |
| R48 | 直接复用＋项目补充 | `/pause`、`/forceexit all` 和状态查询执行结束动作 | 一次“结束本轮”的业务语义、核对后持续暂停和不追补目标由项目补充 |
| R49 | Freqtrade 配置＋安全治理 | 单个 Freqtrade 实例原生不为同一交易对同时开第二笔仓位 | 跨策略或多实例的同交易对所有权需以后协调；首版单策略单实例无需自建锁 |
| R50 | 项目补充 | 原生不知道本项目 `CLOSING`、`RECONCILING` 轮次阶段 | 这两个阶段冻结目标和时长，修改不排队 |
| R51 | 项目补充 | 原生配置重载不保证两个轮次参数的原子事务 | 项目以一次状态更新和同一可靠快照重算，失败则全部不生效 |
| R52 | 项目补充 | 原生没有本项目有效时长定义 | 新总时长从首次成交起计算，不重置已消耗时间 |
| R53 | 直接复用 | 官方 2026.8 已覆盖行情、PairList、IStrategy、回测、Hyperopt、分析命令、Dry-run、订单、持仓、余额、SQLite、Protections、REST API 和 FreqUI | 后续实施只做版本锁定和真实运行验证，不复制能力 |
| R54 | 项目补充 | 原生边界之外确实没有 Codex 生产、独立资格、冻结批准、盈利轮次和 Windows 生命周期 | 保持极薄补充，不恢复旧重控制层 |
| R55 | 直接复用＋项目补充 | `IStrategy`、策略加载、lookahead、recursive、Hyperopt、backtest 和报告均直接复用 | 固定开发区间、独立留出、执行顺序和证据归档由验证编排补充 |
| R56 | Freqtrade 配置 | 一个策略、一个 Dry-run 进程、1 倍杠杆和 `max_open_trades=1` 可由原生配置实现 | 仅需在实施时证明有效配置与冻结清单一致 |
| R57 | 项目补充 | 原生可加载策略和配置，但不提供本项目冻结证据批准 | 生成发布清单，由最小 Web 管理台展示报告并记录每版本一次人工批准；不建设通用审批平台 |
| R58 | 直接复用＋项目补充 | 数据库、状态、`/start`、`/pause` 和持仓查询可执行切换 | 批准绑定、等待旧仓、核对、失败保持暂停及不回滚旧策略由项目编排 |
| R59 | 直接复用＋项目补充 | Freqtrade SQLite 是订单、成交、持仓和利润事实源，并支持重启恢复 | 项目只持久化最小轮次、批准、计时和停机锁，不建第二账本 |
| R60 | Freqtrade 配置＋安全治理 | 期货 `leverage()` 回调可按交易对和行情返回 1 到交易所上限的杠杆 | 动态杠杆方向已确认，但当前固定 1 倍规则禁止实施；公式必须冻结且不得受用户情绪干预 |
| R61 | Freqtrade 配置＋安全治理 | 原生提供 `proposed_leverage` 和 `max_leverage`，并会截断超交易所上限的返回值 | 项目品种硬上限和账户剩余风险取最小值需安全治理后补充 |
| R62 | 项目补充＋安全治理 | 原生有交易所上限、`liquidation_buffer`、Volatility/Volume/Spread 等输入能力 | 按品种独立验证出的杠杆和敞口上限模型不是原生能力，且当前不得实施 |
| R63 | Freqtrade 配置＋项目补充＋安全治理 | `leverage()` 可随当前行情输出，但原生不管理本项目冻结公式版本 | 运行时只允许同公式降险；公式、参数或上限变更必须新版本，自动再提高仍受现行禁令阻塞 |
| R64 | Freqtrade 配置＋项目补充＋安全治理 | `adjust_trade_position()` 支持负 stake 部分退出和全额退出，也明确不能原地修改既有仓位杠杆 | 超限判定、禁止加仓、减到同时满足敞口与爆仓距离由独立风险层决定；当前动态仓位治理未获实施权限 |
| R65 | 直接复用＋项目补充＋安全治理 | 原生部分退出、force exit、emergency exit、订单状态和失败信息可执行动作 | 硬风险触发、有限处理后升级、全平核对、停机锁和通知由项目补充 |
| R66 | 直接复用＋Freqtrade 配置 | 原生 exit order type、`unfilledtimeout`、自定义超时、重复退出和 `emergency_exit` 已覆盖机制 | 项目只冻结参数和选择硬风险直接紧急退出，不建第二套等待或交易所适配 |
| R67 | Freqtrade 配置＋项目补充＋安全治理 | PairList 及 Age、Delist、Precision、Price、Spread、Volume、Volatility 过滤器直接复用 | 按计划仓位的退出压力测试、数据新鲜度硬门槛和扩大交易品种范围需项目验证及安全治理 |

#### 审计后不再重复询问的事项

- 订单类型、部分成交、退出订单超时、取消重挂、减仓、全平和紧急退出：以 R66 及 Freqtrade 原生配置、回调和 REST 状态为准。
- 单个实例的持仓槽位、同一交易对不重复开仓、动态 stake、Dry-run 本金和数据库：以原生配置与 SQLite 为准；只有跨实例共享总预算仍是项目问题。
- 可交易市场发现、上下架、精度、价差、成交量和波动率基础过滤：以 PairList 链为准；项目只补独立验证、退出压力和数据新鲜度。
- 回测、Hyperopt、lookahead-analysis、recursive-analysis、标准报告和 FreqUI：直接复用，不再讨论是否自研同类工具。
- 启停、暂停新入场、查询持仓/利润/余额、force exit 和健康状态：复用 REST API，不再设计第二套交易控制接口。

#### 审计后仍需第二期决定的项目问题

以下才是后续 brainstorming 的有效问题；它们必须一次只处理一个，不能要求用户猜 Freqtrade 已经回答的技术细节。

1. 独立验证：留出数据、最低证据量、成本和压力场景、三类结果阈值，以及运行策略何时判定失格。
2. 轮次监督：净盈利与最大净亏损的精确口径、可靠快照、计时暂停、事件优先级和恢复原子性。
3. 冻结批准：代码、参数、配置、交易对、风险和证据怎样形成一个可核对版本，怎样映射到 Freqtrade 启动或切换。
4. Windows 生命周期：后台、自启、通知、完全退出和故障恢复的最小可验收行为。
5. 独立安全治理：多持仓、多策略共享风险、动态杠杆和扩大品种范围怎样修改现行规则、测试、失败分析和回滚；完成前保持 1 倍和最多一个仓位。

#### 官方证据入口

- Configuration: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/configuration.md
- Strategy callbacks: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/strategy-callbacks.md
- Pairlists: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/includes/pairlists.md
- REST API: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/rest-api.md
- Advanced setup: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/advanced-setup.md
- Backtesting: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/backtesting.md
- Hyperopt: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/hyperopt.md
- Lookahead analysis: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/lookahead-analysis.md
- Recursive analysis: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/recursive-analysis.md
- Protections: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/includes/protections.md
- Producer/Consumer mode: https://github.com/freqtrade/freqtrade/blob/2026.8/docs/producer-consumer.md

本次首次尝试通过 PowerShell 直接传递包含较长官方 URL 的大补丁时，命令在创建进程前被策略拒绝，未写入文件；随后仍使用 Codex `apply_patch` 后端分段写入成功。一次严格 UTF-8 组合检查因 PowerShell 空管道语法错误退出 1，未修改文件；改为先收集结果再输出后重跑通过。一次差异白名单断言又因字符串连接表达式优先级写错，在实际只列出两个目标文件时误报不匹配；改用 `Compare-Object` 后重跑。上述失败均不属于 Freqtrade 或仓库错误。

### 本次整理授权与边界

用户逐项确认六个现行主题和未来演进主题后，确认只保留 `Rxx` 正式编号及中文导航，并要求按计划执行。据此，本次只修改本任务文件和 `CURRENT_TASK.md`；不改第一期框架、任务状态、项目状态、稳定工作流、安全规则、发布政策、产品代码、依赖、运行环境或凭据，也不启动子代理、交易服务或 Freqtrade，不提交或推送。

写入前核对：唯一目录 `D:/CodexProjects/projects/quant-research-live-lab`，分支 `codex/workbench-init`，HEAD `ba8738fe18289398a8c6713772416facddb5532d`，工作区干净，本地跟踪 `origin/codex/workbench-init`。本轮未执行 fetch 或远程 `ls-remote`，因此不把本地跟踪引用冒充实时远程核对。

两次 `apply_patch` 调用分别因 UTF-8 参数传递和补丁末行解析失败，均未写入文件；两次包含过长或外部 URL 上下文的组合命令在执行前被策略拒绝，也未产生写入。随后改为通过同一 Codex 补丁入口的 .NET `ArgumentList` 分段传递补丁并成功，没有使用覆盖式写文件或破坏性 Git 命令。

写后检查观察到：`git diff --check` 退出 0；两个目标文件通过严格 UTF-8、末尾换行、行尾空白、冲突标记和本地 Markdown 链接检查；R01-R67 与 E01-E34 均连续、唯一；任务文件、当前任务和任务索引的状态仍为 `ANALYZING`，责任线程仍为 00｜项目控制塔。分支和 HEAD 未变，差异路径只有本任务文件和 `CURRENT_TASK.md`。本轮是文档整理，未运行 pytest、ruff、Freqtrade、Dry-run、Windows 通知、远程 CI 或独立验收。

## 2026-09-14 Freqtrade 核心版框架修订

### 当前结论

用户确认用修订后的 [第一期系统框架设计](20260912-002-phase-1-framework.md) 替代此前的重控制层方案。Freqtrade 负责行情、交易对、标准策略运行、回测、Hyperopt、正确性分析、Dry-run、订单、持仓、余额、交易数据库、原生风控、REST API 和 FreqUI；项目不复制这些能力，只补 Codex 策略生产、独立验证编排、冻结批准及本轮目标、有效时长、最大净亏损、永久停机锁和 Windows 通知。

首版只证明一个标准 Freqtrade 策略从生成、原生验证、留出验证、冻结、人工批准到 Dry-run 的闭环。每个新策略版本批准一次，批准绑定准确代码、参数、配置、交易对、风险设置和证据，同时构成安全条件满足后的启动或切换授权；旧持仓结束并核对后可自动切换，新版本加载或启动失败时保持暂停并通知，不恢复旧策略。

首版不建设完整控制层、重复订单/资金/交易账本、策略意图路由器、自定义评分系统、自定义订单/持仓交易界面、通用审批平台、微服务、消息队列、FreqAI、多策略并发或实盘适配；只提供已确认的本机单页 Web。PR #7 与现有 `gates.py`、`release.py` 仅是待重新审查的复用候选，不能把未解决审查意见或静态文件存在写成已验收能力。

事实：现行项目规则仍要求杠杆 1、最多一个仓位、不得自动扩大风险，并要求冻结制品和人工批准。本次修订不修改这些规则。未来多持仓、多策略并发或取消每版本人工批准必须单独治理。

控制塔结论：用户已复核并确认修订框架，第一期设计完成。整个任务仍为 `ANALYZING`，不达到 `READY`，不授权安装 Freqtrade、运行 Dry-run、修改产品代码或进入实盘。

### 本次授权与边界

用户先确认修订框架，随后确认第一期内容，并明确授权更新相关项目文档并同步远程 Git。本次允许保留本任务文件中已有 R50-R52 修改，更新第一期框架、本任务文件和 `CURRENT_TASK.md`，提交并普通推送到 `origin/codex/workbench-init`。不更新 `main`，不强推，不创建或合并 PR，不修改 `TASKS.md`、`PROJECT_STATE.md`、产品代码、安全规则、发布政策、运行环境或凭据。

写入前执行 `git fetch origin` 成功；本地 HEAD 与 `origin/codex/workbench-init` 均为 `3b0dbbed1174d5ad28ced7aae22cccd5269c7698`，领先/落后为 `0 0`。工作区只有本任务文件的既有 R50-R52 修改；本次在其基础上继续编辑，不回退或覆盖。

本次目标文件限定为修订框架、本任务文件和 `CURRENT_TASK.md`。没有修改稳定工作流规则，因此不更新 `CHANGELOG.md`；没有修改任务状态，因此 `TASKS.md` 保持 `ANALYZING`。两次组合补丁因同一文件重复操作和长行上下文不匹配而失败：第一次没有改变目标文件，第二次没有改变入口文件；随后拆分为可核对的小补丁并成功，失败没有被省略。

写后检查：`git diff --check` 退出 0；严格 UTF-8、末尾换行、行尾空白、冲突标记、代码围栏和三个目标文档的本地 Markdown 链接检查均通过。任务表中的 R01-R59 和近期来源 E11-E28 连续且无重复，既有 R50-R52 仍完整；`CURRENT_TASK.md` 与 `TASKS.md` 均保持 `ANALYZING`。最终差异白名单恰为三个获准文档，定向私钥、API key、secret 和 token 赋值模式无匹配；这不是全面安全审计。

本次只整理设计文档，因此未运行 pytest、ruff、Freqtrade 命令、Dry-run、Windows 通知、远程 CI 或独立验收；没有安装依赖、启动服务、连接交易所、读取凭据、修改代码或创建子代理。这些跳过项不能被解释为业务检查通过。

### Freqtrade 核心版框架 Git 推送回执

修订框架和任务入口提交为 `b6b614aaa92ee70acbf3cd96a01e7d95b2216179`，提交说明为 `docs: adopt Freqtrade core framework`，只包含三个获准文档，统计为 217 行新增、155 行删除。提交后再次执行 `git fetch origin`；远程工作分支仍指向父提交 `3b0dbbed1174d5ad28ced7aae22cccd5269c7698`，与本次提交父节点一致，因此没有远程分叉。

普通执行 `git push origin HEAD:refs/heads/codex/workbench-init` 成功，远程从 `3b0dbbed1174d5ad28ced7aae22cccd5269c7698` 前进到 `b6b614aaa92ee70acbf3cd96a01e7d95b2216179`。没有强推、更新 `main`、创建或合并 PR、打标签、发布、部署或启动交易服务。

本回执在上述结果确认后单独提交。回执提交自身不预填未来 SHA；可用 `git log -1 --format=%H -- docs/codex/tasks/20260912-002-system-requirements.md` 定位，再与 `git ls-remote origin refs/heads/codex/workbench-init` 核对最终远程。远程持久化只证明第一期设计已保存，不使整个任务达到 `READY`，也不授权业务实施或实盘。

回执提交为 `e3a1b094a4de0b750b22ca656b9f8a240d2de2e1` 并已普通推送。首次最终核对已观察到本地、`ls-remote` 和跟踪分支均为该 SHA、领先/落后原始输出为 `0<TAB>0`、工作区条目为 0，但脚本误将带制表符的单字符串与空格字符串比较，随后抛出 `divergence remains`；这是断言解析错误，没有修改仓库或远程，不能省略为成功。

改为按空白拆分并逐列数值比较后重跑通过：本地、远程和跟踪分支均为 `e3a1b094a4de0b750b22ca656b9f8a240d2de2e1`，ahead 0、behind 0、工作区条目 0，`git diff --check` 退出 0。该结果发生在补写本段前；本段继续使用独立普通提交保存，不改写已推送历史。

## 2026-09-14 旧第一期框架确认（历史）

### 当时结论

用户当时确认第一期只固定系统框架，评分公式、算法、字段、阈值和验收场景放在第二期。该版本后来被上方 Freqtrade 核心版修订替代；以下文字只保留当时的决定和 Git 证据，不再是当前框架入口。

框架固定了以下边界：项目控制层统一管理轮次、策略资格、资金、风险、账本和恢复；Freqtrade 是唯一回测、Dry-run 和执行底座；Codex 只研究候选；策略只提交交易意图，控制层可以批准、缩小或拒绝，但不能放大或改变方向；持仓版本冻结到平仓核对完成；同一合约只归属一个策略；临时问题可在核对通过后自动恢复，本轮结束不能自动重启；第一版只做 Dry-run。

事实：现行项目规则仍禁止自动增加资本、杠杆、持仓数、合约范围或亏损上限，安全模型仍规定杠杆为 1 且最多一个仓位，发布政策仍要求人工批准和冻结发布身份。本次没有修改这些规则。

冲突：框架目标允许用户预设边界内的多合约并行和模拟候选自动晋级，但当前安全与发布规则尚不允许直接实施。第二期必须提出兼容方案或保持更严格现状，不能用设计文档绕过规则。

历史结论：用户当时复核并确认书面稿。其重控制层架构已被上方修订替代；任务状态始终保持 `ANALYZING`。

### 当时第二期边界

第二期讨论策略评分、资金与仓位计算、信号时效、事件优先级、账本字段、接口错误语义、核对与重试、通知和界面细节、规则冲突处理及验收场景。未经第二期确认，不拆实施任务，不创建实施或验收线程。

### 当时授权与记录

用户先授权控制塔整理第一期框架稿并更新任务入口，随后确认第一期并要求按 Git 治理方案提交到远程。本次只新增框架稿并修改本文件和 CURRENT_TASK.md；保留此前未提交修改，不更新 TASKS.md 或 PROJECT_STATE.md。允许提交并普通推送到 `origin/codex/workbench-init`；不直接更新 main，不创建或合并 PR，不打标签、不部署，不修改代码、安全规则、发布政策或运行环境。

提交前远程核对：2026-09-14 执行 `git fetch origin --prune` 成功；本地 HEAD 与 `origin/codex/workbench-init` 均为 `eb1c62afdf4e3df10565e9841a4e3f33b5786199`，领先/落后为 0/0。远程没有需要先合并的提交，禁止强推仍然适用。

写入前核对：唯一目录 D:/CodexProjects/projects/quant-research-live-lab，分支 codex/workbench-init，HEAD eb1c62afdf4e3df10565e9841a4e3f33b5786199；工作区已有 CURRENT_TASK.md 和本任务文件两处未提交修改。共享与项目工作流、项目状态、适用 AGENTS、现行架构、安全模型、发布政策及 migration 说明已回读；migration 仍无正式交接记录。

失败记录：PowerShell 管道调用 apply_patch 两次因补丁参数不是 UTF-8 而失败；通过 apply_patch.bat 传递多行参数两次因换行被破坏而失败；WSL Bash 不存在；一次过大的任务文件补丁被执行策略拒绝。这些尝试均未写入对应目标文件。随后核对包装脚本，改为直接调用同一 Codex 补丁入口并通过 .NET ArgumentList 传递单个多行参数，分段写入成功。

写后检查：三个目标文档均通过严格 UTF-8、末尾换行、行尾空白和冲突标记检查；本地 Markdown 链接全部可解析；框架稿包含目标、模块、数据、状态、权限、冲突、第二期和完成标准，且无 TBD、TODO 或“待补充”占位。git diff --check 退出 0；HEAD 和分支未变；最终改动对象仅为 CURRENT_TASK.md、本任务文件和新增框架稿。定向敏感信息模式搜索无匹配，rg 因零匹配返回 1，按工具语义记录为未发现匹配，不冒充全面安全审计。

### 第一期框架 Git 推送回执（2026-09-14）

第一期框架及任务入口提交为 `6e36d253d582a5a74b43e13b098c19bbff00892b`，提交说明为 `docs: confirm phase one system framework`，只包含 CURRENT_TASK.md、本任务文件和新增框架稿。普通执行 `git push origin codex/workbench-init` 成功，远程从 `eb1c62afdf4e3df10565e9841a4e3f33b5786199` 前进到该提交。

推送后执行 `git ls-remote origin refs/heads/codex/workbench-init`，远程返回与本地 HEAD 相同的完整 SHA；`git rev-list --left-right --count HEAD...origin/codex/workbench-init` 为 `0 0`，工作区当时干净。没有强推、直接更新 main、创建或合并 PR、打标签、发布、部署或启动交易服务。

本回执随后作为独立文档提交再次普通推送。回执提交自身不预填未来 SHA；后续可用 `git log -1 --format=%H -- docs/codex/tasks/20260912-002-system-requirements.md` 定位，再与 `git ls-remote` 核对最终工作分支。远程持久化只证明文档已保存，不使任务达到 READY，也不授权实现或实盘。

## 2026-09-14 旧总路线与 Freqtrade 对照（历史输入）

以下内容记录第一期框架确认前的七阶段路线、静态源码对照和 PR #7 审查证据。事实证据继续有效，但其中“修复 PR #7 后进入下一阶段”的顺序已被上方框架确认取代，不得作为当前实施授权。

### 本次决定、范围与完成条件

用户要求重新设计总计划后，对七阶段路线及“整理进现有任务文件和 CURRENT_TASK.md，不含代码实施或 Git 推送”的询问回答“确认”（E11）。本次唯一目标是将该路线和近期确认可靠落库；只修改这两个文档。旧路线与历次 Git 回执保留为历史，不延续其中的提交、推送授权。TASKS.md、PROJECT_STATE.md、产品、安全与发布规则保持不变；没有稳定工作流变更，不改 CHANGELOG.md。

完成条件：当前入口能找到新路线、下一步和有效契约；近期确认与待确认项分开；旧证据保留；状态和责任一致；差异仅含两个授权文件且文档结构、链接、空白检查通过。这里只验收资料整理，不宣布需求任务 READY、产品可用或策略盈利。

### 已确认的七阶段路线

以 Freqtrade 为优先基础，先验证适配，再逐段交付完整模拟流程，最后接入自动研究和多策略。改的是顺序，不取消已有需求。优先复用现成回测、Dry-run、订单查询与控制接口及已有界面；复用并验证项目 gates.py 和 release.py。只有证据表明现有能力不足，才补本轮目标、计时、核算、持久化停机锁、候选准入及共享风险预算，不预建另一套平台或服务。

| 阶段 | 主要目标 | 可复核通过条件，当前均未通过完整阶段验收 |
| --- | --- | --- |
| 1. 复用与差异分析 | 对照已确认需求、Freqtrade 指定版本和 PR #7，分类直接使用、配置解决、需要补充、存在阻塞 | 每条关键需求都有依据、验证方法；影响架构的障碍及处理路径明确 |
| 2. 修复并验收现有适配 | 在另行授权的任务内处理适配缺陷，建立受控 Dry-run 基础 | 实际加载制品被篡改、实际开仓金额超额、风控状态不明均被拒绝；模拟开平仓记录可核对 |
| 3. 完成一轮模拟运行 | 四项输入、首次成交计时、净盈亏核算、自动停止、主动结束、重启恢复 | 达标后扣费不足、到期、触限、部分平仓重启均可复算；不重复记账、不误解锁 |
| 4. 建立独立策略准入 | 复用分析工具，补齐数据隔离、成本、风险、证据及版本管理 | 每个候选得到合格、不合格或证据不足；生成方不能修改标准和验证结果 |
| 5. 接入 Codex 研究循环 | 运行数据反馈、生成候选、独立验证、合格晋级、继续反馈 | 候选及失败过程可追溯；额度中断保存进度，恢复研究不解除交易停机锁 |
| 6. 扩展多策略与研究范围 | 多策略共用总资金和总风险，覆盖约定的全部合约研究候选池 | 不重复分配本金，不混淆持仓归属；失格退出不误伤其他策略，数据不足品种不能入场 |
| 7. Windows 日常使用与整体验收 | 完善后台运行、原生提醒、开机核对、退出和恢复体验 | 在实际 Windows 环境核对正常使用及故障场景，留下独立、可复核的整体验收记录 |

这些是里程碑，不是七个已登记任务或七个并行线程。单策略只是中间验证步骤；Codex、多策略及全部约定合约候选池仍属于这版模拟产品。最小操作和观察入口随模拟流程提供，第 7 阶段完善日常体验，不等到最后才允许操作。工程测试策略须与正式候选隔离；没有合格策略时正确等待是合法状态，不能为推进计划降低标准。软件正确性不等于盈利证明。

### 第 1 阶段必须提前核对

- 多策略、同一合约单一持仓归属及共享预算能否由现有能力承载；不能等到第 6 阶段才发现基础限制。
- 暂停和结束状态能否跨重启可靠保留；不能把单个暂停接口当作持久化停机锁。
- 成交、手续费、资金费、浮动盈亏和预计平仓成本的数据来源是否充分、可对账且不重复计入。
- 现有 Codex 的任务提交、结果收集、额度中断及恢复路径是否可集成；桌面版可用不证明系统能自动调用。

下一项分析产物至少包含：需求编号、Freqtrade/现有项目能力、适用版本及源码或文档依据、复用分类、缺口、核验方法、尚未解决的取舍。技术资料先核对指定版本；若需要运行探针、依赖、服务或环境变更，另行确认范围，不把路线批准当运行授权。发现硬性不兼容先列阻塞及最小替代方案，不悄悄削弱需求或扩建平台。

现行单持仓、禁止自动扩大风险及人工发布要求仍有效。自动模拟晋级、多策略等冲突必须在受影响实施前审阅解决；无法安全解决就停止该项。每个后续任务沿用既有流程：分析明确范围，达到 READY 后由单一实施者修改、自测，再独立验收，由控制塔关闭。不新增工作流规则，不自动派发实施或验收线程。

### 已核对事实与尚未验证的能力

2026-09-14 通过 GitHub get_pr_info 和 list_pull_request_review_threads 核对 PR #7（Add governed Freqtrade dry-run adapter）：仍 open、未合并，head 为 8d1037fe317ebfddbc3f70b5b306a73ac4681036，base main 为 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28。它在 integration/freqtrade-dry-run，不是本地当前分支已经具有的运行成果。入口：<https://github.com/JunYIChen12/quant-research-live-lab/pull/7>。

三条未解决、未过时的 P1 审查意见分别指向：实际加载制品与清单绑定（评论 3994788029）、实际入场金额校验（3994788034）、开仓前运行时风控（3994788038）。这是远程审查记录，不是本次已复现或已修复的结论。PR 描述报告 Freqtrade 2026.8 及若干检查通过，但本次未复跑，也未核验运行实例；分析时须锁定实际版本。

控制塔推断：现有适配可作为优先调查起点，但上述审查与未验证能力不足以批准合并或宣告可用。具体复用比例、架构、指标和实施规格仍待分析；阶段 2 的修复只在达到 READY 的独立实施任务中进行。

### 2026-09-14 阶段 1：Freqtrade 复用与差异分析

本节是静态源码、PR 与本地安全内核对照，不是容器、模拟订单、交易所、账户或实盘验证。外部引擎基准以 Freqtrade `2026.8` 标签为准；当前工作分支没有该适配代码，远程 PR #7 的 head 为 `8d1037fe317ebfddbc3f70b5b306a73ac4681036`，仍为 open、未合并。PR 作者报告的镜像、pytest、ruff 和启动检查未在本轮复跑，不能替代独立验收。

| 需求或能力 | 分类 | 已核对事实与依据 | 缺口或下一步验证 |
| --- | --- | --- | --- |
| 回测、Dry-run、订单与持仓记录 | 可直接复用 | Freqtrade 2026.8 提供 Dry-run；PR #7 声明独立 SQLite、命名卷、只读候选目录和仅本机端口 | 只在阶段 2 的受控 Dry-run 验收中验证实际存储隔离、开平仓记录和恢复行为 |
| Binance USDT 永续、逐仓、1 倍、单一 BTC 合约与每笔上限 | 可通过配置约束 | PR 策略显式检查 dry-run、futures、isolated、`max_open_trades == 1`、BTC/USDT:USDT、配置 stake 不大于 10，并固定返回 1 倍杠杆 | 配置值不是实际订单金额校验；实际请求名义金额必须在开仓临界路径重新计算并拒绝超限 |
| 同一合约由一个策略持仓 | 可直接复用 | Freqtrade 2026.8 官方 FAQ 明确同一交易对同时只开一个仓位；与 R49 一致 | 策略归属、等待队列和共享预算仍由项目控制，不能交给多个机器人各自判断 |
| 冻结制品、发布批准与开仓闸门 | 需要修复，当前阻塞 | 本地 `release.py` 可校验提交、标签、清单和制品摘要；PR #7 有三条未解决 P1：实际加载文件未与清单绑定、强制开仓实际金额未校验、`evaluate_runtime_gate` 未接到开仓路径 | 阶段 2 必须先以失败测试证明三种情形都拒绝，再修复；未通过不得合并 PR 或启动 Dry-run |
| 风控失败时拒绝开仓 | 需要修复，当前阻塞 | 本地 `gates.py` 只表达纯规则，尚无交易所快照接入；Freqtrade 2026.8 用 `default_retval=True` 包装初始开仓回调，而 PR 的回调未捕获 `ReleaseGate` 异常 | 入口闸门必须对数据、清单、账本和风控的任意异常明确返回拒绝；用故障注入验证，不能仅依赖抛异常 |
| 本轮四项输入、有效计时、净盈亏、平仓核对、重启续平仓与持久停机锁 | 需要项目补充 | 现有 `gates.py` 只计算门槛；R17-R28、R43-R48 定义的是整轮生命周期，而非单笔交易参数 | 阶段 3 建立正式轮次账本和状态机；数据不可靠时禁止新开仓，未完成平仓按 R46 核对后续办 |
| 独立策略准入、Codex 研究反馈与版本晋级 | 需要项目补充 | R31-R41 要求生成方不能修改准入标准或验证结果；本地 Codex CLI 已观察到支持非交互执行和 JSONL 输出，但第三方中转 API 的自动调用路径尚未核验 | 阶段 4-5 才建立隔离数据、候选证据和额度恢复处理；研究恢复不得解除交易锁 |
| 多策略跨合约并行 | 当前阻塞 | R29 允许最终共享预算多策略，R49 已解决同一合约冲突；但现行 AGENTS 第 4 条与 `RiskLimits.max_open_positions = 1` 仍禁止自动增加仓位数 | 阶段 2-3 保持全局单仓位；阶段 6 前须经过独立的安全规则审阅、变更理由、测试和回滚设计，不能借 Freqtrade 配置绕过 |

官方与版本依据：<https://github.com/freqtrade/freqtrade/blob/2026.8/docs/faq.md>（Dry-run 和同一交易对单仓位）；<https://github.com/freqtrade/freqtrade/blob/2026.8/freqtrade/freqtradebot.py> 及 <https://github.com/freqtrade/freqtrade/blob/2026.8/freqtrade/strategy/strategy_wrapper.py>（初始开仓回调与异常默认返回值）。项目和 PR 依据：`src/quant_lab/gates.py`、`src/quant_lab/release.py`、PR #7 及其三条未解决审查意见。这里的“需要修复”和“阻塞”是控制塔基于静态证据的判断；尚未运行的行为明确保留为待验证。

### 阶段 1 结论与阶段 2 边界

历史阶段 1 结论：当时保留 Freqtrade 作为唯一的执行、回测和 Dry-run 引擎，同时保留项目完整控制层。该结论已被上方 R53-R59 和修订框架替代，只作为旧路线证据，不授权实现、合并 PR、启动容器或交易。

历史阶段 2 范围当时仅包括：(1) 实际加载的策略和配置与冻结清单一一绑定；(2) 用实际订单名义金额和固定 1 倍检查每次开仓；(3) 从可信快照调用 `evaluate_runtime_gate`，任何未知或异常都拒绝；(4) 对应失败分析、回滚说明和独立验收场景。该顺序已被修订框架替代，不作为当前实施范围；任务继续 `ANALYZING`，不能跳过 `READY`。

### 近期确认来源

来源均为控制塔任务 01a09416-4080-7b41-a5bc-a56dae7cf247（00｜项目控制塔_量化交易）。下列记录对应本次直接回读的用户消息和已完成答复；金额例子不是固定配置，路线批准不将未回答的问题自动变成确认。R24 已替换旧口径，新增 R43-R67 见当前契约。

| 来源 | 已完成提问或方案轮次 | 用户确认轮次、原话及含义 |
| --- | --- | --- |
| E11 | 01a09daf-df6f-7651-9b52-d34f4acbe8df | 01a09db9-7ec6-7352-b995-ba44b096561f：“确认”；认可七阶段路线，并授权两个文档本地整理 |
| E12 | 01a09d7d-5f88-7273-85a4-a132b5791846 | 01a09d82-e711-77f0-a7c1-bbd872fa2931：“同意”；本轮亏损硬上限启动前确定，运行中固定，仍可按批准规则提前保护 |
| E13 | 01a09d57-217f-7cc0-a568-35a6339100e9 | 01a09d5b-c701-7d40-882c-6fba290ffc10：“同意”；同时只运行一轮正式模拟，隔离试跑不算另一轮 |
| E14 | 01a09d5b-c701-7d40-882c-6fba290ffc10 | 01a09d60-0253-7993-93f6-61c537545380：“同意”；一轮内不追加或取出模拟资金 |
| E15 | 01a09d60-0253-7993-93f6-61c537545380 | 01a09d65-9ec0-7c12-a5f2-39c64471c8be：“同意”；行情可靠且无其他暂停原因时，平仓核对继续计时 |
| E16 | 01a09d82-e711-77f0-a7c1-bbd872fa2931 | 01a09d84-da69-7263-9e7f-ad6a95ce0c1d：“是”；重启核对后自动续完已启动的平仓，保留账目和锁 |
| E17 | 01a09d84-da69-7263-9e7f-ad6a95ce0c1d | 01a09d86-f36e-76f0-b7de-f0619f3c7311：“按最新结果核算”；否定仅因离线期间曾越线而追认停机 |
| E18 | 01a09d9b-37a3-71b3-b2c9-b82282a13ec5 | 01a09d9d-acab-7d33-9d04-3ca2aa1b7860：批注“就按照你这个去定义吧”；一次结束指令后平仓核对并暂停，盈利不足也不再追补 |
| E19 | 当前控制塔对话的 Freqtrade 复用与差异分析 | 用户答复“那就等待吧”；确认同一合约已有某策略持仓时，其他策略等待该仓完全结束后再竞争下一笔 |
| E20 | 当前控制塔对话的第二期细节分析 | 用户答复“是”；确认进入 `CLOSING` 或 `RECONCILING` 后冻结本轮盈利目标和有效时长，修改不排队到本轮生效 |
| E21 | 当前控制塔对话的第二期细节分析 | 用户答复“是”；确认正常运行中修改盈利目标或有效时长立即整体生效，同时修改时全部成功或全部不生效，满足新停止条件便立即进入平仓 |
| E22 | 当前控制塔对话的第二期细节分析 | 用户答复“是”；确认运行中修改的是从首次成交开始累计的本轮总有效时长，不从修改时刻重新计时或追加完整时长 |
| E23 | 当前控制塔对话的 Freqtrade 架构复盘 | 用户明确要求沿用成熟 Freqtrade 框架，并连续确认 Freqtrade 作为完整交易基础、首版先打通一个标准策略闭环 |
| E24 | 当前控制塔对话的原生验证设计 | 用户要求 Freqtrade 已有设计直接参考，不继续要求用户选择技术细节；确认使用原生 Hyperopt、Backtesting、lookahead-analysis、recursive-analysis 和 FreqUI，不自建评分或界面 |
| E25 | 当前控制塔对话的发布批准 | 用户选择“前者”；确认首版每个新策略版本进入 Dry-run 前分别人工批准一次 |
| E26 | 当前控制塔对话的最小审批（已被 2026-09-16 最新界面决定部分替代） | 继续保留冻结验证报告、发布清单和批准绑定准确制品；“不开发审批界面”已被最小 Web 管理台决定替代 |
| E27 | 当前控制塔对话的启动与切换 | 用户答复“是”；确认批准同时作为启动授权，无持仓时自动启动，有旧持仓时等待结束并核对后自动切换，不二次确认 |
| E28 | 当前控制塔对话的失败与主线审计 | 用户接受新策略加载失败后保持暂停并通知、不恢复旧策略；随后确认回到总体框架，并确认 Freqtrade 核心版框架替代旧方案，授权更新文档并同步远程 Git |
| E29 | 2026-09-15 当前控制塔对话的 Freqtrade 边界纠偏 | 用户指出大量问题已有 Freqtrade 答案，随后确认停止逐项重设计原生交易机制，只讨论项目特有差异 |
| E30 | 2026-09-15 当前控制塔对话的动态杠杆原则 | 用户确认杠杆由策略根据行情决定、用户不手动干预；冻结公式输出变化不算新版本，修改公式、参数或上限须形成新版本并重新验证批准 |
| E31 | 2026-09-15 当前控制塔对话的品种风险上限 | 用户确认按交易品种分别设置，由系统自动计算；风险恶化可以降低，有效上限再次提高必须重新验证 |
| E32 | 2026-09-15 当前控制塔对话的存量仓位降险 | 用户确认普通超限先减仓，硬风险或减仓失败才全平；减仓后须同时满足最新风险敞口上限和最低爆仓安全距离 |
| E33 | 2026-09-15 当前控制塔对话的退出与流动性准入 | 用户在公开实践调研后确认复用 Freqtrade 的限价退出、固定超时、有限重试及紧急市价退出；无法证明可靠退出的品种禁止入场，独立验证掌握硬门槛 |
| E34 | 2026-09-15 当前控制塔对话的契约整理方式 | 用户确认保留所有人类确认的 `Rxx` 正式条款，只增加中文主题目录；取消 `C/F` 第二套编号，目录不得替代原子契约 |

E18 批注定位的原答复 messageId 为 msg_070965b6a1089944016aa752fbfeac87d2a1b9366d713fee01。此前未回答的平仓核对期间修改问题已由 E20 确认并形成 R50；R22 的正常运行动态修改权限不因此取消。

### 后续场景与本次核验边界

保留已有正常流程、钱和时间核算、停止与继续、异常恢复、一致性检查五项内容，纳入新路线相关阶段，不新建五个任务。后续用虚构数据核验：预计达标但扣费不足、部分平仓后重启、到期与开仓信号并发、普通空仓与无合格策略等待、Codex 恢复但交易锁仍在。将旧“运行中调低上限后超限”场景替换为“用户或系统尝试修改本轮亏损硬上限被拒绝，原值保留”；另覆盖主动结束后盈利不足不追补、离线最新核算不清零且不解已有锁。上述业务场景本次均未执行，精确并发优先级尚未定稿。

写前已执行 Get-Location、git rev-parse HEAD、git status --short --branch --untracked-files=all、git remote -v 及 git diff --check，目录/分支/HEAD 如本文件基线，工作区干净。适用父级与项目 AGENTS、共享及项目 WORKFLOW、状态、索引、指针及当前任务已核对；migration 仅 README，无未吸收交接。Node 内置模块只在内存保存两个目标和十一项保留文件的写前快照；没有新建辅助文件。

失败与补救：父级规则读取命令首次出现 PowerShell “An empty pipe element is not allowed”，改用变量承接后读取成功；read_thread 的 turnLimit=12 被拒绝，改为 10 后只返回元数据和空 items，不能据此声称读到消息。随后只读当前任务的本地 rollout，按 JSON 类型提取用户消息及 task_complete 的完成答复，核对 E11-E18。首次筛选 response_item 的 assistant/final 未找到目标，未作为缺失事实；改读明确的完成答复字段后找到来源。未读取其他项目对话、凭据或账户导出。

写后自查：git diff --check 退出 0，已回读两文件完整差异；Node 内置模块核验两个文件的严格 UTF-8、末尾换行、空白、冲突标记、围栏、12 个本地链接及 18 个表格均 PASS。R01-R48 共 48 个唯一编号，E01-E18 共 18 个唯一来源；E11-E18 的八组提问、紧接的用户确认轮次和原话与本地任务记录相符。首次编号完整性检查误用 R1 匹配原有 R01 而失败，改为解析编号数值后重跑通过，没有改动原编号。

历史与范围核验：旧六阶段计划正文及其回执、2026-09-13 推送回执、2026-09-12 从“可观察目标”开始的正文保持原样；十一项保留文件与写前快照逐字节相同。git diff --name-only HEAD 恰为本文件和 CURRENT_TASK.md；暂存区及未跟踪列表为空，分支和 HEAD 未变。状态仍为 ANALYZING，责任仍为 00｜项目控制塔，与任务索引一致。新增差异的定向私钥和令牌模式检查通过，不是全面安全审计；文档源文和表格核对不冒充浏览器视觉验收。

本次资料整理已完成，但未运行 pytest/ruff、模拟交易、Windows 运行测试、远程 CI 或独立验收；未提交、推送、合并 PR、安装依赖、启动服务或子代理。下一步仍为当前任务内的 Freqtrade 复用与差异分析，不进入业务实施。回退须另获授权后使用审阅过的反向补丁，不覆盖用户后续修改。

## 2026-09-14 旧推进计划与同步记录（历史）

本节六阶段路线、五步顺序和当时的“下一步”已由上方七阶段路线替代；保留原文与同步证据。这里的“本次”及提交、推送权限均仅指此前计划同步，不是当前授权；有效需求以下方更新后的当前契约为准。

### 确认来源与本次边界

来源为当前控制塔线程 01a09416-4080-7b41-a5bc-a56dae7cf247。用户对“复用现有基础、先打通可靠模拟流程、再接入自动研究与多策略”的路线回答“同意”，随后要求通俗解释，并在“一轮模拟交易，从开始到结束到底怎么运行”的批注中问“这个有计划吗”。控制塔给出五步细化计划后，用户要求同步到远程 Git，前次工具读取后中断，2026-09-14 再次明确“这个计划同步到远程git吧”。此授权用于保存计划，不代表逐项设计审批、业务实施或验收通过。

本次唯一可观察目标：将六阶段路线和五步细化计划保存在当前任务文件，使仓库入口能找到下一步，并核对 GitHub 工作分支包含该计划。不新建业务任务，不改变当前状态和责任人；详细设计、技术选型和执行级任务清单尚未定稿。计划不覆盖下文 R17-R42 或现行安全与发布规则。

仅修改本文件及 CURRENT_TASK.md。TASKS.md 保留本任务 ANALYZING；PROJECT_STATE.md 通过现有当前任务链接定位计划，不复制计划或改写历史快照。其他文档、代码、测试、配置、依赖和共享规则保持不变；没有稳定规则变更，因此不修改 CHANGELOG.md。

### 已认可的总体方向

保留完整的首阶段模拟自动化目标，按依赖分段交付：先检查基础是否可靠，再建立策略证据，最后扩大自动化范围。优先复用现有风险及发布校验，评估成熟行情、回测和模拟引擎，只补项目特有的轮次控制、核算与权限衔接；不推倒重写，也不为了适配工具而改变核心需求。Freqtrade 及既有适配成果仅是待评估对象，不是本次确定的技术选型或已验收能力。

| 阶段 | 主要交付 | 阶段检查点，不代表已通过 |
| --- | --- | --- |
| 1. 收口总体设计 | 一轮运行流程、权限边界、异常优先级、准入指标及校准方案；检查现有适配、行情来源和 Codex 接入可行性 | 影响下一项实施的关键冲突已解决；范围、保持不变项、允许修改对象与验证方法明确 |
| 2. 最小模拟流程 | 四项输入、模拟开平仓、完整成本与盈亏核算、有效计时、停止和重启恢复 | 用可复算场景证明账目、计时、平仓核对和停机锁正确；工程测试策略不得冒充合格策略或进入正式合格池 |
| 3. 独立验证流程 | 数据版本及隔离、候选冻结、成本评估、隔离试跑、三类结论、持续复评和退场 | 不合格或证据不足不能入场；每个结论可追溯到版本、数据、标准和全部试验记录 |
| 4. Codex 研究循环 | 运行反馈、生成新候选、独立验证、合格后自动参与模拟；额度中断和恢复 | 完成一次可追溯迭代；Codex 不可用不撤销风控，不能改准入标准或解除交易停机锁 |
| 5. 多策略与研究范围 | 多策略共用资金和总风险，明确持仓归属、动态分配及全部目标合约的候选管理 | 不重复分配本金或风险预算；策略退出不误伤其他策略；新增合约数据不足时不得入场 |
| 6. Windows 使用与整体验收 | 最小界面、原生通知、关窗后台运行、开机核对、完整退出和故障恢复 | 在实际 Windows 环境核对正常流程及断网、休眠、重启、额度耗尽、未完成平仓等场景 |

六阶段是路线，不是六个已登记或已获实施授权的大任务。Codex、多策略及目标合约全范围仍属于首阶段产品目标，只调整开发先后；第 2 阶段可提供最小操作入口，第 6 阶段完善日常使用与整体验收。候选隔离执行、文件访问权限和资源限制须设计、验证，不能只靠提示词防越权。

### 当前分析的五步细化计划

| 顺序 | 工作内容 | 可复核产物与完成条件 |
| --- | --- | --- |
| 1. 讲清正常流程 | 从填写目标、研究与验证，到模拟买卖、计时、平仓和暂停，顺序走完一轮 | 一轮交易步骤表；每步写明触发条件、动作、记录及下一状态，与既有契约一致 |
| 2. 补齐意外情况 | 处理断网、重启、平仓未完成、无合格策略、Codex 额度耗尽，以及多个条件同时触发 | 异常处理表；写明禁止动作、恢复条件和提醒，不以发出平仓命令代替已平仓 |
| 3. 确定分工与标准 | 定义核算、订单、风控、生成与验证权限，提出准入及复评指标和校准方法，检查可复用工具 | 职责与权限表、指标草案和复用结论；不要求用户猜技术阈值，未决冲突不得写成已批准 |
| 4. 写出验收场景 | 用正常、边界和失败例子逐项检查设计；区分工程正确与策略证据 | 每例有输入、操作、预期结果和复核方法，可对应需求；需用户确认的取舍仍明确标出 |
| 5. 拆成小开发任务 | 按依赖拆成一个目标、一个责任人、一个写入范围的小任务 | 用户确认相应设计后，控制塔登记当前一项任务；范围和验收方法完备且达到 READY 才交实施，实施后独立验收 |

第 1-4 步仍属于本分析任务，不另起多个业务任务。总体路线已获认可，具体设计和执行级任务清单尚待形成、核对及确认；第 5 步不得将文档生成或路线获认可当作 READY。已有需求不重复盘问，由控制塔先给草案，每次只问一个真正影响行为的关键问题。

验收清单至少保留下列已讨论场景，具体输入数值和复核命令待设计时补齐，当前均未执行：

- 触及亏损上限时禁止新开仓，完成平仓核对并保留暂停锁；平仓未完成时不能声称已空仓。
- 重启后保留本轮盈亏与停机锁，核对未通过不得恢复；不能通过重启重新获得风险额度。
- 仅 Codex 额度不可用时保存研究进度，执行与独立风控仍按规则运行；额度恢复不解锁交易。
- 预计达标但平仓扣费后不足，只在本轮剩余有效时长和原风险限制内继续；不承诺一定补足。

### 未决事项与停止条件

现行单持仓、禁止自动扩大风险及人工发布规则与新需求的冲突仍未解决，继续按现行规则执行。不得靠计划文字开放权限或放宽限制；后续须通过规定的审阅与变更流程解决。数值阈值、离线估值、平仓重试、并发优先级、同币种持仓归属及具体技术选型仍需设计，详见下文“当前模块边界与未决事项”。

本次不运行测试策略、不访问交易所或读取真实凭据与账户导出，不设置服务、自启、通知或自动继续；不启动子代理。仅推送 origin/codex/workbench-init，不更新 main、不创建或合并 PR、不打标签、不部署、不强推。若远程历史不一致、出现无法保留的并发改动或推送拒绝，停止修改范围扩展并如实报告，不覆盖远程。

### 本次基线与核验记录

2026-09-14 使用 Get-Location、git status --short --branch --untracked-files=all、git rev-parse --verify HEAD、git remote -v 及 git ls-remote --symref origin HEAD refs/heads/main refs/heads/codex/workbench-init 核对：唯一目录与本文件一致，分支 codex/workbench-init，工作区干净，本地及远程工作分支均为 60abb777ff079a301b70685cbdfd4ecffc96df3c；远程默认分支 main 为 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28。前次中断仅已读取技能，没有留下文件改动。

父级及项目规则、共享及项目 WORKFLOW、状态、索引、指针、当前任务和 migration 已回读；migration 仅有 README，无未吸收的交接文件。记忆关键词查询无匹配退出 1，仅表示没有相关记忆，不是业务检查结论。本次不依赖聊天作为唯一计划存储。

写入工具失败记录：首次通过管道调用 apply_patch 被拒绝为“requires a UTF-8 PATCH argument”；随后经 PowerShell 向批处理传递多行参数被拒绝为“last line ... must be '*** End Patch'”。git status 再核对仍干净。停止重复尝试后读取 apply_patch.bat，确认它调用 codex.exe --codex-run-as-apply-patch；改由 Node execFileSync 直接向相同补丁入口传递完整参数，避免批处理的多行参数传递问题。不是绕过审批或使用其他文件写入方式。

写后自查：已回读 git diff 中两文件的实际改动并与本线程已讨论计划对照。Node 内置 fs/path/assert/child_process 检查两文件 UTF-8、末尾换行、行尾空白、冲突标记及围栏均 PASS；12 个本地链接均存在，15 个表格的列数一致。当前状态、责任与任务索引一致；从“2026-09-13 当前契约”起的原始字节完整保留。git diff --check 退出 0；git diff --name-only 60abb777ff079a301b70685cbdfd4ecffc96df3c 恰为本文件与 CURRENT_TASK.md，其余跟踪文件未改。

新增差异的定向凭据模式检查 PASS，仅覆盖所检查的私钥和令牌模式，不是全账户安全审计。上述为文档源文、结构和差异自查，不冒充浏览器视觉验收、独立验收或业务测试。提交前复查暂存内容与白名单，提交后仍须核对真实远程引用；实际提交及推送结果在后续回执记录，不以本段自查替代。

未执行业务 pytest/ruff、模拟运行、Windows 运行测试、远程 CI 或独立验收；这些不在计划同步范围内。回退需另获授权后使用审阅过的反向补丁或新 revert 提交，不重写已推送历史。任务继续 ANALYZING，下一步仍由控制塔开展第 1 步讨论。

### 计划同步回执（2026-09-14）

- git diff --cached --check 退出 0；暂存白名单恰为 CURRENT_TASK.md 与本任务文件，未暂存改动为空。提交前复查文档结构、链接、状态、原契约字节保留及新增差异凭据模式，结果同上。
- git commit -m "docs: persist agreed roadmap and simulation planning steps" 成功；2 文件，72 行新增、6 行删除。git rev-parse --verify HEAD 返回 a94905146dbe9fcbc83489ecf761e72052494e6a。
- git push origin HEAD:refs/heads/codex/workbench-init 退出 0，从 60abb777 推进到上述计划提交，无强推。
- 随后 git ls-remote origin refs/heads/codex/workbench-init refs/heads/main 退出 0：工作分支为 a94905146dbe9fcbc83489ecf761e72052494e6a，与本地一致；main 保持 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28。git status --short --branch --untracked-files=all 显示跟踪一致且无未提交或未跟踪文件。

本回执在上述结果确认后补写，随后单独提交、同步，不改写已推送历史。可用 git log -1 --format=%H -- docs/codex/tasks/20260912-002-system-requirements.md 定位含本回执的最后提交，再对照 git rev-parse HEAD 与 git ls-remote 核对最终分支；不预填本回执自身未来的 SHA。计划文件已远程持久化，不等于需求定稿、主分支合并、业务实施或独立验收，当前任务与责任保持不变。

## 当前契约（2026-09-15 更新）

R17-R42 于 2026-09-13 形成；2026-09-14 按 E12 更新 R24，新增 R43-R59；2026-09-15 按 E29-E34 补充 R60-R67，并确认中文目录只作导航。未被后续用户明确确认替代的需求继续有效。下列旧授权说明和本节末尾的历史自查、Git 回执只记录对应历史操作，不扩大当前文件整理权限。

### 2026-09-13 目标、授权与证据等级（历史说明）

主要目标仍是形成可实施、可复核的个人自动化系统需求与模块边界。本次可观察交付仅为：完整整理续聊确认事项，标明历史替代与未决项，并在指定 GitHub 工作分支保存文档；不宣称需求任务完成。

用户在当前线程先说“你的建议我采纳了”，随后对整理入库的询问回复“允许，顺便也更新一下远程的git”。据此，本次控制塔可修改本任务文件、CURRENT_TASK.md、PROJECT_STATE.md，并提交、推送此前已审阅的工作台文档及本次更新。TASKS.md 状态无需改变，原样保留。此次不改稳定工作流规则，CHANGELOG.md 只随原有初始化记录入库，不新增规则修订。

需求确认、用户描述的使用条件、仓库实测、公开作者自述和控制塔建议分开记录。以下 R17-R42 为经连续问答确认的产品需求，不是运行事实。涉及现行安全或发布规则的冲突仍是实施阻塞，不以本文件覆盖原规则。

R01-R13 未被本节替代或收紧的部分继续作为需求保留：个人使用、第一阶段仅模拟、验证后按 R57-R58 经每版本人工批准再切换、亏损停机须明确重授权、Windows 通知，以及全部可交易 USDT 本位永续合约进入研究候选池。新上架同类合约须先满足数据和验证要求才可参与模拟，不要求所有合约同时持仓；实际持仓继续受现行单仓位规则限制。R02/R03 的适用边界见替代对照表。ROI 相关 R14-R16 仅保留历史效力。

### 盈利目标与风险

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R17 | 启动时由用户填写模拟本金、预期净盈利金额、期望达成时长、最大允许净亏损金额 | 四项均为用户输入，不硬编码金额或固定 2 小时；预期盈利不是收益保证。E01 |
| R18 | 盈利目标按本轮整体扣除手续费、资金费等交易成本后的净盈利计算 | 不按单笔或某个策略单独达成目标；本轮资金流按 R44，金额单位及精度待定。E02、E05、E14 |
| R19 | 以“现在全部平仓后本轮预计净盈利”触发目标平仓，包含此前已实现盈亏、当前持仓盈亏及预计平仓成本 | 先禁止新开仓、平掉全部模拟持仓并核对；最终达标以实际结算为准。E02 |
| R20 | 实际净盈利超过目标则保留超额；实际不足可在本轮剩余时长和原风险授权内按原计划继续开仓尝试补足 | 不是无限追补、不保证补足，不绕过到期、亏损停机或 R48 主动结束；达标全平后暂停。E02、E18 |
| R21 | 有效时长用尽仍未达标时，禁止新开仓、平掉全部模拟持仓、核对结果、暂停并发 Windows 提醒 | 未经用户明确授权不得恢复；“二次平仓”经澄清只是平仓并核对，不是另设特殊交易流程。E02 |
| R22 | 运行中用户可动态修改盈利目标和期望时长；系统不能自行降低目标或延长期限，只能在授权内调整策略 | 修改不清零本轮盈亏；正常运行中的生效规则按 R51，总时长解释按 R52，进入平仓或核对阶段后按 R50 冻结，具体输入校验仍待定。E03、E20-E22 |
| R23 | 亏损上限按“本轮启动本金减去现在全部平仓后预计剩余资金”判断，计入浮动盈亏与预计平仓成本 | 触及上限立即禁止开仓、全平并锁定暂停，用户明确重新授权才能恢复；不重置记录绕过。E03 |
| R24 | 第一版中，本轮亏损金额硬上限在启动前确定，运行中用户和系统均不能修改 | 替代“只允许调低”；仍可按已批准规则提前减仓、平仓或停机，不要求等亏够才保护。R03 不适用于修改本轮硬上限。E12 |

### 计时、多策略与停机

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R25 | 本轮第一笔模拟开仓成交开始计时；之前的研究、验证和等待机会不计时；开始后普通空仓等待计入 | 不是点击开始就计时，也不是每次平仓都暂停。E04 |
| R26 | 电脑关机期间不运行且不计时；断网导致行情不可靠时暂停计时、禁止新开仓；恢复后先核对状态再续算剩余时长 | 盈亏与停机锁保留，最新结果核算按 R47；补数、估值实现、休眠和故障边界待定，时间暂停不是风险消失。E04、E08、E17 |
| R27 | 所有策略退场、账户已空仓且没有合格策略时，进入等待合格策略：暂停计时、继续研究 | 新合格策略出现后，状态核对通过且未触发硬停机，可自动恢复同一轮交易和计时。普通空仓不自动适用此例外。E07 |
| R28 | 达标、到期、亏损停机或按 R48 主动结束后，研究和验证可继续，但新候选、重启或 Codex 恢复均不能解锁交易或自动启动下一轮 | 人工明确授权边界保留；结束本轮不是退出整个程序，不创建实际后台研究任务。E06、E08、E09、E18 |
| R29 | 同一轮允许多套合格策略并行，共用本轮本金、目标、总盈亏和总亏损上限；系统在总预算和风险内动态分配资金 | 不给每个策略复制一份完整本金或亏损额度；同一合约持仓归属按 R49，仓位数与相关性约束仍待定。E05、E19 |
| R30 | 策略产生信号；执行统一处理订单和持仓核对；独立风控对新开仓有最终否决权，可要求全平并锁定 | 策略不能改硬限制或绕过风控直接下单。“独立”是权限分离，不要求多套部署或人工逐笔批准。E05 |
| R49 | 同一合约已有某策略持仓时，其他策略必须等待该仓完全结束后，才能竞争下一笔开仓 | 同一合约同一时间只归属一个策略；其他合约是否可并行仍受 R29、现行单持仓和统一风控约束。E19 |

### 研究、验证与退场

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R31 | 生成策略的模块不能修改准入标准或验证结果，只有独立验证通过的具体版本才获得模拟交易资格 | 验证自动执行，不由生成方自行宣布合格；模拟资格不是实盘资格。E06 |
| R32 | 不直接改写正在运行的策略版本；代码或参数变化形成新候选并重新验证 | 沿用不可变版本边界；正常替换按 R58 交接，失格和紧急风险按 R35/R23 处理。E07、E27 |
| R33 | 持续循环：合格策略运行取数，积累当期行情、信号、模拟成交、成本与盈亏；复盘反馈给 Codex，生成新候选，独立验证并经批准后入场 | 反馈数据若用于开发或调整候选 B，就不能再作为 B 的独立验证；使用未参与开发的预留数据或后续新行情。E07、E25-E27 |
| R34 | 新候选先跟随新行情做隔离模拟试跑，单独验证和记账；不占本轮模拟本金，不计入本轮盈亏与目标 | 通过后才可正式参与本轮模拟交易；隔离试跑不等于真实交易试错。E09 |
| R35 | 运行策略按预定标准被判不合格时，自动停止它的新开仓并平掉其模拟持仓；其他合格策略在账户总风控允许下继续 | 失格标准与持仓归属须可执行、可验证；不把任意一次亏损自动等同策略失效。E07 |
| R36 | 每次验证保存策略版本、数据范围、所用标准、结果及失败原因，保留全部候选与失败记录 | 不能只保存赢家；版本、反馈和重复试验可追溯，不能以换策略重置本轮记录。E06、E10 |
| R37 | 候选准入检查正确性、独立收益证据、完整成本、风险和运行一致性 | 覆盖未来信息使用、指标初始化偏差、开发与验证数据隔离、模拟成交假设、信号/订单/持仓/账本核对；短期盈利本身不足以准入。E10 |
| R38 | 验证输出三种结果：合格、不合格、证据不足 | 合格只获得申请批准的资格，启用还需 R57 的每版本批准并受运行状态和现行风险规则约束；不合格拒绝并说明原因；证据不足继续采集。E10、E25 |
| R39 | 评估前确定标准；不能因没有策略通过就自动放宽；由控制塔提出有依据的指标并用项目数据校准，再确认数值 | 默认复用 Freqtrade 原生指标、报告和 `MultiMetricHyperOptLoss`，不自建评分公式；尚未约定准入阈值、交易笔数或观察长度。用户盈利目标不能替代验证证据。E10、E24 |

### Codex 与 Windows 运行条件

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R40 | 研究工具选现有 Codex，用于生成和改进候选；已验证策略、执行和风控应脱离 Codex 独立运行 | 仅 Codex 不可用时，只暂停依赖它的研究，不据此中断正常模拟交易或撤销风控。接入方式尚未实施。E09 |
| R41 | 按用户描述：当前是 Codex 桌面版配第三方中转 API、订阅额度制；额度用完停止，恢复时间不固定。额度不足时保留进度、暂停依赖 Codex 的研究并通知；确认恢复可用后自动续研 | 这是用户描述，未读取配置、账单或凭据核验。不假定官方按量计费或固定重置时间；没有购买额度或提高套餐的授权。自动续研不解锁交易。E09 |
| R42 | Windows 原生通知、关闭窗口后后台运行、开机恢复核对及保留停机锁继续沿用 R07-R10；第一阶段不要求电脑关机后继续运行 | 不引入常驻云服务；主窗口关闭不等于完全退出。自启、通知、后台进程均尚未设置或验证。E08 |

### 2026-09-14 补充的运行边界

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R43 | 第一版同一时间只运行一轮正式模拟交易，多套合格策略共用这一轮资金、目标和风险预算 | 候选隔离试跑不算另一轮正式交易；不能另开一轮绕过停机锁。E13 |
| R44 | 第一版一轮开始后不允许追加或取出模拟资金，本轮起始本金固定 | 余额可随盈亏和费用变化；策略间可在授权内重新分配，不能补钱改写盈利或亏损。E14 |
| R45 | 已开始计时后，行情可靠且没有其他已确认暂停原因时，整轮平仓与核对继续消耗有效时长 | 时间用尽后不能因盈利不足重开仓；即使剩余时长为零，仍须完成平仓与核对。E15 |
| R46 | 已启动但未完成的平仓，重启后先禁止新开仓，核对已成交和剩余持仓，再自动继续，无需再次批准 | 原盈亏和停机锁保留；核对不清保持异常并提醒，不盲目重复下单或声称已平仓。E16 |
| R47 | 离线恢复后先核对状态，按最新可靠结果计算本轮净盈亏，不因离线期间曾越线而追认停机 | 历史盈亏与费用继续累计；中断前已触发并记录的锁不能因结果好转解除；继续还须满足时间、资格和其他风控，未知状态禁止开仓，不虚构离线成交。E17 |
| R48 | 用户发出一次“结束本轮”指令后，禁止新开仓，处理剩余平仓、核对实际盈亏与费用，然后保持暂停 | 盈利不足也不追补；未平完或核对失败显示结束处理中或异常并提醒，不能宣布成功；重启按 R46，恢复交易须明确重新授权，关窗不等于结束本轮。E18 |
| R50 | 本轮一旦进入 `CLOSING` 或 `RECONCILING`，用户和系统均不能修改本轮盈利目标或有效时长，修改请求也不排队到本轮生效 | 新数值只能用于下一轮，避免正在结束的轮次重新开放交易，并保持停机锁、计时和账目核对一致。E20 |
| R51 | 本轮处于正常运行状态时，用户修改盈利目标或有效时长立即整体生效；同时修改多项时全部成功或全部不生效 | 生效后立即按同一份可靠快照重算；新目标已经达到或新有效时长已经用尽时，立即禁止新开仓并进入平仓流程。E21 |
| R52 | 运行中修改有效时长时，修改的是从本轮首次开仓成交起累计的总有效时长，不从修改时刻重新计时，也不在剩余时长上追加完整的新时长 | 已消耗的有效时长保持不变；新总时长减去已消耗时长得到新的剩余时长，结果小于或等于零时按 R51 立即进入平仓。E22 |

### Freqtrade 核心版框架边界

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R53 | Freqtrade 2026.8 作为首版完整交易基础，负责行情、交易对、标准策略运行、回测、Hyperopt、分析命令、Dry-run、订单、持仓、余额、SQLite、原生风控、REST API 和 FreqUI | 不修改 Freqtrade 核心，不在项目内复制这些能力；文档能力仍须在后续实施任务中按锁定版本运行验证。E23-E24、E29 |
| R54 | 项目只补 Codex 策略生产、原生验证编排、冻结发布闸门，以及本轮目标、有效时长、最大净亏损、永久停机锁、最小 Web 管理台和 Windows 通知 | 首版不建设完整控制层、第二套订单/资金/交易账本、策略意图路由器、自定义订单/持仓交易界面或分布式平台；交易明细继续使用 FreqUI。E23-E24、E28 |
| R55 | 候选必须是标准 Freqtrade `IStrategy`，依次经过加载检查、lookahead-analysis、recursive-analysis、Hyperopt、固定开发区间回测和独立留出验证 | 第一版优先使用 `MultiMetricHyperOptLoss` 和原生报告；工程测试策略与正式候选隔离，不自建回测、优化或评分系统。E24 |
| R56 | 首版先打通一个候选策略到一个 Freqtrade Dry-run 进程的完整闭环 | 单个策略可以研究多个交易对，但实际仍执行杠杆 1 和最多一个仓位；多策略同时运行、FreqAI 和官方多实例评估延期。E23、E28 |
| R57 | 每个冻结策略版本进入正式 Dry-run 前都需要一次人工批准；首版在最小 Web 管理台展示验证报告和发布清单，并接收批准或拒绝 | 批准绑定准确代码、参数、配置、交易对、风险设置和证据；任一项改变都形成新版本并重新验证、重新批准。首版仍只允许一个正式策略，不因增加 Web 管理台开放多正式策略并发。E25-E26 及 2026-09-16 最新界面决定 |
| R58 | 每版本批准同时构成安全条件满足后的 Dry-run 启动或切换授权；无旧持仓时自动启动，有旧持仓时等待其结束并核对后自动切换 | 不二次确认；新版本加载、启动或核对失败时保持暂停并通知，不自动恢复旧策略，也不能宣称切换成功。E27-E28 |
| R59 | Freqtrade 交易数据库是订单、成交、持仓、费用和余额的执行事实来源；项目只保存 Freqtrade 没有覆盖的最小轮次、发布、批准、计时和停机锁状态 | 具体存储、字段、可靠快照和原子更新规则属于第二期；不复制完整交易流水或维护第二套订单状态机。E23-E24、E28 |

### 2026-09-15 确认的未来风险演进契约

以下条款是用户明确确认的正式产品需求，不因当前无法实施而降低优先级。它们与现行 `AGENTS.md` 第 4 条、固定 1 倍和最多一个持仓的安全规则冲突，当前实施状态为受阻；必须通过独立治理任务修改规则、补测试、失败分析和回滚说明后，才可进入实施。

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R60 | 未来策略可以根据冻结规则和当前行情为每笔交易动态给出杠杆建议，用户不手动设置或临时干预杠杆 | 不得因为亏损、追求回本或盈利目标尚远而临时提高杠杆；现行第一版仍固定 1 倍。E30 |
| R61 | 实际允许杠杆取交易所上限、项目按品种确定的有效上限、策略建议值和账户剩余总风险允许值中的最低值 | 任一约束缺失、过期、冲突或无法核对时禁止新开仓，不把某一较高值用于覆盖其他约束。E30-E31 |
| R62 | 项目按交易品种分别设置杠杆与敞口硬上限，由独立验证依据交易所限制、波动率、流动性和最低爆仓安全距离自动确定 | 用户不逐品种填写上限；策略生成模块不能修改或降低验证标准。E31 |
| R63 | 杠杆计算公式、参数和获准最高上限属于冻结策略版本；行情输入变化导致同一公式输出不同杠杆不算新版本 | 运行期有效上限可以自动降低；修改公式、参数、获准上限或再次提高已经降低的有效上限，都必须形成新候选并重新验证、批准。E30-E31 |
| R64 | 已有持仓超过最新有效风险上限时，先禁止该品种新开仓和加仓，并自动减小仓位，直到风险敞口和最低爆仓安全距离同时合格 | 旧策略没有失效且未触发硬风险时，不仅因普通上限下降就默认全平；减仓后的实际状态必须重新核对。E32 |
| R65 | 触发硬风险、减仓失败、限定处理后仍不合格或状态无法确认时，全部平仓、保持停机锁并通知用户 | 发出减仓或平仓命令不等于成功；部分成交继续按实际剩余仓位处理。E32-E33 |
| R66 | 普通风险减仓复用 Freqtrade 原生限价退出、固定超时和有限重试，仍未完成时升级为紧急市价退出；硬风险直接使用紧急退出 | 超时和重试参数随风险配置冻结；不另建动态等待算法，不绕过 Freqtrade 直接调用交易所。E33 |
| R67 | 币安全部可交易的 USDT 本位永续合约只自动进入研究候选池；能否交易由独立流动性准入决定 | 默认复用 Freqtrade 基础过滤，再按计划仓位做退出压力测试和入场前数据新鲜度复查；任一硬门槛失败或数据缺失、过期、矛盾时禁止入场，策略无权降标。E33 |

### 2026-09-16 确认的首版试跑安排

| 编号 | 当前已确认约定 | 边界与来源 |
| --- | --- | --- |
| R68 | 首版采用一个正式 Dry-run 实例和最多一个候选隔离试跑实例；旧策略可继续正式模拟，新候选同时跟随新行情试跑 | 用户在 2026-09-16 对该安排明确回答“是”。候选配置、数据目录、数据库和模拟资金独立，不占本轮本金、不计本轮盈亏；仅替代 R56 的总共单进程限制及该试跑实例延期，保留正式轮次单策略、R57-R58 批准和交接要求。试跑授权及现行仓位/风险边界冲突仍须独立安全治理，当前不得启动。 |
| R69 | 用户预先批准固定的隔离试跑范围，系统在范围内自动测试候选；候选完整验证通过后，进入正式模拟前由用户对准确冻结版本批准一次 | 2026-09-16 用户对“范围内自动试跑，正式入场逐版本批准”回答“是”。系统不得自行扩大试跑权限或批准正式运行；R57-R58 正式批准与交接不变。仅确认产品授权方向，不是实际范围授权或现行发布政策修订；独立治理完成前不得自动试跑。 |
| R70 | 固定隔离试跑范围批准后持续有效，范围内更换候选或电脑重启不重复审批；重启必须先核对授权和状态，用户撤销、范围改变或无法核对时禁止启动新的试跑 | 2026-09-16 用户对“范围不变就沿用授权，重启先核对、不重复审批”回答“是”。不免除每个候选的启动检查、R57 正式版本批准或现行安全/发布治理；范围变化不能沿用旧授权，恢复不能解除正式停机锁，撤销后存量模拟仓位的收尾尚待确认。 |
| R71 | 用户撤销候选试跑授权时，停止新开仓、取消未成交入场委托、平掉该候选模拟持仓、核对并保留记录后停止试跑；失败或无法核对则保持暂停并提醒 | 2026-09-16 用户对该收尾方案回答“是”，补齐 R70 的撤销收尾未决。正常撤销只停止候选试跑；正式策略在自身安全且隔离正常时继续，共享安全故障仍按既有规则处理。命令发出不等于收尾完成，不删库抹账，不影响正式批准/停机锁，现行治理与实施门禁不变。 |
| R72 | 新候选通过准入只进入候选池；当前正式策略仍有效时，新候选必须以同一证据口径证明整体更好，才可申请正式替换；未证明更好则继续试跑或等待，不为换新平掉有效策略 | 2026-09-16 用户对控制塔提出的准入/替换分离原则回答“是”。“更好”的具体风险收益规则尚待确认和校准；不影响 R35 硬失格退场、R57 正式批准或 R58 交接。不能用不同区间、不同成本或不同版本证据制造优势。 |
| R73 | 用户风险型投资偏好允许在全部硬安全、隔离和执行可靠性要求合格且不超过预先批准风险上限时，用更高回撤或波动换取有充分证据支持的更高扣费后收益 | 2026-09-16 用户对该受控权衡回答“是”。风险偏好不是情绪化追损或无限扩权；不得越过硬亏损、爆仓安全距离、授权或现行政策，不得因亏损/回本目标临时调高风险。收益更高不自动通过，具体比较规则仍须依据和校准。 |
| R74 | 候选相对仍有效正式策略增加的风险必须由足够大、同口径且稳健的扣费后收益改善补偿；略高收益不能补偿明显更大风险 | 2026-09-16 用户对此原则回答“是”。具体指标、容差和阈值由公开方法及项目数据校准，不要求用户凭空指定；不得临时改口径、忽略成本或挑选有利区间，仍受 R72-R73 和全部硬门槛约束。 |
| R75 | 候选替换采用不计算总分的证据表：硬安全、隔离、执行可靠性和证据完整性先作不可抵消门槛；通过后以同口径扣费后收益与 wallet 最大回撤作主比较，其他原生指标及留出/隔离前向结果负责交叉反证 | 2026-09-16 用户对“不要算一个总分；先过安全红线，再主要比较赚得更多是否值得多冒的风险，同时让其他指标负责找反例”回答“是”。单一漂亮指标不能抵消硬失败；具体容差、充分性和冲突处理继续校准。 |
| R76 | 固定隔离试跑授权采用冻结边界：至少包括交易所/市场类型、允许品种、最大杠杆、最大持仓数、模拟资金上限和有效期；系统只可在边界内自动更换候选 | 2026-09-16 用户确认该结构及到期收尾。任一边界扩大或无法核对都必须停止并重新批准；到期按 R71 撤销流程停止新开仓、取消入场委托、平掉候选模拟持仓、核对留档，失败保持暂停并通知。具体数值后续填写，本条不自动放宽现行 1 倍杠杆、单持仓、安全或发布规则。 |
| R77 | 候选证据充分性采用自适应混合门槛：正确性、未见数据和隔离前向三层证据缺一不可；样本长度按策略频率和统计不确定性判断，不为所有策略规定同一天数或交易笔数 | 2026-09-16 用户对此方式回答“是”。无法稳定计算、关键路径未覆盖或证据仍矛盾时为“证据不足”；不能用历史证据替代前向试跑，不能因样本数量达到某个统一数字自动合格，也不自动获得正式运行批准。具体统计置信、成本压力和关键路径仍待校准。 |
| R78 | 首个正式候选在没有仍有效正式策略时，以不交易/保持空仓为主要基准；已有仍有效正式策略时按 R72-R75 与现有策略同口径比较 | 2026-09-16 用户对此回答“是”。首个候选必须证明扣费后优势为正、风险在授权内且 R77 三层证据充分；市场变化或买入持有只作辅助参考。用户盈利目标不能替代准入基准，也不因没有现有策略而降低证据要求。 |
| R79 | 候选成本证据采用正常与不利双情景，不使用全策略统一固定倍数 | 2026-09-16 用户对此回答“是”。正常成本数据缺失为证据不足，正常成本下扣费后优势不成立为不合格；合理不利成本触发硬风险为不合格，只让优势消失但未触发硬风险为证据不足；两种情景均通过且隔离试跑实际成本未明显反驳才通过。压力范围依据交易所规则和项目数据校准。 |
| R80 | 策略相关路径每个冻结候选必须重跑；平台公共路径只在版本、配置、环境、依赖身份和证据有效性均未变化时复用，变化时只重跑受影响部分 | 2026-09-16 用户对此回答“是”。候选应覆盖但未触发的路径为证据不足，确认错误或越过硬风险为不合格；平台证据复用不免除新候选形成完整验证结论、冻结身份和按 R57 重新批准。只保存简单证据清单和依赖身份，不建设测试矩阵平台。 |
| R81 | 递归指标差异按是否改变实际交易或风险决策判断，不要求绝对零差异或全指标统一百分比 | 2026-09-16 用户对此回答“是”。差异确认会改变入场、退出、止损、杠杆、仓位或其他风险决策为不合格；命令未覆盖、样本或临界场景不足为证据不足；非零差异已证明不改变相关决策可通过。原生结果、启动蜡烛配置和最小决策差异证据须绑定冻结候选。 |
| R82 | 候选统计证据默认要求修正后的置信达到至少 95%；候选生成、Hyperopt 或比较次数越多，越必须按全部试验修正选择偏差 | 2026-09-16 用户对此回答“是”。单个候选使用 PSR/MinTRL 或等价单侧不确定性检查，多候选使用 DSR 或等价选择偏差修正，PBO 可作额外交叉反证；Freqtrade mean profit p-value 只作辅助。扣费后优势确认不成立为不合格；优势为正但修正置信不足、试验数量无法核对或方法不稳定为证据不足。达到 95% 不是盈利保证，也不能抵消其他硬失败。 |
| R83 | 未来数据偏差先排除 Freqtrade 官方已知误报；确认影响实际交易或风险决策的偏差一律不合格，不设置可接受容差 | 2026-09-16 用户对此回答“是”。命令失败、交易数量不足、冻结信号未触发或结果无法绑定最终候选时为证据不足；限价单、入场连带退出或 FreqAI 目标列等已知误报必须有可复核排除证据。没有确认偏差且所有冻结信号类型均被覆盖时，该项才通过，未覆盖路径的 `No` 不能冒充通过。 |
| R84 | 每个冻结候选使用一份最小试跑清单，绑定候选身份、最终生效配置、前置证据和隔离路径；系统自动核对全部一致后才允许启动 | 2026-09-16 用户对此回答“是”。核对在 R69-R70 固定授权范围内自动完成，不增加逐候选人工审批；任何必需信息缺失、过期、越界或冲突都失败关闭。清单须覆盖冻结提交与文件摘要、Freqtrade 版本、无敏感值的最终配置、独立目录/数据库/日志/端口及 R79-R83 前置证据；隔离前向成绩不是本次试跑的启动前提，启动资格不等于 R57 正式版本批准。 |

### 被替代或收紧的旧约定

| 旧约定或问题 | 当前处理 |
| --- | --- |
| R14-R16：ROI 核心目标、固定 2 小时、用户填 ROI | 已被 R17-R22、R25-R27 替代。ROI 是否作为辅助展示尚未定，不继续按旧问题追问 |
| “可以调高调低亏损上限”及其二次确认方案 | 用户已撤回；R24 为当前约定 |
| 原 R24：“本轮运行中亏损金额硬上限只允许调低，用户和系统都不能调高” | 已被 E12 的运行中固定硬上限替代；不再设计中途调低及其立即超限流程，提前降低实际风险仍保留 |
| 预计达标平仓后差一点也保持暂停 | 此建议未获采纳；按 R20，在剩余时长和风险内可继续尝试补足 |
| 截止时间一律按墙钟流逝，关机/断网也算 | 用户否定；当前是带明确暂停情形的有效计时，离线恢复细则仍待设计 |
| 每次空仓暂停计时 | 用户否定；普通空仓计时，只有 R26-R27 等已确认例外暂停 |
| R03：降低后的风险额度可恢复 | 不能解释为修改本轮固定亏损硬上限；其他风险额度的类型和恢复条件仍待确认，并受现行 AGENTS 第 4 条约束 |
| R02：正常替换先等待旧仓退出 | 首版按 R58：一个策略版本运行，有旧持仓时等待其结束并核对后自动切换；失格策略平仓退场按 R35，整体紧急风控按 R23 |
| R29-R30：首版多策略共享预算、由自建控制层统一处理交易意图 | 作为未来需求保留但不进入首版；R53-R56 改为一个标准策略由 Freqtrade 原生运行，现行最多一个仓位规则继续有效 |
| 旧第一期：项目控制层拥有完整资金、订单、账本、恢复和逐笔意图审批 | 已被 R53-R59 替代；项目只保存最小轮次安全状态，不复制 Freqtrade 的交易基础 |
| 旧方案将 AI 策略生成延后 | 当前需求选择第一阶段接入现有 Codex；技术可行性和现行规则兼容性仍待验证 |

### 2026-09-13 来源索引（E01-E10）

当前来源线程：`01a09416-4080-7b41-a5bc-a56dae7cf247`，应用标题 `00｜项目控制塔_量化交易`，逻辑责任角色为 00｜项目控制塔。本次通过 read_thread 回读用户消息与最终答复，不把内部推理、历史自测或未完成输出当作用户批准。以下 ID 是对话定位标识，不是交易账户或订单标识。

| 来源 | 可定位的问答与确认 |
| --- | --- |
| E01 | 目标回退及四项输入：01a097c7-1c04-7e61-9bdb-9207dbc66aa3 至 01a097ca-4caa-7e01-8303-84c668bf8505；用户提出预期盈利金额，随后连续确认时长、本金和亏损上限 |
| E02 | 目标、到期、平仓、净收益及差额：01a097ca-fa5f-7a41-ba70-0ea4a22b0eea 至 01a097d7-e338-7723-a0bb-97debbfdb2f2；明确“多了就算了”“少了按照原来的计划继续开仓”，并确认到期仍停 |
| E03 | 亏损基准和动态权限：01a097d8-c617-7d63-9388-3bcfdb1097de 至 01a097f6-d4d1-70a3-a674-6866922bdb58；含撤回调高亏损上限的批注及后续“对” |
| E04 | 首次开仓计时：01a09801-0b24-7f21-af6b-b784485c68f0 至 01a09802-cfe4-7332-86c0-c1ab92378cde；用户两次“不算”，随后“不用，算在一起”确认普通空仓计时 |
| E05 | 多策略、资金与独立风控：01a09803-a9a4-7292-910c-9121addfc3ae 至 01a09815-8e1c-7403-aa03-12ec4be19e2e；用户允许并行和动态分配，接受职责分离并要求继续 |
| E06 | 独立验证批注及研究留档：01a09837-74c4-7911-a397-9518a5e2762d、01a09839-1162-70b1-a2ad-7eb62fa38e3b；达标/到期继续研究确认 01a09839-e6e5-7bc1-b1b8-319ee43e3d01 |
| E07 | 持续迭代至等待恢复：01a0984e-4c6c-7e10-bf04-4a55e00342c3 至 01a0987c-49cb-72d1-a9cc-9088562c108e；包含用户对完整五步闭环、失格退场、空仓无策略暂停与自动恢复的确认 |
| E08 | 关机与断网：01a0983c-fa46-7801-9389-e17234bd6ec4 至 01a09842-79f0-7eb1-9c10-55b571f1c73f；关机不用继续运行、不计时，断网行情不可靠暂停并恢复核对 |
| E09 | Codex 分工确认 01a09845-01bd-71e1-be91-7a895db99e7e、01a0984a-fd6a-7782-bedb-2544823965e5；计费、恢复与隔离试跑 01a09880-04a6-7282-92b5-6e7ddcf820db 至 01a09898-695b-7ae2-8c2f-3a68d02c7f62 |
| E10 | 公开实践调研 01a0989e-3c93-7d92-ba55-79a77022442c；用户在 01a098dc-3d5c-7dc0-a9ad-a3abe69f24c1 说“你的建议我采纳了”；本次 01a098dd-b33a-7032-9346-733f3072e45a 授权整理并更新远程 Git |

公开资料仅作为本线程建议的来源，不是本项目实测或盈利证明。此前汇报的六个账号集中于两个社区讨论及一个 GitHub 问题，部分含项目推广；未独立审计收益，不能称为行业共识。本次整理回读了研究答复，未重新抓取网页或运行第三方工具。保留原始入口，后续确定具体技术指标前须重查原文与适用版本：

- 案例：<https://www.reddit.com/r/algotrading/comments/1r1x7mp/my_journey_into_automated_crypto_trading/>、<https://www.reddit.com/r/algotrading/comments/1tv8m1z/its_finally_working/>、<https://github.com/freqtrade/freqtrade/issues/2472>。仅作者自述，不用自报盈利推导阈值。
- 方法参考：<https://www.freqtrade.io/en/stable/lookahead-analysis/>、<https://www.freqtrade.io/en/stable/recursive-analysis/>、<https://www.freqtrade.io/en/stable/backtesting/>、<https://www.quantconnect.com/docs/v2/writing-algorithms/live-trading/reconciliation>、<https://www.davidhbailey.com/dhbpapers/deflated-sharpe.pdf>。保留验证正确性、成本、执行差异和重复筛选风险的依据，不等于选定这些框架或统计指标。

### 当前模块边界与未决事项

当前模块边界以 R53-R59 和第一期框架稿为准：Freqtrade 管理行情、策略运行、订单、持仓、余额、交易数据库、原生风控、API 和 FreqUI；项目外围程序只负责策略生产、独立验证编排、冻结批准和最小轮次安全监督。旧的完整控制层、重复账本和策略意图路由器不再是当前方案。

实施前必须处理的冲突：

1. AGENTS 第 4 条与安全模型仍禁止自动扩大风险、杠杆及范围，并要求最多一个仓位；全部合约可以进入研究候选池，但多持仓和多策略执行必须另行治理。需求记录不解除这些限制。
2. R57 已将首版改为每个冻结策略版本人工批准一次，满足当前发布政策；未来取消批准仍须独立治理。实盘继续需要单独里程碑和人工批准。
3. 本轮亏损硬上限固定、运行中不追加或取出模拟资金已确认；R50-R51 已明确平仓核对阶段冻结和正常运行中立即整体生效。仍需明确金额单位与精度、新一轮授权如何继承历史亏损，以及目标与时长的具体输入校验规则。
4. 具体准入/持续复评/失格阈值、样本充分性、成本情景、数据版本与访问隔离、重复试验的统计校准尚未确定；不得把用户接受原则写成接受具体数字。
5. R46-R47 已确认自动续平仓和恢复后最新核算原则，R58 已确认版本切换失败保持暂停；可靠快照、补数、估值、平仓核对、重试节奏和同时触发优先级仍需设计并验证。不能把发出命令当作已平仓，既有锁不可自动解除。
6. 订单、持仓和交易明细继续使用 FreqUI；项目轮次、验证报告、正式批准、停止和重新授权统一进入最小 Web 管理台。主动完全退出程序、Windows 后台与通知、历史数据覆盖、本机资源限制、Codex 可用性探测和重试节奏仍未选定或核验。

控制塔推断：按当前确认的暂停规则，“期望达成期限”应在设计中清楚展示为有效运行时长及暂停原因，不能让用户误以为固定墙钟截止时刻；具体命名和交互待确认。当前证据足够保存需求阶段记录，不足以进入 READY、业务实施或独立验收。

控制塔建议：首期复用中转服务已有的额度限制，不另建费用管理模块；独立的本机计算资源限制和 API 可用性探测仍待设计。此项是最小实现建议，不是对供应商计费行为的独立核验。

### 2026-09-13 范围、验收方式与 Git 留痕（历史）

- 仅修改本任务、CURRENT_TASK.md、PROJECT_STATE.md。保留其他八个工作台文件原始字节；先前 11 项文档变化已审阅并原样提交为 78165c8112779701cd3ace8c9cca88c9576a315d。没有新增稳定规则，因此不新增 CHANGELOG 条目。
- 允许将这两个阶段的分析文档提交并推送至 origin/codex/workbench-init；不覆盖远程已有工作，不强推，不直接更新 main，不创建或合并 PR，不打标签、不发布。若远程拒绝或出现不同历史，停止推送并记录阻塞。
- 完成条件：源文可读，历史/当前约定明确分开；R17-R42 可对应确认来源；当前状态、责任及入口一致；三个文件外无本次新增改动；原安全条款和旧验收证据不变；本地提交与远程工作分支 SHA 一致。
- 核验方式：逐条回读问答和文档，严格 UTF-8/空白/冲突标记/围栏/本地链接检查，八个保留对象逐字节对比，检查暂存路径、敏感模式和 git diff --check；提交后用 git rev-parse、git ls-remote 及 git status 验证。结果在实际执行后补入。
- 任务仍为 ANALYZING。文档自查不冒充独立验收；业务 pytest/ruff、端到端模拟、通知/自启、交易所接口、部署和实盘均不在本次执行范围。分支推送也不代表远程 CI 通过。
- 回退仅在另获授权后，以审阅过的反向补丁或新 revert 提交恢复文档；不重写已推送历史，不自动删除用户资料。

本次已执行的基线检查：Get-Location 为唯一根目录；git status --short --branch --untracked-files=all 为原有 11 项文档改动，暂存区初始为空；git remote -v 指向约定仓库；git ls-remote --symref origin HEAD refs/heads/main refs/heads/codex/workbench-init 退出 0，HEAD/main 为 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28，目标工作分支不存在。未执行 fetch/pull，未混入其他分支代码。

写前自查：11 文件 UTF-8、换行、空白、围栏、冲突标记检查 PASS，本地链接 41 处均可解析；暂存 11 文件白名单、原 AGENTS 完整前缀和定向敏感模式检查 PASS，git diff --cached --check 退出 0。随后 git commit 成功，git rev-parse --verify HEAD 返回上述文档快照完整 SHA。

本次失败或限制：read_thread 首次请求 turnLimit=30 被工具拒绝（最大为 10），未写入任何状态；改为每页至多 10 后成功回读。记忆关键词检索无匹配退出 1，不作为项目检查失败或通过证据。没有读取真实凭据、账户导出或旧项目目录。

### 2026-09-13 文档自查结果（历史）

2026-09-13 写后回读确认：新增 R17-R42 共 26 条，与 E01-E10 问答对照；R14-R16 及被撤回的建议有明确替代说明；从旧文“可观察目标”开始的历史正文逐字保留。当前契约与 CURRENT_TASK 的行首状态均唯一为 ANALYZING、责任均唯一为 00｜项目控制塔，TASKS 的当前任务 ANALYZING 与旧任务 CLOSED 未变。

Node 内置 fs/path/assert 与严格 UTF-8 解码检查 11 文件，UTF-8、末尾换行、空白、冲突标记、围栏及 42 个本地链接全部通过；比较写前内存字节快照，恰有三个授权文件变化，另八个文件原样保留。git diff --check 退出 0。源文和表格已回读，不冒充浏览器视觉验收或独立验收。

保留对象可按文档快照复核：AGENTS.md、CHANGELOG.md、docs/codex/WORKFLOW.md、docs/codex/TASKS.md、docs/codex/tasks/20260912-001-workbench-init.md，以及 decisions、migration、archive 的 README.md。八文件相对 78165c8112779701cd3ace8c9cca88c9576a315d 不应有差异。本次没有新增规则、代码、依赖、CI、配置或运行数据。

提交与推送前后须执行下列 Git 检查；远程操作尚未完成的部分不得提前视为通过，实际结果见后续推送回执：

```powershell
git diff --cached --check
git diff --cached --name-only
git diff --name-only 78165c8112779701cd3ace8c9cca88c9576a315d
git rev-parse --verify HEAD
git ls-remote origin refs/heads/codex/workbench-init refs/heads/main
git status --short --branch --untracked-files=all
```

### Git 推送回执（2026-09-13）

需求主体已提交并推送至 origin/codex/workbench-init。本节是在推送成功并独立读取远程引用后补记的实际证据，不是预期结果；本节自身随后作为文档回执提交同步，不重写前述提交。

| 命令或检查 | 实际结果 |
| --- | --- |
| git diff --cached --check；git diff --cached --name-only（需求提交前） | 空白检查退出 0；暂存范围恰为 CURRENT_TASK.md、PROJECT_STATE.md、本任务文件 |
| 定向暂存差异敏感模式检查；git diff --quiet 对比产品基线和八个保留对象 | PASS；未发现所检查的凭据模式；产品文件未变、八个保留文件未变；不是全账户或全磁盘安全审计 |
| git commit -m "docs: reconcile confirmed simulation requirements and open decisions" | 成功；3 文件，165 行新增、16 行删除，包含历史标识、当前契约、来源及检查记录 |
| git rev-parse --verify HEAD（需求主体提交后） | 98233df9c18f9f5625b3ecaea82566accad4a6cd |
| git diff --check a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28 HEAD | 退出 0 |
| git push --set-upstream origin HEAD:refs/heads/codex/workbench-init | 退出 0，新建远程工作分支并建立上游跟踪；未强推 |
| git ls-remote origin refs/heads/codex/workbench-init refs/heads/main（推送后） | 工作分支为 98233df9c18f9f5625b3ecaea82566accad4a6cd，与本地需求提交一致；main 仍为 a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28 |
| git status --short --branch --untracked-files=all（补写本回执前） | 仅 codex/workbench-init...origin/codex/workbench-init，无未提交或未跟踪文件 |

回执核对方法：`git log -1 --format=%H -- docs/codex/tasks/20260912-002-system-requirements.md` 可定位包含本节的最后提交，再用 `git rev-parse HEAD` 和上述 `git ls-remote` 命令核对最终本地与远程工作分支；本节不填写自身未来的提交号。提交树中的文档及上述需求主体 SHA 是交付证据，不要求依赖对话恢复内容。

未创建或合并 PR；main 未由本次操作更新。没有标签、部署、交易、后台服务、子代理或独立验收。未运行本地业务 pytest/ruff、浏览器渲染或远程 CI/保护核验，均不冒充通过。需求数值、规则冲突和异常细节尚未定稿，当前任务继续 ANALYZING，下一步仍是控制塔与用户细化指标。

## 2026-09-12 历史记录起点

以下保留上次整理的原始内容、确认编号 R01-R16、未决项、授权边界及检查结果，仅代表 2026-09-12 的快照。其中“本轮”“当前”“未提交”“禁止推送”和 ROI 续聊问题均属当日语境，不覆盖上方当前契约、七阶段路线及本次仅本地文档整理的授权。旧条目不删除；以替代对照表理解其效力。

## 可观察目标

形成一份可复核、经用户确认的个人自动化量化系统需求与模块边界基线，明确自主权限、风险约束、指标口径、异常行为及第一阶段边界，让后续设计不依赖聊天记忆。

本次只完成讨论资料落库和续聊入口。整个需求梳理尚未完成，不是最终设计、可实施规格或业务验收结论。

## 来源与证据等级

1. 当前讨论：用户要求先讨论、不写代码、每次一个关键问题；最新授权为“今天的讨论先到这里，把资料整理一下明天再继续”。这只授权本轮文档整理。
2. 用户指定的旧线程：`codex://threads/01a084fd-5247-7210-a498-43d9ae564af0`，实际标题 `Clarify task request`。本线程此前已回读其需求、评审及阶段总结；历史结论只按历史记录保存，不等于本轮重新验收。
3. 当前仓库：已读 [AGENTS.md](../../../AGENTS.md)、[工作流](../WORKFLOW.md)、[项目状态](../PROJECT_STATE.md)、[架构](../../architecture.md)、[安全模型](../../safety-model.md)、[发布政策](../../governance/RELEASE_POLICY.md)。这些是现行文件声明，不证明运行可用。

下文区分用户确认的需求、旧线程记录、当前仓库事实、待确认建议。需求确认不代表实现完成或现行规则已经改变。

## 项目的来龙去脉：历史记录

| 日期 | 旧线程记录的推进 | 不能据此推出的结论 |
| --- | --- | --- |
| 2026-09-09 | 从部署 Freqtrade 和合约自动交易起步，尝试下单与界面记录 | 会下单不等于交易、核算、界面和安全约束已形成完整闭环 |
| 2026-09-10 | 研究过去一年的交易行为、盈利与亏损画像，结合行情及交易成本提出候选；记录过测试失败的候选 | 历史表现或少量结果不能代替当前流程的完整策略证据 |
| 2026-09-11 | 用户提出先打通受控实验闭环、持续研究策略；评审推动研究与执行隔离，需求文档演进至 V0.4，并讨论规则表修改基线 | 旧 V0.4 曾延后 AI 策略生成，不是本轮已确认的第一阶段范围 |
| 2026-09-12 | Git 治理、测试、证据、批准和冻结版本进入主线；旧线程最后记录 PR #5 已合并、PR #7 等待批准，尚未启动长期 Dry-run | PR、CI 和旧线程声明不等于本轮远程状态、部署状态或实盘授权 |
| 当前讨论 | 明确个人使用、自主研究与模拟运行，以及权限、停机、通知、品种范围和 ROI 目标配置 | 讨论不是业务开发授权，未决项不能自动补成已确认规则 |

旧线程提到的资料包括 `D:/量化交易/客户需求说明.md`（V0.4）、`D:/量化交易/量化实验规则表_V0.1.xlsx`、V0.2 修改基线及早期研究记录。本轮没有打开旧目录、复制这些文件、读取账户导出或核验原始研究数据；V0.2 不能仅因讨论过就记成已生成文件。

修正此前不完整的项目认识：项目历史早于当前 Git 仓库，已有早期需求和研究；“当前仓库未收录”不等于“此前从未做过”。失败候选归档、完整费用核算和实验可追溯是旧讨论的重要方向，具体归档与核算规格仍待本任务确认。

`migration/` 当前只有说明文件，没有旧线程生成的正式交接记录。本文件是控制塔对指定历史的整理，不冒充旧线程交接，也不宣称历史工件已全部迁移。

## 需求背景与此前对齐方向

- 短期用户是本人，核心痛点是情绪化扛单和亏损后扩大风险；目标是减少人工参与，而非仅增加提醒。
- 产品希望覆盖研究、提出假设、生成候选策略、验证、筛选、执行和复盘，而不只是自动下单。
- 此前上下文已对齐第一阶段先模拟、暂不动用真实资金；新策略或新版本进入实盘仍需单独批准。
- 系统可以在授权范围内自主调整，但不能自行突破用户授权的硬风险上限。
- 没有候选达标时告知用户，由用户决定继续研究还是改变筛选标准；系统不得自行降低标准。改变筛选标准不等于放宽风险上限，变更后须重新验证。
- “没有合格策略”和“证据不足”可以是研究结论；用户希望用 ROI 衡量收益目标，不构成收益承诺，也不自动替代技术正确性与风险约束。

以上承接本线程此前的需求回顾。以下表格单列本轮连续问答中已经明确的具体偏好，避免把旧方案的限制自动继承为新需求。

## 本轮用户确认的需求契约

| 编号 | 已确认需求 | 用户确认依据及边界 |
| --- | --- | --- |
| R01 | 模拟环境中，候选策略通过预先确认的独立验证后，可以自动切换，不逐次请求批准 | 对自动切换问题回答“希望”；不包含实盘授权，独立验证的具体标准和权限设计尚未定稿 |
| R02 | 正常换策略时，旧策略停止开新仓，按原规则管理已有模拟仓位，全部退出后再启用新版本 | 回答“是”；不等于紧急风险事件也必须等待正常退出 |
| R03 | 仅在模拟阶段，主动降低的风险额度可以在满足预先约定的恢复条件后自动恢复到原授权水平 | 对解释后的问题回答“允许”；不允许超过原上限或自行放宽恢复条件。解释中的 100/30/200 只是示例，不是需求数值 |
| R04 | 模拟账户触发最大累计亏损上限后停止开新仓，并立即平掉全部模拟持仓，随后保持停机 | 先回答“停止”，再确认立即平仓；具体数值、核算口径、执行失败行为待定 |
| R05 | 这类停机必须由用户明确重新授权才能恢复 | 回答“是”；不能清零亏损记录、重启或换策略来绕过停机，不得套用 R03 的普通风险恢复 |
| R06 | 交易停机期间，研究可以继续生成、测试和筛选候选 | 回答“是”；找到合格新策略也不能自动解除交易锁 |
| R07 | 使用 Windows 原生系统通知，提醒停机、无合格策略和待授权事项 | 用户指定“电脑 Windows 自带的提醒功能”；当前不扩展邮件、微信等渠道 |
| R08 | 通知只负责提醒；用户进入系统查看原因和证据，再明确确认恢复或标准变更 | 回答“是”；收到、关闭或点击通知本身都不是批准 |
| R09 | 关闭主窗口后，后台研究、模拟交易、风控和通知继续运行 | 回答“是”；关闭界面不等于退出后台，主动完全退出的交互尚未讨论 |
| R10 | Windows 开机后自动启动，先恢复记录并核对状态；停机锁跨重启保留，核对失败不恢复模拟交易 | 回答“是”；这不授权本次设置系统自启，休眠和掉电后的细节待定 |
| R11 | 第一阶段候选池覆盖币安全部可交易的 USDT 本位永续合约，由系统筛选 | 先说“上架的全部”，再明确同意 USDT 本位永续范围；不扩大为所有合约类型，也不要求所有品种同时持仓 |
| R12 | 新上架的同类合约自动进入研究候选池，满足数据和验证标准后才参与模拟交易 | 回答“是的”；数据不足或验证未通过不得交易，下架及暂停交易的处理尚未确认 |
| R13 | 用户不预先指定固定的同时持仓数量，希望系统根据目标自主决定 | 用户原话“这个没规定啊，能够达成目标就行”；不等于无限风险，算法与账户总风险约束尚未形成可实施规格 |
| R14 | ROI 是核心收益指标 | 用户指定“roi指标”；尚未确认账户/单笔口径、分母、浮盈浮亏、费用和资金流处理 |
| R15 | ROI 考核周期目前取 2 小时 | 用户回答“2小时吧”；固定窗口还是滚动窗口、能否配置未确认，周期结束的交易动作未确认 |
| R16 | 目标 ROI 是用户在系统中自行填写的变量，不写死收益数字 | 用户两次明确“这个是一个变量，用户自己填写”；数值范围、校验、修改何时生效及历史目标保留尚未确认 |

约束说明：2 小时 ROI 考核不能自动被解释为必须交易、必须清仓、必须盈利或可以提高风险。当前也没有确认每两小时重置累计亏损、模拟资金或停机锁。

## 模块边界：待用户确认的草案

沿用仓库已有概念做逻辑分工，不承诺拆成独立服务、多个进程或新的平台。

| 模块 | 建议职责 | 建议边界 |
| --- | --- | --- |
| 研究 | 数据整理、行为画像、提出假设、生成候选与新版本 | 不直接修改执行中的版本，不拥有真实下单权限 |
| 验证 | 回测、独立数据检验、成本与异常检查，输出结论和证据 | 生成策略的一方不能自行改准入标准或绕过结果；隔离机制待确认 |
| 发布 | 绑定版本、参数、证据及获准用途，管理模拟切换与实盘批准 | 模拟资格不得自动升级为实盘资格；须解决与现有发布政策的冲突 |
| 执行 | 运行指定版本、处理订单及持仓状态、反馈结果 | 不自行改写策略；正常切换与累计亏损紧急停机走不同流程 |
| 风控 | 执行账户约束、降低及按条件恢复风险额度、停机锁定 | 普通恢复不能解锁累计亏损停机，也不能扩大授权上限 |
| 核算与审计 | 记录成交、费用、收益、版本、调整和失败原因 | 不只保存成功结果，不允许通过删记录改变停机或考核事实 |
| 用户界面与通知 | 展示原因和证据、填写 ROI 目标、接收明确授权、Windows 提醒 | 通知不批准；后台与界面生命周期区分；前端形式尚未决定 |

这是后续讨论草案，不是已完成架构审批。没有选择新的技术栈、数据库、桌面框架或 AI 服务供应商。

## 与现行规则的未决冲突

| 冲突 | 当前文件或旧方案 | 本轮需求方向 | 当前处理 |
| --- | --- | --- | --- |
| 自主恢复风险 | AGENTS 第 4 条禁止自动增加资金、杠杆、持仓数量、币种范围或亏损限额 | R03 希望模拟阶段可恢复已降低的风险额度 | 只记录需求；不能把“恢复”改名后直接绕过禁止条款 |
| 持仓与品种范围 | 安全模型规定最多一个仓位；现行规则禁止自动扩大币种范围 | R11-R13 为全部 USDT 永续、新上架自动纳入、持仓数自主决策 | 尚未修改现行固定限制；研究范围与实际下单权限须分别定义 |
| 自动模拟发布 | 当前发布政策要求匹配冻结版本、证据、批准 PR 和人工 release-approved 行为 | R01 希望验证通过后自动模拟切换 | 自动研究结果不能冒用现有发布身份，需设计模拟专属授权边界 |
| 第一阶段能力 | 旧 V0.4 曾将 AI 生成策略延后，先做人工固定策略 | 本轮希望自主研究与生成进入模拟闭环 | 保留历史依据，以用户最新方向继续收敛，不能直接照搬旧阶段范围 |
| 运行版本变化 | 架构规定执行结果不能原地修改正在运行的策略，变更须新版本全程验证 | 希望系统自主调整策略和风控 | 区分版本升级、获准参数调整与账户风险控制，避免把自主性等同于任意热修改 |
| 数据研究来源 | 项目 AGENTS 第 1 条禁止读取真实凭据或账户导出 | 旧项目曾用个人交易数据做画像 | 本轮不读取旧导出；未来须明确合法且符合项目边界的数据输入方式，不能沿用旧授权 |

本文件不是 AGENTS、安全模型或发布政策的修订。没有获得现行规则变更和业务实施授权之前，继续执行原规则；不得降低安全要求。稳定规则如需变更，应另行说明原因、影响和验证，并按工作流记录 CHANGELOG，而不是在本任务中悄悄放宽。

## 未确认问题与下一步顺序

## 2026-09-14 任务暂挂

用户确认先处理独立的远程 Git 仓库治理任务 `20260914-003`。本任务保持 `ANALYZING`，不改变已确认契约，不进入实施或验收；治理任务完成并核对远程结果后，恢复本节以下的第二期细节分析顺序。

暂挂期间保留本任务全部需求、冲突、历史证据和未决问题。当前任务指针以 [CURRENT_TASK.md](../CURRENT_TASK.md) 为准。

1. 首先回答本文件开头的续聊问题：2 小时周期 ROI 未达标时如何处理。
2. ROI 的核算口径：账户或单笔、资本分母、浮动盈亏、交易成本、资金流、窗口起点及未满周期如何展示。
3. 目标 ROI 修改的生效时点、输入校验，以及无法满足目标时的告知方式；收益目标与策略准入标准不可混为一项。
4. 累计亏损上限的数值、计量区间、重授权后的预算处理；考核周期不等于亏损预算周期。
5. 总风险授权如何表达，持仓数自主决策、相关持仓、杠杆及额度恢复的边界和恢复条件。
6. 独立验证标准、评估数据隔离、策略生成方是否能修改评估规则，以及反复试验的证据保存。
7. 多策略并行需求、切换条件和切换频率；当前正常交接规则不代表已经允许并行执行。
8. 下架、行情缺失、断网、异常退出、休眠及重启后的状态核对；紧急平仓失败/部分成功的恢复与告警。
9. UI 形式、完整退出方式、模拟资金初值、研究数据来源、计算资源预算及技术复用范围。

这些是后续问题清单，不是本次同时询问用户。不要为补齐文档而猜定数值、配置、交易规则或自动回复。

## 本次修改范围与保持不变项

本轮只允许控制塔整理分析材料：本文件、[TASKS.md](../TASKS.md)、[CURRENT_TASK.md](../CURRENT_TASK.md)，以及 [PROJECT_STATE.md](../PROJECT_STATE.md) 对“初始化关闭时无活动任务”这一历史快照的澄清与当前指针引用。项目状态不新增未经验证的产品结果。

保留全部既有修改。产品代码、测试、依赖、CI、策略、风险数值、安全规则、共享工作流、发布政策、旧任务与已有验收证据、历史目录和数据保持不变。不创建真实发布清单，不读凭据或账户导出，不连接交易所，不运行或设置后台服务、自启和通知，不创建子代理，不安排自动继续或提醒，不提交、推送、合并、部署或清理资源。

## 验收标准与停止条件

本次阶段记录的检查标准：

1. 明天仅凭当前任务入口即可找到已确认需求、历史来源、冲突、未决问题和续聊位置。
2. R01-R16 能对应本轮用户确认，不把例子当数值，不把讨论当实现或收益保证。
3. 索引、当前任务及本文件仅有一个当前状态 ANALYZING 和一个责任线程；旧任务仍 CLOSED。
4. 修改只在上述四文件；保留对象未变化；文档可读、链接可解析、无冲突标记和行尾空白。

需求任务进入 READY 的条件尚未满足：需要用户确认指标口径、异常行为、风险和自主权限的可执行边界，并确认模块草案及下一阶段范围。不得因本文件生成就进入实施或标记 ACCEPTED/CLOSED。

用户已要求本日讨论停止，文档核对后停止推进；下次由用户主动继续。不存在应在后台自动消化的业务开发任务。

## 分析记录、命令与检查结果

2026-09-12 登记：此前没有活动任务；按用户已提出的需求梳理和本次整理授权，登记 DRAFT 后进入 ANALYZING。只写分析交接资料，不进入 IN_PROGRESS，不跳过 READY 进行产品实施。责任仍为当前控制塔，未创建分析、实施或验收子任务。

| 命令或检查 | 本轮实际结果 |
| --- | --- |
| Get-Location | 本文件唯一根目录 |
| git rev-parse HEAD | a5fdc77ccb54f5d4d3d704bf4a78cb0166942c28 |
| git status --short --branch --untracked-files=all（写入前） | codex/workbench-init；AGENTS.md 已修改，CHANGELOG.md 及 8 个 docs/codex 文档未跟踪，共 10 个既有改动对象 |
| git remote -v | origin fetch/push 与本文件基线一致；未 fetch |
| 父级、项目及文档层 AGENTS 检查 | 适用 D:/CodexProjects/AGENTS.md 与项目 AGENTS；D:/AGENTS.md、projects/AGENTS.md、docs/AGENTS.md、docs/codex/AGENTS.md、tasks/AGENTS.md 均不存在 |
| 共享和项目 WORKFLOW、状态、索引、指针、旧任务、migration 回读 | 旧任务 20260912-001 CLOSED；migration 无正式交接；新任务编号 20260912-002 未占用 |
| git diff -- AGENTS.md（写入前） | 只有先前追加的工作流入口，不是本轮修改 |
| 写前保留对象快照 | 在工具内存中读取既有 10 个工作台文件并计算 SHA256，未额外创建备份文件或读取业务数据 |

### 文档自查结果（2026-09-12）

- 完整回读本文件，将已确认需求与本轮问答对照：R01-R16 共 16 项，ROI 目标没有被填入固定数值；未达标处理保留未确认；模块草案和现行规则冲突分开列示。未把历史 PR/测试报告当成本轮验收。
- 以 Node 内置 fs、assert、path、crypto 和严格 UTF-8 解码检查本轮四文件：输出 PASS，UTF-8、末尾换行、行尾空白、冲突标记和围栏检查 4/4；本地链接解析 24 处，全部存在。未依赖业务环境，未新增依赖或检查脚本文件。
- 对写前内存快照进行逐字节比较：既有文件仅 CURRENT_TASK.md、TASKS.md、PROJECT_STATE.md 改变，新建本任务文件；其余 7 个既有文件逐字节保持一致。项目状态的两处修改仅澄清初始化快照和指针，不改变验收结论。
- 索引、当前任务和任务顶部的当前状态均为 ANALYZING；两处当前责任均为 00｜项目控制塔；旧任务索引仍 CLOSED，旧任务文件未修改。
- git diff --check HEAD 退出 0；因工作台文件尚未跟踪，另做上述逐文件检查，不把普通 diff 为空当作新增文档通过。
- 最终 git status --short --branch --untracked-files=all 为原有 10 个改动对象加本任务文件，共 11 个；git diff --name-only HEAD 仍只有先前的 AGENTS.md；git rev-parse HEAD 与登记基线一致。七项字节对比证明该 AGENTS 改动不是本轮新增。

保留对象的写前 SHA256 如下，最终字节对比均一致；可在唯一根目录对表中路径执行 `Get-FileHash -Algorithm SHA256 -LiteralPath <路径>` 复核：

| 保留文件 | SHA256 |
| --- | --- |
| AGENTS.md | 298c902bdcd158323f96b370440d4c90b701c2e6d60de204895556c0dc7c03f5 |
| CHANGELOG.md | a6ec2f8b86ea4e851682c8613b231611cf30385eca8a13c1eec3b6166707f7be |
| docs/codex/WORKFLOW.md | 6c9abb70a7d2e1ff8f59dc8221db4b0c1497f3d050e15a34854b129150d2a19c |
| docs/codex/archive/README.md | 9a31d7f77739092929c9006bbb2862dfe9c1d4a64916fe997cb677d9c8a2a9a0 |
| docs/codex/decisions/README.md | 2868571e0596a6f46ea69291d0df8730fca29ee10835a3623e0c9dc345d6dda5 |
| docs/codex/migration/README.md | e3479b98e877db7c01ca34aabdb9886208d811db2e797221d16cc266d33e47b6 |
| docs/codex/tasks/20260912-001-workbench-init.md | 00f8f5b7c3aeaae568321f1fcf14beb2220aa09614367e9f0221415069fc5f4d |

本轮没有失败的文件写入或检查。文件搜索、记忆相关性搜索的无匹配退出 1 仅表示没有匹配项，不作为业务失败或业务通过证据。没有修改稳定工作流规则，因此不改 CHANGELOG。

## 实施、独立验收与限制

业务实施：未开始。本轮授权范围内的分析资料写入不构成业务实现。

独立验收：未进行；用户未授权创建子代理，本任务未进入 READY_FOR_VERIFY。用户尚未复核整份整理稿，部分需求待继续讨论，不关闭任务。

未执行：业务 pytest/ruff、运行场景、Windows 通知/自启/后台测试、交易或实时 ROI 验证、远程 PR/CI/保护检查、旧文档及研究数据原件核验。Markdown 只做源文、结构、链接检查，不宣称浏览器视觉验收或真实交易通过。

当前风险：新需求与现行安全规则未消解；ROI 和风险口径未完备；旧工件未迁移；本地文档未提交，GitHub 尚无本次交接。任何后续实施前必须重新确认边界和 READY 条件。

回退：仅在另获授权后，对本轮四文件使用经审阅的反向补丁，保留之前修改与旧任务证据；不自动删除文件或使用破坏性 Git 命令。

## 2026-09-16 实施交回：1.0 候选验证入口

### 实施状态

- 本切片状态：`READY_FOR_VERIFY`。
- 实施责任线程：`20｜实施｜20260912-002 1.0 候选验证入口`（`01a0a983-8717-71e0-aaec-098a71a8741b`）。
- 下一责任线程：待创建独立 `30｜验收｜20260912-002 1.0 候选验证入口`；实施线程不自行验收或关闭任务。
- 实施基线：`ba8738fe18289398a8c6713772416facddb5532d`，分支 `codex/workbench-init`；开始时已存在的文档改动和未跟踪任务文件均保留，未覆盖。

### 可观察结果

给定一个标准 `IStrategy`、最终无敏感 Freqtrade 配置快照、风险配置、数据集身份和规则版本，新增 `quant_lab.validation` 模块及 `python -m quant_lab.validation` 入口，按固定顺序调用 Freqtrade 原生命令：

1. `list-strategies` 加载检查。
2. `lookahead-analysis` 未来数据检查。
3. `recursive-analysis` 递归指标检查。
4. `hyperopt` 参数搜索检查，固定单进程并关闭参数回写。
5. 开发区间 `backtesting`。
6. 留出区间 `backtesting`。

每项命令使用无 shell 的参数数组、独立的输出目录下 Freqtrade 用户目录，并分别保存 stdout/stderr 和返回码。入口生成机器可读的 `validation-report.json`、`evidence.json` 和带 SHA-256 摘要的 `release-manifest.toml`；报告同时记录候选 ID、数据集身份、规则版本、失败项和证据路径。

命令失败、异常或证据不完整时不假装通过；本切片即使六项技术命令全部成功，也因统计/经济准入阈值尚未由后续策略准入实现提供而输出 `evidence_insufficient`。清单中的 `approval_pr_number = 0` 明确表示尚未批准，不授权 Dry-run 或实盘。

为绑定实际加载的最终配置，`release.py` 的 `ReleaseManifest` 新增可选 `runtime_config` 制品摘要。旧的三制品清单仍可读取和校验；出现该字段时，发布校验会检查其路径唯一性、存在性和 SHA-256，防止配置被替换后继续沿用旧证据。

### 本线程修改对象

- `src/quant_lab/validation.py`：候选验证编排、证据保存、冻结清单和 CLI。
- `src/quant_lab/release.py`：增加可选运行配置摘要并纳入制品完整性校验。
- `tests/test_validation.py`：全成功记录、失败证据和不合格结论保持证据不足的回归测试。
- `tests/test_release.py`：运行配置摘要篡改被拒绝的回归测试。
- `tests/fixtures/validation_cli/CliCandidate.py`、`freqtrade.json`、`risk.toml`：仅用于真实 CLI 加载的合成策略和无凭据样例；不代表业务策略、真实账户或运行授权。
- `docs/codex/TASKS.md`、`docs/codex/CURRENT_TASK.md`：同步为 `READY_FOR_VERIFY` 并指向独立验收。

### 实施验证证据

| 命令 | 实际结果 |
| --- | --- |
| `.venv\Scripts\python.exe -m pytest tests/test_validation.py`（首轮） | 先以 `ModuleNotFoundError: quant_lab.validation` 收集失败，确认测试先于生产代码；实现后该文件 3 项通过 |
| `.venv\Scripts\python.exe -m pytest tests/test_validation.py tests/test_release.py` | `13 passed in 7.42s` |
| `.venv\Scripts\python.exe -m pytest` | `18 passed in 7.01s` |
| `.venv\Scripts\ruff.exe check .` | `All checks passed!` |
| `.venv\Scripts\python.exe -m pip check` | `No broken requirements found.` |
| `.venv\Scripts\python.exe -m quant_lab.validation --help` | 退出码 `0`，参数帮助正常输出，无模块重复加载警告 |
| `.venv\Scripts\python.exe -m compileall -q src tests` | 退出码 `0` |
| `.venv\Scripts\freqtrade.exe list-strategies --no-color -c tests/fixtures/validation_cli/freqtrade.json --userdir tests/fixtures/validation_cli --strategy-path tests/fixtures/validation_cli --one-column` | Freqtrade `2026.8` 输出 `CliCandidate`，退出码 `0`；只做合成策略加载，未运行 Dry-run、未下载行情、未读取凭据或账户导出 |
| `.venv\Scripts\python.exe -m quant_lab.validation` CLI 冒烟（将 `--freqtrade` 指向不存在的可执行文件） | 输出 `evidence_insufficient` 及报告/清单路径，并返回非零；六项失败证据均落盘，未执行交易命令 |
| `git diff --check` | 退出码 `0` |
| 严格 UTF-8、末尾换行、行尾空白、冲突标记和新增实现敏感字段扫描 | `text-integrity: PASS`；敏感字段扫描 `no matches` |

注：敏感字段扫描第一次尝试误用了 Unix `||`，PowerShell 因语法不支持而未执行检查；随后使用 PowerShell 原生退出码判断重跑，结果为 `no matches`，不把第一次命令当作通过证据。

### 未执行、限制与风险

- 没有用真实历史数据运行完整回测、Hyperopt、lookahead 或 recursive 链；仓库没有获准的合成 OHLCV 数据集，且本切片禁止下载数据。真实 CLI 验收仅覆盖标准策略加载场景；其余命令已由固定编排和失败证据测试覆盖。
- 没有启动 Freqtrade Dry-run 或交易服务，没有连接交易所，没有创建真实凭据，没有开发 Web、轮次监督、Windows 宿主或 1.1 候选并发试跑。
- 本切片不判定策略盈利、不提供统计/经济阈值评估、不提供正式批准；当前任何候选都不能因该报告进入正式运行。
- 本线程未提交或推送 Git；远程和 `main` 保持不变。

### 交接结论

切片目标已完成到可独立验收的程度，状态流转为 `READY_FOR_VERIFY`。验收线程应独立核对固定命令顺序、输出文件和摘要绑定、失败关闭、旧清单兼容性、合成 CLI 加载以及上述未执行项；验收失败应回写 `REWORK`，不得在验收线程中直接修复。

## 独立验收结果（2026-09-16）

### 结论

- 状态：`REWORK`。
- 验收线程：`30｜验收｜20260912-002 1.0 候选验证入口`（`01a0a99d-1d9e-75e0-bb73-0ca9c6e35068`）。
- 验收基线：目录 `D:/CodexProjects/projects/quant-research-live-lab`，分支 `codex/workbench-init`，HEAD `ba8738fe18289398a8c6713772416facddb5532d`；与任务要求一致。
- 未修改产品实现、测试、配置或治理规则；未提交、未推送、未启动 Dry-run、未连接交易所、未下载行情、未读取真实凭据或账户数据。

### 已通过检查

| 检查 | 结果 |
| --- | --- |
| `.venv\Scripts\python.exe -m pytest tests/test_validation.py tests/test_release.py` | `13 passed in 8.80s` |
| `.venv\Scripts\python.exe -m pytest` | `18 passed in 8.78s` |
| `.venv\Scripts\ruff.exe check .` | `All checks passed!` |
| `.venv\Scripts\python.exe -m pip check` | `No broken requirements found.` |
| `.venv\Scripts\python.exe -m compileall -q src tests` | 退出码 `0` |
| `git diff --check` | 退出码 `0` |
| `python -m quant_lab.validation --help` | 退出码 `0` |
| Freqtrade `2026.8` 合成 `CliCandidate` 的 `list-strategies` 加载 | 输出 `CliCandidate`，退出码 `0` |
| 旧三制品 manifest 兼容、runtime_config 篡改拒绝、`approval_pr_number=0` 拒绝 | 通过测试和独立核对 |
| 缺失 Freqtrade 可执行文件的失败冒烟 | 六项均记录异常，结论 `evidence_insufficient`，返回非零 |
| 敏感路径及敏感赋值模式扫描 | 无匹配；当前差异未发现凭据、账户数据或越权产品文件 |

### 必须返工的问题

1. **三制品没有完整互相绑定。** 期望 `validation-report.json`、`evidence.json` 和 `release-manifest.toml` 都能通过候选身份、路径和 SHA-256 相互核验。实际生成的 manifest 只有 `strategy`、`runtime_config`、`risk_config` 和 `evidence_bundle` 四个制品表，没有 `validation-report.json` 的制品摘要；报告只保存候选身份和 `evidence_path`/`manifest_path`，没有 SHA-256 字段。独立产物核对结果为 `manifest_evidence_sha_matches=True`、`report_has_sha256=False`。因此报告可以被替换而不触发当前 manifest 的制品校验。
2. **成功但无证据时没有失败关闭。** 使用临时目录和合成 runner 验证：`list-strategies` 返回请求策略名且其余五个命令返回码为 `0`，但其 stdout/stderr 均为空。当前结果为 `technical_checks_passed=True`、`failed_checks=()`，空的 `lookahead.stdout.txt` 被接受。按本切片要求，证据缺失必须输出失败关闭状态，不能把返回码成功当作证据完整。

### 限制和下一步

- 未运行真实历史数据链、完整回测、Hyperopt、lookahead 或 recursive；没有启动交易服务，故本次不判定策略盈利、统计/经济门槛或 Dry-run 可用性。
- Freqtrade CLI 真实检查仅覆盖合成策略加载；其余顺序和失败路径由代码与定向测试检查。
- 返工应补充上述两项的失败测试和最小实现，再重新回到 `READY_FOR_VERIFY`，由新的独立验收重新核对；验收线程不自行修复。

## 2026-09-16 实施返工交回

### 状态与范围

- 状态：`READY_FOR_VERIFY`。
- 实施线程：`20｜实施｜20260912-002 1.0 候选验证入口`（`01a0a983-8717-71e0-aaec-098a71a8741b`）。
- 下一责任线程：待创建独立 `30｜验收｜20260912-002 1.0 候选验证入口`；实施线程不自行验收或关闭任务。
- 返工只处理独立验收列出的两项问题；未开发 Web、轮次监督、Windows 宿主或 1.1，未提交或推送。

### 修复结果

1. `release-manifest.toml` 现在可选记录 `[artifacts.validation_report]` 的相对路径和 SHA-256。`release.py` 会在读取后检查该制品的路径安全性、存在性、路径唯一性和内容哈希；旧的三制品清单没有该表时仍可读取和校验。
2. 六项命令返回码为 `0` 时必须提供最低可验证证据：策略加载要求输出请求策略名；lookahead 要求非空 CSV；recursive 要求 Freqtrade 递归分析结果标记；hyperopt 要求非空 `.fthypt` 结果文件；两个 backtest 要求对应隔离目录中的非空 JSON 结果文件。任一证据缺失或为空，该检查失败，`technical_checks_passed` 保持 `false`，并写入 `failed_checks`。
3. 报告新增 `evidence_sha256`，可核对 `evidence.json`；报告最终内容的 SHA-256 由清单记录，因此替换 `validation-report.json` 会被现有发布校验拒绝。

### TDD 证据

先新增三条回归断言并运行：

| 命令 | 实际结果 |
| --- | --- |
| `.venv\Scripts\python.exe -m pytest tests/test_validation.py tests/test_release.py -k "binds_report_in_manifest or zero_exit_with_empty_required_evidence or validation_report_artifact_tampering"` | 先红灯：3 failed，分别复现缺少报告制品字段、空证据被接受和报告篡改未产生制品哈希违规 |
| 同一命令（修复后） | `3 passed, 13 deselected` |
| `.venv\Scripts\python.exe -m pytest tests/test_validation.py tests/test_release.py` | `16 passed` |

### 验证证据

| 命令或场景 | 实际结果 |
| --- | --- |
| `.venv\Scripts\python.exe -m pytest` | `21 passed in 10.51s` |
| `.venv\Scripts\ruff.exe check .` | `All checks passed!` |
| `.venv\Scripts\python.exe -m pip check` | `No broken requirements found.` |
| `.venv\Scripts\python.exe -m compileall -q src tests` | 退出码 `0` |
| `git diff --check` | 退出码 `0` |
| `.venv\Scripts\python.exe -m quant_lab.validation --help` | 退出码 `0`，CLI 帮助可加载 |
| Freqtrade `2026.8` 合成 `CliCandidate` 的 `list-strategies` | 输出 `CliCandidate`，退出码 `0`；只验证策略加载 |
| 编排 CLI 冒烟，`--freqtrade __missing_freqtrade__` | 返回 `2`；`technical_checks_passed=False`，六项均在 `failed_checks`；清单存在 `[artifacts.validation_report]`；未执行 Freqtrade 命令 |
| 直接篡改报告的发布校验回归测试 | 产生 `validation_report_hash_mismatch`，并拒绝发布 |

### 未执行、限制与剩余风险

- 未下载行情、读取真实凭据或账户数据、连接交易所、启动 Freqtrade Dry-run 或交易服务。
- 未运行真实历史数据的回测、Hyperopt、lookahead 或 recursive 链；仓库没有获准的合成 OHLCV 数据集，本切片不以空数据伪造业务结果。
- 本切片仍不判断盈利、统计/经济准入、不提供正式批准；`approval_pr_number=0` 和现有发布审批边界保持不变。
- 旧清单兼容性由现有三制品测试覆盖；新报告制品和空证据关闭由本次新增测试覆盖，仍需独立验收线程从任务标准重新核对。

### 交接结论

返工目标已完成到可独立验收程度，任务交回 `READY_FOR_VERIFY`。验收线程应独立复核报告制品绑定、旧清单兼容、六项最低证据、失败关闭和未执行边界；不得在验收线程中直接修复。

## 第二次独立验收结果（2026-09-16）

### 结论

- 状态：`REWORK`。
- 验收线程：`30｜验收｜20260912-002 1.0 候选验证入口返工复验`（`01a0a9e9-8fe3-7823-873c-e15b95fc6441`）。
- 验收基线：目录 `D:/CodexProjects/projects/quant-research-live-lab`，分支 `codex/workbench-init`，HEAD `ba8738fe18289398a8c6713772416facddb5532d`；与任务要求一致。
- 验收线程未修改产品实现、测试或配置；仅回写本任务、`CURRENT_TASK.md`、`TASKS.md` 的状态与证据；未提交、未推送、未启动 Dry-run、未连接交易所、未下载行情、未读取真实凭据或账户数据。

### 已通过检查

| 检查 | 实际结果 |
| --- | --- |
| `.venv\Scripts\python.exe -m pytest tests/test_validation.py tests/test_release.py` | `16 passed in 12.36s` |
| `.venv\Scripts\ruff.exe check .` | `All checks passed!` |
| `.venv\Scripts\python.exe -m pip check` | `No broken requirements found.` |
| `.venv\Scripts\python.exe -m compileall -q src tests` | 退出码 `0` |
| `git diff --check` | 退出码 `0` |
| Freqtrade `2026.8` 合成 `CliCandidate` 加载 | 输出 `CliCandidate`，退出码 `0` |
| 独立全零退出、空证据场景 | `technical_checks_passed=False`；五个缺证据检查全部列入 `failed_checks` |
| 独立报告篡改场景 | 未篡改时允许；篡改后拒绝并出现 `validation_report_hash_mismatch` |
| 缺失 Freqtrade 可执行文件真实 CLI 冒烟 | 返回 `2`；六项均失败；清单含 `[artifacts.validation_report]` 且 `approval_pr_number = 0` |
| 敏感字段扫描 | 无凭据、账户数据或明显敏感赋值匹配 |

### 必须返工的问题

1. **旧证据可被当前候选复用。** 期望：六项命令返回 `0` 时，证据为空、缺失或无法证明属于本次最终候选，必须失败关闭。实际：独立临时目录预置旧候选的非空 `lookahead-analysis.csv`、Hyperopt `.fthypt` 和两份回测 JSON；本次 runner 对这些命令均返回 `0` 但不生成新证据，`load` 返回当前 `Candidate`，`recursive-analysis` 返回当前命令标记。结果仍为 `technical_checks_passed=True`、`failed_checks=()`。因此现有非空检查没有排除陈旧或不可归属当前候选的制品。

### 限制和下一步

- 未运行真实历史数据的回测、Hyperopt、lookahead 或 recursive 链；未验证盈利、统计/经济门槛或 Dry-run 可用性。
- 实施线程需补充“本次运行证据身份/新鲜度绑定”的失败测试和最小修复，完成后重新交回 `READY_FOR_VERIFY`，再由新的独立验收复验。验收线程不自行修复。

## 2026-09-16 第二次返工实施交回

### 状态与范围

- 状态：`READY_FOR_VERIFY`。
- 实施线程：`20｜实施｜20260912-002 1.0 候选验证入口`（`01a0a983-8717-71e0-aaec-098a71a8741b`）。
- 下一责任线程：待创建新的独立 `30｜验收｜20260912-002 1.0 候选验证入口返工复验`；实施线程不自行验收或关闭任务。
- 基线保持不变：`D:/CodexProjects/projects/quant-research-live-lab`、`codex/workbench-init`、HEAD `ba8738fe18289398a8c6713772416facddb5532d`。
- 本次只修复“旧制品被当前运行复用”这一项；保留报告/清单 SHA-256 绑定、旧三制品兼容、`approval_pr_number = 0`、无 shell 调用和原有失败关闭边界。未开发 Web、轮次监督、Windows 宿主或 1.1，未提交或推送。

### 根因修复

- 每次验证在请求输出目录下创建新的 `runs/<candidate-id>-<随机运行标识>/` 隔离目录，并把本次六项命令的 Freqtrade userdir、证据目录和命令专属输出全部指向该目录。
- 新运行目录使用独占创建，不依赖粗粒度文件修改时间，也不读取请求输出目录下的历史 `evidence`、`freqtrade-userdir` 或结果文件；旧制品保留且不会被本入口清理或覆盖。
- `evidence.json` 与 `validation-report.json` 记录同一 `run_id`，各项证据路径记录当前隔离目录；因此命令返回 `0` 但没有在本次目录产生最低证据时，检查结果仍失败关闭。

### TDD 与直接冒烟证据

先新增“预置旧非空制品、本次 runner 返回 `0` 但不写新制品”的回归测试并运行：

| 命令 | 实际结果 |
| --- | --- |
| `.venv\Scripts\python.exe -m pytest tests/test_validation.py -k preexisting_evidence_is_not_accepted_as_current_run_output`（修复前） | 红灯：`1 failed, 5 deselected`；旧制品被错误接受，`technical_checks_passed=True` |
| 同一命令（修复后） | `1 passed, 5 deselected`；`failed_checks` 明确包含 `lookahead`、`hyperopt`、`backtest_development`、`backtest_holdout` |

该回归测试同时作为本次直接冒烟证据：它预置旧的 CSV、`.fthypt` 和两份回测 JSON，验证入口只使用新建运行目录，旧制品不能冒充当前候选证据。

### 验证证据

| 命令或场景 | 实际结果 |
| --- | --- |
| `.venv\Scripts\python.exe -m pytest tests/test_validation.py tests/test_release.py` | `17 passed in 8.49s` |
| `.venv\Scripts\python.exe -m pytest` | `22 passed in 8.47s` |
| `.venv\Scripts\ruff.exe check .` | `All checks passed!` |
| `.venv\Scripts\python.exe -m pip check` | `No broken requirements found.` |
| `.venv\Scripts\python.exe -m compileall -q src tests` | 退出码 `0` |
| `git diff --check` | 退出码 `0` |
| `.venv\Scripts\freqtrade.exe list-strategies --no-color -c tests/fixtures/validation_cli/freqtrade.json --userdir tests/fixtures/validation_cli --strategy-path tests/fixtures/validation_cli --one-column` | Freqtrade `2026.8` 输出 `CliCandidate`，退出码 `0`；仅加载合成策略 |

### 未执行、限制与剩余风险

- 未下载行情、读取真实凭据或账户数据、连接交易所、启动 Freqtrade Dry-run 或交易服务；未运行真实历史数据的回测、Hyperopt、lookahead 或 recursive 链。
- 本切片仍不判断盈利、统计/经济准入或正式批准；报告即使技术检查通过，结论仍受后续策略准入阈值约束，`approval_pr_number = 0` 不授权运行。
- 任务尚未独立验收；下一步由控制塔创建新的只读验收线程，独立复核本次运行隔离、旧证据排除、报告/清单绑定、旧清单兼容和失败关闭。


### 交接结论

第二次返工已按验收意见完成最小根因修复，验证结果已写回仓库，任务交回 `READY_FOR_VERIFY`。本线程不自行验收、不关闭任务、不提交或推送 Git。

## 第三次独立验收结果（2026-09-16）

### 结论

- 状态：`ACCEPTED`。
- 验收线程：`30｜验收｜20260912-002 1.0 候选验证入口证据隔离复验`（`01a0aa12-d897-7be3-a62d-8a061d1fc249`）。
- 验收基线：目录 `D:/CodexProjects/projects/quant-research-live-lab`，分支 `codex/workbench-init`，HEAD `ba8738fe18289398a8c6713772416facddb5532d`；远程为 `https://github.com/JunYIChen12/quant-research-live-lab.git`。
- 验收线程未修改产品实现、测试或配置；只回写本任务、`CURRENT_TASK.md` 和 `TASKS.md` 的验收状态与证据。未提交、未推送、未启动 Dry-run、未连接交易所、未下载行情、未读取真实凭据或账户数据。

### 独立证据隔离场景

| 检查 | 实际结果 |
| --- | --- |
| 旧候选非空制品不能复用 | 预置旧 lookahead CSV、Hyperopt `.fthypt` 和开发/留出回测 JSON；本次 runner 对对应命令返回 `0` 但不生成新制品。结果 `technical_checks_passed=False`，`failed_checks` 包含 `lookahead`、`hyperopt`、`backtest_development`、`backtest_holdout`；旧文件保持不变。 |
| 连续运行隔离 | 同一候选和输出目录连续运行两次，生成不同运行目录和 `run_id`：`candidate-v0.1.0-8179dc14fb9946d7af8eab21bac01c39`、`candidate-v0.1.0-92a0595d2dd745afae38125cafcd41b9`。 |
| 运行身份与路径绑定 | `evidence.json` 与 `validation-report.json` 的 `run_id` 一致；命令 stdout/stderr 和制品路径均位于当前独占运行目录；两次运行目录不同；清单中的 evidence/report SHA-256 与实际文件一致。 |
| 路径异常失败关闭 | 临时清单分别验证路径逃逸、制品路径冲突和制品缺失，结果分别包含 `strategy_path_invalid`、`artifact_paths_not_unique`、`evidence_bundle_missing`，均未放行。 |
| 报告篡改拒绝 | 临时冻结 Git 清单初始验证允许；篡改 `validation-report.json` 后拒绝，并报告 `validation_report_hash_mismatch`。 |

### 回归与静态检查

| 检查 | 实际结果 |
| --- | --- |
| `.venv\\Scripts\\python.exe -m pytest tests/test_validation.py tests/test_release.py` | `17 passed` |
| `.venv\\Scripts\\python.exe -m pytest` | `22 passed` |
| `.venv\\Scripts\\ruff.exe check .` | `All checks passed!` |
| `.venv\\Scripts\\python.exe -m pip check` | `No broken requirements found.` |
| `.venv\\Scripts\\python.exe -m compileall -q src tests` | 退出码 `0` |
| `git diff --check` | 退出码 `0` |
| Freqtrade `2026.8` 合成 `CliCandidate` 加载 | `list-strategies` 输出 `CliCandidate`，退出码 `0` |
| 缺失 Freqtrade 可执行文件 | 真实 CLI 冒烟返回 `2`；`load`、`lookahead`、`recursive`、`hyperopt`、`backtest_development`、`backtest_holdout` 六项均失败，保持证据不足 |
| 既有兼容和授权边界 | 旧三制品清单兼容、`runtime_config` 篡改拒绝、`approval_pr_number=0` 不授权均通过 |
| 敏感信息扫描 | 未发现凭据、账户数据或明显敏感赋值匹配 |

### 限制与交接

- 未运行真实历史数据回测、Hyperopt、lookahead 或 recursive 链；未验证盈利、统计/经济门槛或策略是否适合交易。
- 未启动 Freqtrade Dry-run 或任何交易服务，未连接交易所，未读取真实凭据或账户数据；本次验收不构成 Dry-run、实盘或盈利授权。
- `approval_pr_number = 0` 仍表示没有正式发布批准；后续正式运行仍须通过现行 GitHub `release-approved` 验证和批准边界。
- 本次 `ACCEPTED` 只表示“1.0 候选验证入口”的实现已通过独立验收，不表示整个需求任务已 `CLOSED`。下一步交回 `00｜项目控制塔` 关闭本任务并更新项目状态；不自动开展 Web、Dry-run、Windows 宿主、候选并发试跑或 1.1。
